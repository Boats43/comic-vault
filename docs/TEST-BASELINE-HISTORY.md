# Test Baseline History

Moved here verbatim from CLAUDE.md's `### Directive preflight requirement`
subsection during the CLAUDE-COMPACT-1 pass (2026-08-23), per the
baseline-stamp compression ruling: CLAUDE.md keeps only the CURRENT
baseline stamp plus the one immediate predecessor; every earlier stamp in
the cascade lives here, verbatim, not merely reachable via git history.

The full, byte-identical pre-compaction CLAUDE.md text (all six cascading
entries, in their original wording) is also preserved by git history
itself — see the compaction commit's own message for the exact
pre-compaction SHA — but this file exists so the cascade doesn't require a
git archaeology exercise to read.

---

**Test baseline:** 222 PASS / 19 FAIL / 4 TIMEOUT / 245 total — HEAD (this commit), 2026-08-22 (GK-159, commit4-terminal floors instead of clears). Prior 220/19/4/243 (GK-157/158, `6ecbc5d`). Two new files, all fully passing: `tests/gk159-commit4-terminal-floor.test.js` (28/28 — computeIssueAuthorityContractPatch's new floor patch, the byte-identical genuine-no-price hard-clear control, the three untouched sibling branches — issueProvisional/the P1 commit-p carve-out/yearOnlyProvisional — the priorOut.refusedToPrice short-circuit precedence, full end-to-end deriveMarketStanding/deriveActionAuthority/assembleContract integration proving REVIEW/SIMILAR_ONLY/ISSUE_CONTESTED/listable=false, and a genuine no-price control through the same real downstream machinery), `tests/gk159-commit4-terminal-floor-handler-smoke.test.js` (18/18 — GK-138 real handler proof, the AWW shape: title/year/publisher resolve cleanly, only Vision's issue guess disagrees with a unanimous comps pool, card commits a real non-zero price at REVIEW/SIMILAR_ONLY/ISSUE_CONTESTED, never ID_REQUIRED/REFUSED/PRICED, listing stays not-listable). Four pre-existing suites updated as real, expected consequences of the GK-159 fix (each supplied a real, already-computed price alongside issueAuthority.status='conflicted' and asserted the now-superseded unconditional hard-clear behavior): `tests/q-trackB-commit4-adoption-provisional.test.js` (158/158), `tests/grailkey-directive-aq-followup-gk128-evidence-completeness.test.js` (23/23), `tests/q-trackB-commit4.3.1-retention-decline-fail-closed.test.js` (73/73 — also surfaces a genuine, disclosed finding: decisionEngine.js's computeDecision has no awareness of issueAuthority at all, so it independently resolves to LIST_NOW for this shape; listing stays correctly gated at the deriveActionAuthority/assembleContract layer regardless, the same established precedent the pre-existing P1 commit-p carve-out already relies on), `tests/q-trackB-commit4.3-winning-family-authority.test.js` (260/260, both CONTROL T1 and CONTROL T6). FAIL/TIMEOUT list (19+4) verified against the full, real filename set: confirmed byte-identical in composition to the prior 220/19/4/243 stamp — zero new regressions from this dispatch's `src/lib/issueAuthority.js`/`api/enrich.js` edits. GK-153/154/155/156/157 CLOSED and GK-152's eligibility half CLOSED this same dispatch (physical rescan evidence, `docs/TICKET-REGISTRY.md`) — no test-suite impact, registry-only.

Prior entry, retained for history: 220 PASS / 19 FAIL / 4 TIMEOUT / 243 total — HEAD (this commit), 2026-08-22 (GK-157/158, the residual gap and the discarded-evidence gap). Prior 216/19/4/239 (GK-153/154/155/156, `d360eaa`) plus 1 tracked addition since (`tests/assets-module-boundary.test.js`, DATA-1B, 17/17 — a separate, non-GrailKey data-infrastructure dispatch, not itself re-stamped here), giving 240 pre-existing tracked files this dispatch's own sweep started from. Three new files, all fully passing: `tests/gk157-elected-title-canonicalization.test.js` (13/13 — sanitizeSeriesTitle word-boundary protectedHit + resolveIdentity's FAMILY_OVERRIDE_DECISIONS canonicalizer projection, GI Joe mid-string fixture, Venom attribution-clause fixture, X-Men/Captain Marvel/Ms Marvel prefix-precedence + Joe Jusko/Casey negative controls), `tests/gk158-provisional-issue-match.test.js` (24/24 — classifyEvidenceRow/buildEvidencePopulations matching-row admission under provisional/conflicted issue authority, mismatching-row rejection, the untouched deriveMarketStanding/deriveLocks floor, genuine no-consensus-junk negative control), `tests/gk157-158-handler-smoke.test.js` (17/17 — GK-138 real handler proof: Part 1/2 re-run the byte-identical GK-153-156/GK-152 production fixtures through the real `/api/enrich` handler with zero regression, Part 2 additionally surfaces an organic confirmation that a title-matching row under conflicted issue authority is admitted in the real handler, excluded only by an unrelated WRONG_VARIANT mechanism; Part 3 calls the real, exported `api/comps.js:fetchComps` directly with issue pre-resolved to a contested value, proving 2 matching comps price while a 3rd mismatching comp does not, with the SIMILAR_ONLY/ISSUE_CONTESTED floor intact). Two large pre-existing integration suites updated as real, expected consequences of the GK-158 fix (their old assertions encoded the now-corrected blanket-demotion behavior): `tests/q-trackB-commit4-adoption-provisional.test.js` (156/156) and `tests/grailkey-dispatch-25-fix1-instrumentation.test.js` (25/25, plus a new GK-35 side-finding documented in `docs/TICKET-REGISTRY.md`, not fixed — out of scope). FAIL/TIMEOUT list (19+4) verified against the full, real filename set, not assumed: confirmed byte-identical in composition to the prior 216/19/4/239 stamp — every one of the 19 FAIL and 4 TIMEOUT files reproduces identically with `src/lib/identityCore.js`/`src/lib/evidenceEligibility.js` stashed back to their pre-this-dispatch state (direct `git stash` before/after comparison, not assumed), confirming zero new regressions from this dispatch's two library edits. No pre-existing test's assertions needed updating beyond the two large suites named above (both anticipated, not accidental).

Prior entry, retained for history: 216 PASS / 19 FAIL / 4 TIMEOUT / 239 total — HEAD (this commit), 2026-08-22 (GK-153/154/155/156, G.I. Joe #5 Kirkham dispatch train). Prior 211/19/4/234 (GK-152, `3aa4126`). Five new files, all fully passing: `tests/gk153-gijoe-compound-whitelist.test.js` (12/12 — sanitizeSeriesTitle compound-whitelist protection, real fixtures + Joe-Jusko negative controls), `tests/gk154-year-anchor-gate.test.js` (9/9 — the narrow pcYear/resolveYear anchor-rejection predicate, genuine-anchor negative controls, live-source wiring proof that q141-a itself is untouched), `tests/gk155-variant-dedup.test.js` (7/7 — extractConfirmedVariant token-set dedup, no-overlap and case-insensitive controls), `tests/gk156-poolyearhint-eralock-rawtitle.test.js` (7/7 — the r.title/r.rawTitle field fix, real-pool reproduction of both the bug and the fix, live-source regression guard), `tests/gk153-156-gijoe-handler-smoke.test.js` (8/8 — GK-138 real handler proof, all four fixes proven together end to end: title retains "joe," year does not confidently land on the wrong 1987 anchor). FAIL/TIMEOUT list (19+4) verified against the full, real filename set: every one of the 19 FAIL and 4 TIMEOUT files is a pre-existing, already-documented entry (GK-89/90/91, the "Known stale test suites" paragraph, and the 2026-08-11-C sweep's own named set) — zero new failures. One TIMEOUT (`grailkey-directive-aj-http-handler.test.js`) was not previously named in this stamp's own history; re-run directly with a longer timeout and confirmed 13/13 PASSING in 44s — the same sweep-cutoff-artifact class already documented for `dispatch-42-comicvine-kill.test.js`/`grailkey-commit-m-pc-query-fallback.test.js` (a real handler-smoke test with several real fetch-stub round trips, genuinely slow, not hung or broken), not a new regression. No pre-existing test's assertions needed updating this dispatch (unlike GK-152's writer-site count bump) — none of the four fixes changed a count or shape any other suite asserts on.

Prior entry, retained for history: 211 PASS / 19 FAIL / 4 TIMEOUT / 234 total — HEAD (this commit), 2026-08-22 (GK-152, comps-pool issue rescue). Prior 206/19/4/229 (Phase 0.3, GK-142/GK-143, `551db6b`) — note that figure undercounted the real tracked-file total by 1 at the time (230, not 229; a pre-existing stamp drift, corrected here, not introduced by this dispatch) plus 1 tracked addition since (`data-0e-pilot-review-tier-numbering-fixtures.test.js`, DATA-0E-PILOT), giving 231 pre-existing tracked files this dispatch's own sweep started from. Three new files, all fully passing: `tests/gk152-comps-issue-rescue.test.js` (11/11 — unit coverage of `rescueIssueFromCompsPoolConsensus`, including the genuine no-issue/empty-pool/non-unanimous/sub-floor/graded/partial-unanimity controls), `tests/gk152-market-standing-issue-contested.test.js` (6/6 — `deriveMarketStanding`'s new issue-facet CONTESTED floor + the matching `responseContract.js` lock), `tests/gk152-handler-smoke.test.js` (11/11 — GK-138 real handler proof, real production title mix). FAIL/TIMEOUT list (19+4) verified against the full, real filename set, not assumed: every one of the 19 FAIL and 4 TIMEOUT files is a pre-existing, already-documented entry (GK-89/90/91, the "Known stale test suites" paragraph, and the 2026-08-11-C sweep's own named set) — zero new failures. One pre-existing count-assertion updated as a real, expected consequence of this dispatch's own fix: `grailkey-directive-aq-canonical-facet-authority.test.js`'s "exactly 4 remaining `out.issueAuthority` writer sites" became 5 (GK-152's rescue is a genuinely new, independently-justified 5th site, reusing the same `projectIssueAuthority` projection — never a competing authority mechanism — matching that test's own established discipline for enumerating legitimate writers). Full history: Pattern Library "GK-152."

Prior entry, retained for history: 206 PASS / 19 FAIL / 4 TIMEOUT / 229 total — HEAD (this commit), 2026-08-21 (Phase 0.3, GK-142/GK-143 fixes). Prior 203/19/4/226 (GK-148, `48b44b4`). Three new files, all fully passing: `grailkey-gk143-title-corruption.test.js` (14/14 — sanitizeSeriesTitle Unicode-boundary fix, exact r5v6b repro + é/ü/ñ/' control corpus), `grailkey-gk142-series-core-query.test.js` (24/24 — deriveSeriesCoreQuery unit coverage), `grailkey-gk142-series-core-query-handler-smoke.test.js` (12/12 — GK-138 real handler proof). FAIL/TIMEOUT list verified byte-identical to the prior baseline by DIRECT DIFF, not assumed: the full 226-file sweep was re-run against the pre-Phase-0.3 code (`git stash` of exactly the 3 touched source files) and diffed line-for-line against the post-fix 229-file sweep — the only delta is the PASS/TOTAL counts (+3), the FAIL and TIMEOUT filename lists are identical. Full history: Pattern Library "GrailKey Dispatch 2026-08-21" and "Phase 0.3."

Prior entry, retained for history: 202 PASS / 19 FAIL / 4 TIMEOUT / 225 total — HEAD, 2026-08-21 (GrailKey Dispatch 2026-08-21, GK-145/146). Prior 201/19/4/224 (AW, `a185425`). One new file, fully passing: `grailkey-dispatch-2026-08-21-gk145-gk146-measurement.test.js` (27/27 — Part 1 unit coverage of `buildScanLogRecord`'s two new fields, Part 2 real `/api/enrich` handler proof that `collectionItemId` and `outcome.*` reach the built scanlog record unchanged, Part 3 proves a free-standing scan with no `collectionItemId` still completes cleanly with a legitimate null). FAIL/TIMEOUT list byte-identical to the prior baseline (same 19 named files, same 4 named files — confirmed by direct file-name comparison, not just count). Full history: Pattern Library "GrailKey Dispatch 2026-08-21."

Prior entry, retained for history: 201 PASS / 19 FAIL / 4 TIMEOUT / 224 total — HEAD, 2026-08-20 (Directive AW, GK-140). Prior 199/19/4/222 (AV, `3a85c17`). Two new files, both passing: `grailkey-directive-aw-title-canonicalization.test.js` (24/24, canonicalizeTitleCandidate/reconcileTitleFacet unit coverage — clean-candidate/verbatim-retention/no-over-strip/cooperative-path-byte-identity) and `grailkey-directive-aw-handler-smoke.test.js` (16/16, GK-138 real-handler proof — the clean-query eBay comps search returns a real 8-item pool where the pre-fix noisy query returned nothing; PriceCharting's own product lookup not firing at all in this fixture is a separate, pre-existing gap outside this dispatch's scope, not asserted on — logged as GK-141). FAIL/TIMEOUT list byte-identical to the prior baseline. Two real bugs found and fixed mid-build (not shipped broken), neither in the directive's own text: (1) a TDZ circular-import hazard — `new RegExp(SLAB_RE.source, ...)` evaluated at identityCore.js's own MODULE TOP LEVEL depended on compHygiene.js having already finished initializing, which held for some test entry points and threw `ReferenceError: Cannot access 'SLAB_RE' before initialization` for others purely as a function of import order; fixed by deferring all such reads inside the function body (lazy, post-module-load); (2) a global-regex `lastIndex` statefulness bug — a shared, `g`-flagged RegExp module singleton mutates `lastIndex` on `.test()`, risking a later call silently missing a match at the start of a different string; fixed via a shared `applyStrip` helper that builds a fresh RegExp per call and uses `.match()` (spec-reset regardless of prior state) instead of `.test()`+a second `.match()`. Both caught by running the FULL suite, not the dispatch's own named fixtures alone — the same discipline every prior GrailKey dispatch has required. **Standing rule: re-stamp this line in the same commit as any test-file add/remove.** Full history: Pattern Library "GrailKey Directive AW."
