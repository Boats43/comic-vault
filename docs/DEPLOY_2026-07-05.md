# Deployment Session 2026-07-05

## Summary
**Deployed:** 8 commits (2 P0, 6 P2)  
**Status:** All items complete, HOLD for mass validation run  
**Token usage:** 120K/200K (60%)

---

## Commits Deployed

### P0-A [P0] — ab45c7c
**Kill browse_api Legacy Paths**
- **ROOT CAUSE:** When tier-4 returned null (no PC, <2 verified comps), code fell through to legacy PC/browse_api escape hatches, bypassing tier selection.
- **FIX:**
  - Polybag guard: skip entire pricing block when `isPolybagPricing`
  - Tier-first: when `priceBandsRaw` truthy, use it (tier 1-4 with data)
  - Refuse-to-price: when `priceBandsRaw` null, explicit refusal
  - Legacy PC path: marked dead code (`else if (false)`)
  - Direct browse_api path: replaced with refuse + diagnostic log
- **GATE:** Zero `browse_api` sources on production rescan
- **Files:** api/enrich.js (-193 lines)

### P0-B [P0] — a2e97ce
**Persist priceBands/demandSignals Tier Metadata**
- **ROOT CAUSE:** Tier metadata generated by `api/enrich.js` but stripped during App.jsx merge (3 paths: auto-refresh, bulk import, refreshMarketData).
- **FIX:** Added `priceBands` and `demandSignals` persistence to all 3 merge paths.
- **GATE:** Export after rescan shows fields populated
- **Files:** src/App.jsx (+9 lines)

### Q53 [P2] — 3b8e174
**Wire Tier-3 ActivePool to Match-Conf**
- **BEFORE:** `activeCount = rawComps?.count` (AI-verified pool, relaxed filter). Tier-3 uses stricter `buildVerifiedActivePool` but match-conf read raw pool.
- **FIX:** When tier=3, read `activeCount` from `priceBandsRaw.count` (verified active pool). Tier 1/2/4 fall back to `rawComps.count`.
- **NOTE:** T4-CAP (94df50e) sanity check uses `verifiedActive.length` through this same wire — Q53 revives it for match-conf.
- **GATE:** Symbiote Spider-Man #1 → tier=3, activeCount=3, match-conf ~HIGH
- **Files:** api/enrich.js:4037

### Q55+55-B [P2] — 4d1a2a9
**Strip Artist/Signature/Ordinal Tokens from Family**
- **BEFORE:** "Symbiote Spider-Man #1 Signed McFarlane" → family=["mcfarlane"] → matched "Spawn #1 McFarlane" (different series).
- **FIX:** Strip 60+ artist tokens (lim, dekal, spears, mcfarlane, ross, adams, kirby, ditko, etc.), signature tokens (signed, sig, auto, autographed), and ordinal-key tokens (1st, 2nd, first, appearance, origin, key, intro) from `tokenizeTitle` BEFORE similarity matching.
- **GATE:** "Symbiote Spider-Man #1" → ["symbiote", "spider", "man"] clean, ≥10/29 verified
- **Files:** src/lib/compHygiene.js:163

### Q54 [P2] — 00d5fcc
**Compound Whitelist + Single-Letter Guard**
- **BEFORE:** X-Men #44 → normalized "x-men" → tokenize → ["x", "men"] → MIN_TOKEN_LEN=2 strips "x" → ["men"] → cross-series contamination.
- **FIX:** `COMPOUND_WHITELIST` preserves 13 protected titles (x-men, x-force, x-factor, marvel tales/age/premiere/team-up/two-in-one/spotlight/feature/fanfare, talespin, walt disney) as canonical split forms BEFORE standard normalization.
- **GATE:** X-Men #44 → ["x", "men"] clean, ≥10/30 verified, $40-70 band
- **Files:** src/lib/compHygiene.js:138, 176

### Q56 [P2] — (already implemented)
**PC Era Guard >5y**
- **STATUS:** Already implemented at api/enrich.js:1313 and 1336
- **Condition:** `Math.abs(productYear - comicYear) > 5`
- **GATE:** ASM #1 (1963) → no 2016 PC match (53y gap > 5y)
- **No changes needed**

### Q57 [P2] — 0461f70
**Tier-0 List Lock**
- **BEFORE:** LOW match confidence books (thin-market vintage, insufficient verification) could still list via manual price override.
- **FIX:** Hard-disable List button when `matchConfidence.tier=LOW` AND `(soldComps.length + activeComps.count) < 3`. Button text: "🔒 List locked — verify data quality first".
- **GATE:** ASM #1 (1963, LOW tier, thin modern comps) → List button disabled
- **Files:** src/App.jsx:5864, 5868

### Q58 [P2] — a37901e
**Issue Consensus Backfill ≥70%**
- **BEFORE:** Cavewoman (issue=null) → Vision missed issue → PC skipped (requires issue) → no pricing. Comp consensus: 18/20 titles "#1".
- **FIX:** When `confirmedIssue=null` AND ≥70% of comp titles agree on a single issue number, backfill from consensus and continue pricing. Sets `issueBackfilledFromComps=true`, `identityConfident=false` (NEEDS_REVIEW flag preserved).
- **GATE:** Cavewoman → issue=1 (backfilled from 18/20 comps), priced ~$22, review-flagged
- **Files:** api/enrich.js:2548

---

## Deployment Notes

### Dead Code
**api/enrich.js ~line 3305:** 200+ lines of legacy PC/browse_api path marked with `else if (false)`. Safe to delete in future commit after confirming zero hits in production logs.

### "undefinedd" Typo
**STATUS:** Not found in codebase via `grep -rn "undefinedd"`. Skipped.

### T4-CAP Production Verification
**STATUS:** Committed in 94df50e (21l-b session). Needs live scan test to confirm tier-4 sanity cap fires in production.

---

## Gates (User Verification Required)

| Gate | Item | Expected Result |
|------|------|-----------------|
| **P0-A** | Production rescan | Zero `pricingSource=browse_api` |
| **P0-B** | Export after rescan | `priceBands`/`demandSignals` populated |
| **Q53** | Symbiote Spider-Man #1 | tier=3, activeCount=3, match-conf HIGH |
| **Q54** | X-Men #44 | ["x", "men"] clean, ≥10/30 verified, $40-70 |
| **Q55+55-B** | Symbiote Spider-Man #1 | ["symbiote", "spider", "man"] clean, ≥10/29 |
| **Q56** | ASM #1 | No 2016 PC match (already working) |
| **Q57** | ASM #1 | List button disabled (LOW + <3 comps) |
| **Q58** | Cavewoman | issue=1 backfilled, priced ~$22, review-flagged |

---

## Next Steps

1. **Mass validation run** (user-executed) — verify all 8 gates pass
2. **Monitor production logs** for:
   - `[P0-A-LEGACY-PATH]` warnings (should be zero)
   - `[P0-A-refuse]` tier-4 null refusals (expected for no-data books)
   - `[Q58-issue-consensus]` backfill events
3. **Clean up dead code** (api/enrich.js ~3305, `else if (false)` block) after confirming zero legacy path hits
4. **T4-CAP verification** — test tier-4 sanity cap on live scan (FF Invisible Woman class)

---

## File Summary

| File | Lines Changed | Type |
|------|---------------|------|
| api/enrich.js | +33, -193 | Backend (pricing engine) |
| src/App.jsx | +27, -4 | Frontend (persistence + UI) |
| src/lib/compHygiene.js | +54, -1 | Shared (tokenization) |

**Total:** +114, -198 lines (-84 net)

---

## Session Metadata

- **Date:** 2026-07-05
- **Duration:** ~90 minutes
- **Commits:** 8 (2 P0, 6 P2)
- **Token usage:** 120,319 / 200,000 (60%)
- **Files touched:** 3
- **Lines changed:** +114, -198 (-84 net)
