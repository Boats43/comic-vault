# GrailKey Ticket Registry

Greppable, one line per ticket. No prose beyond the one-line summary. This file
is the state contract — any directive referencing a ticket ID or a structural
fact about this repo must run the preflight check against this file before
doing work (see CLAUDE.md, "Directive preflight requirement").

**Alias rule, absolute:** every retired identifier appears in the `aliases:`
field of its successor. A grep for a dead ticket number must return the live
line below. A dead number must never be greppable as OPEN.

Format: `ID | STATUS | SHA | DATE | ONE-LINE | aliases:`
Status vocabulary (closed set): `OPEN` `CLOSED` `RENAMED` `SUPERSEDED` `DEFERRED`

Populated from `git log` and `docs/PATTERN-LIBRARY.md` on 2026-08-11 against
HEAD `1aa6eb0`. Five tickets could not be conclusively resolved from evidence
in this repo and are marked `UNKNOWN` — see the note block at the bottom.

```
GK-19 | UNKNOWN | -       | -          | Q22/hasSufficientTitleOverlap bigram-join relocation target — status not established | aliases:
GK-21 | CLOSED  | -       | -          | applyVariantFallbackDivergenceCap — divergence-cap mechanism, has its own EX-A(a)-(d) coverage | aliases:
GK-24 | UNKNOWN | -       | -          | printing-axis fallback trigger (priceBands.js) — status not established | aliases:
GK-29 | OPEN    | -       | -          | gradeMult=0.55 displayed but not applied — recurrence logged, untracked | aliases:
GK-31 | CLOSED  | -       | -          | activeAnchoredOverFallbackSold — tier-3 active-anchor-over-fallback-sold mechanism, shipped (regression-test coverage gap tracked separately, not this ticket) | aliases:
GK-34 | CLOSED  | -       | 2026-08-07 | MIN_POOL_FOR_OVERRIDE=3 floor — single thin sold comp no longer overrides N actives (mirror of GK-21), Bone #1 class | aliases:
GK-35 | OPEN    | -       | 2026-08-07 | PRICING_GATE_CODES gap — log only. **Concrete new instance found while building GK-158 (2026-08-22), still not fixed here — scope discipline, not an oversight.** `PRICING_GATE_CODES` (`src/lib/evidenceEligibility.js`) does not include `WRONG_ISSUE` — confirmed directly, not assumed (`tests/grailkey-dispatch-25-fix1-instrumentation.test.js` Section 4 asserts this live) — so `buildPricingEligibleRows`/`isPricingMathEligible` (the real gate both `api/comps.js` and `soldVerification.js` call) do not exclude a row `classifyEvidenceRow` itself correctly marks `rawPricingEligible=false` for a plain issue mismatch. Before GK-158, a mismatch under provisional/contested issue authority was safely caught anyway, because the OLD blanket-reject behavior produced `TARGET_ISSUE_PROVISIONAL_AUTHORITY` (which IS gate-listed) for every row regardless of match. GK-158 makes this combination reachable for the first time: a row that mismatches under provisional/contested authority now produces `WRONG_ISSUE` instead, which this gate ignores. Not currently observed to matter in the real pipeline — both `api/comps.js`'s own per-attempt issue-number pre-filter and `soldVerification.js`'s own dedicated `issueMismatch` check already remove plainly-mismatched rows earlier, before `classifyEvidenceRow`/`buildPricingEligibleRows` ever runs (confirmed via direct trace on both paths, not assumed) — but those are simpler checks than `hasIssueNumber`'s own more nuanced logic, so a row that fools the earlier filter but not `hasIssueNumber` could reach this exact gap. Candidate fix (not built): add `'WRONG_ISSUE'` to `PRICING_GATE_CODES` — a real pricing-math-adjacent change requiring its own scoping and full regression pass against every existing `PRICING_GATE_CODES` consumer, not a one-line addition folded into GK-158's scope. | aliases:
GK-36 | OPEN    | -       | 2026-08-07 | [ship11] visual-pool-fallback median not grade-scoped — log only | aliases:
GK-37 | OPEN    | -       | 2026-08-07 | PUBLISHER_CONSENSUS_PATTERNS no bare-word fallback for major publishers — confirmed not root cause of the case that found it; still open, log only | aliases:
GK-38 | DEFERRED| -       | 2026-08-08 | Fix 2's issue-axis path renders a correction box on a book it just confirmed (identityProvisionalFields not cleared on promotion) — log only, not fixed | aliases: GK-38(draft, Dispatch 48 — unrelated collision, see GK-66)
GK-39 | CLOSED  | f03d564| 2026-08-10 | Fabricated authenticationScore/breakdown/confidence/needsReview deleted; listing gate at App.jsx:7506 removed (GK-39A) | aliases:
GK-40 | OPEN    | -       | 2026-08-08 | Three-to-four independent variant-token vocabularies (drifted-duplicate-constant class) — consolidation not scoped | aliases:
GK-41 | CLOSED  | -       | 2026-08-xx | Non-comic gate fix — shipped same commit as the Dispatch 29/30 wording fix it was found alongside | aliases: GK-41(draft, Dispatch 48 — unrelated collision, see GK-68)
GK-42 | OPEN    | -       | 2026-08-08 | Spawn #351 "Spawn #1" near-miss (commit4.3.1 family-conflict margin decline, prior=1 fabricated issue) — re-check before scoping, may self-resolve via Fix 3a; last checked Dispatch 31, unchanged | aliases: GK-42(draft, Dispatch 48 — unrelated collision, see GK-67)
GK-43 | DEFERRED| -       | 2026-08-08 | gradeBlob non-comic title check — pre-existing, deliberately untouched | aliases:
GK-44 | DEFERRED| -       | 2026-08-08 | Vision unstated-criterion reasoning-shortcut pattern — standing rule for future prompt changes, not a code defect | aliases:
GK-45 | OPEN    | -       | 2026-08-08 | convergence.tier doesn't actually certify PC agreement on issue/title (dead .issue axis, missing title/pc key) — log only | aliases:
GK-46 | OPEN    | -       | 2026-08-08 | soldVerification.js:934 zero-pool variant fallback already unconditionally reachable regardless of variantIdentitySource — log only | aliases:
GK-47 | UNKNOWN | -       | -          | Referenced as a pre-existing externally-assigned label (App.jsx, Dispatch 48) — never formally logged in this repo | aliases:
GK-48 | UNKNOWN | -       | -          | Referenced as a pre-existing externally-assigned label (App.jsx fold-in, reconfirmed live Dispatch 48) — never formally logged in this repo | aliases:
GK-49 | UNKNOWN | -       | -          | Referenced as a pre-existing externally-assigned label (api/comps.js:442 fold-in, reconfirmed live Dispatch 48) — never formally logged in this repo | aliases:
GK-50 | OPEN    | -       | 2026-08-10 | Logged verbatim as reported by operator (Dispatch 42-I) — not investigated | aliases:
GK-51 | OPEN    | -       | 2026-08-10 | Era filter returns bypassed:false while actually bypassing | aliases:
GK-52 | OPEN    | -       | 2026-08-10 | yearCorrected true when the raw year was empty | aliases:
GK-53 | OPEN    | -       | 2026-08-10 | noImage:true on scans that had images | aliases:
GK-54 | OPEN    | -       | 2026-08-10 | comicVine:{} stored on no-match — 3 of 5 production records | aliases:
GK-55 | CLOSED  | cf96d1b| 2026-08-10 | Shipped same push as Dispatch 42-I (SAFE-KILL certification) | aliases:
GK-56 | OPEN    | -       | 2026-08-10 | Same eBay listing ID present in both active and sold pools — double-counted as two independent observations | aliases:
GK-57 | OPEN    | -       | 2026-08-10 | [bulk] "year healed" and [persist-bulk] both logged twice for one savedId | aliases:
GK-62 | CLOSED  | 33079e4| 2026-08-10 | convergenceSources' vision slot no longer self-corroborates (a value must not vote for itself) | aliases:
GK-63 | RENAMED | -       | 2026-08-10 | Retired Dispatch 46B — see GK-65 (renumbered so GK-62 stays unique to the vision-provenance fix) | aliases: -> GK-65
GK-64 | OPEN    | -       | 2026-08-10 | identityAlignment is a stale Phase-2 snapshot vs. terminal out.confirmedYear, unmarked — not worked | aliases:
GK-65 | SUPERSEDED | f03d564 | 2026-08-10 | identityAlignment never persisted to IndexedDB (listing gate structurally unreachable) — resolved not by fixing persistence but by deleting the gate outright in the same push as GK-39A; underlying non-persistence fact itself still true, just nothing depends on it now | aliases: GK-63(draft)
GK-66 | CLOSED  | d07c236| 2026-08-10 | priceLadder removed as HOLD_FOR_CGC decision authority | aliases: GK-38(draft, Dispatch 48)
GK-67 | CLOSED  | c0653a5| 2026-08-10 | Pricing-source display truth (blend-line + source-label fixes), zero computation change | aliases: GK-42(draft, Dispatch 48)
GK-68 | OPEN    | -       | 2026-08-11 | Quick band never floor-checked. Was DEFERRED (2026-08-10) on the premise that operational band-suppression removed the exposure; reclassified to OPEN (Directive G Task 3) — Directive F proved the gap surfaces through the decision-warning panel (recommended-below-floor), which is NOT suppressed, and GK-74 made its detection live in production the same day. The deferral premise is false. Not fixed here — Directive G Task 1 fixed the false "raised to $X" wording only, not the underlying missing floor enforcement | aliases: GK-41(draft, Dispatch 48)
GK-69 | OPEN    | -       | 2026-08-10 | Pricing-derivation custody gap — priceDerivationTrace/blendedAvg never persist to IndexedDB, priceLadder does — logged, not fixed | aliases:
GK-70 | OPEN    | -       | 2026-08-11 | api/comps.js:1803 — eBay grade-proximity comp filter falls back to the FULL unfiltered pool when the grade filter would remove every comp (Resurrect-Rejected-Evidence shape, 4th instance, found sweeping GK-66-class gates in Directive B Task 3). Deliberately NOT fixed: closing it changes which comps enter price computation — pricing-math boundary, needs its own greenlight | aliases:
GK-71 | CLOSED  | f6ce28e | 2026-08-11 | Three ComicVine identity gates (year-strict, token, publisher) no longer restore the rejected candidate set when the filtered set is empty — candidates.length=0 instead, matching the reprint-publisher gate's pre-existing Q99 precedent. Generalizes the standing "Rejection Must Not Create Authority" invariant | aliases:
GK-72 | CLOSED  | bbcb716 | 2026-08-11 | eBay UNAVAILABLE != EMPTY — emptyComps() classifies outage vs genuine-zero at the source, out.ebaySourceUnavailable/Reason surfaced unconditionally (not just in the refuse-to-price path), decisionEngine escalates to RESEARCH via a new critical warning, refuse-to-price priceNote distinguishes outage from a genuine zero-market book | aliases:
GK-73 | OPEN    | -       | 2026-08-11 | out.ebaySourceUnavailable/Reason merge gap in App.jsx — CORRECTED on direct code inspection from the "3 sites, all dropping" premise this ticket was opened against. Two sites confirmed genuinely dropping a fresh flag: duplicate-confirm (~App.jsx:12845) and reIdentifyBook (~App.jsx:12093) both build their updated catalogue object from an explicit field list (`{ ...cur/item, field: enrich.X ?? cur.X, ... }`) that never references ebaySourceUnavailable/Reason at all, so a genuine new true from a fresh /api/enrich response is silently discarded and whatever the old item had (often nothing) survives instead. The third originally-suspected site, submitManualCorrection (buildCorrectedCatalogueItem, src/lib/manualCorrection.js), does NOT exhibit this — its merge is a full spread of the enrichData response (`{...cleared, ...enrichData}`), and ebaySourceUnavailable/Reason are in neither its clear-list nor its preserve-list, so a genuine new true is copied through correctly. It has a narrower, different gap instead: api/enrich.js:6240-6244 only ever explicitly sets out.ebaySourceUnavailable=true (no else branch clears/sets it false on the healthy path), so a stale true from before a correction can survive a correction whose new response is genuinely healthy and simply omits the key. TRIGGER (duplicate-confirm/reIdentifyBook): a re-identified or duplicate-confirmed book shows a normal price where it previously would have shown eBay-unavailable. TRIGGER (manual correction): a corrected book still shows eBay-unavailable after a correction that actually resolved cleanly. Deliberately deferred under Operator Mode Hold — not fixed, App.jsx not touched. | aliases:
GK-74 | CLOSED  | 359f751 | 2026-08-11 | decisionEngine.js's numeric safety checks were dead code against real production data — `item.price`/`systemPrice` is a currency STRING ("$800.00", via `fmtUsd()`) by the time `computeDecision(out,...)` runs, and every magnitude-based `>`/`<` comparison against it (catastrophic-system-overprice blocker, active-floor-far-below, recommended-below-floor, active-avg-far-below CRITICAL warning) silently evaluated false via NaN coercion. FIXED: `computeDecision` parses `item.price` to a local numeric (`systemPrice`, via `responseContract.js`'s `parsePriceNumber`) once, near the top, before any of these CHECK sites — reactivating all four on real data (Bone #1 real-shape regression: LIST_NOW → DO_NOT_LIST). SCOPE LIMIT, deliberately not closed by this fix: every `decision.price =` COMPUTATION site (`decisionEngine.js:862-865` et al.) still reads the raw `item.price` STRING unchanged, and `computeBestChannel`'s own separate `item.price` read is independently untouched — reaching either would unmask new values into the UI and needs its own greenlight. That remaining scope is GK-75, not this ticket. Verified: `tests/grailkey-dispatch-74-price-coercion.test.js` (12/12), `tests/decision-engine.test.js` 39/7/46 unchanged (documented baseline), zero regressions across 30 other dependent test files | aliases:
GK-75 | OPEN    | -       | 2026-08-11 | GK-74's explicit scope limit, not yet worked: remaining formatted-price ("$X.XX" string) read sites in `decisionEngine.js` that still do raw numeric arithmetic/comparison against `item.price` without `parsePriceNumber` coercion. Two named instances: the catalogue exit-strategy filter `c.price < 10` (~line 816, part of `computeBestChannel`'s bundle-routing logic) and `computeBestChannel`'s own top-of-function `item.price` read (~line 16, `const price = item.price ?? null;` then used directly in `price > 50`/`price < 25` comparisons a few lines later) — both independently NaN-prone on the real fmtUsd()-string shape, same root cause as GK-74, different call sites GK-74 deliberately left untouched. TRIGGER: exit-strategy recommendation (grade/barter/bundle routing) is identical across all books regardless of price or grade — the price-magnitude branches in `computeBestChannel` never numerically evaluate on real data, so every book falls through to the same default routing regardless of its actual price. Touches `decisionEngine.js`'s decision logic (not pure display) — pricing/decision-math boundary, needs its own greenlight before implementation | aliases:
GK-76 | OPEN    | -       | 2026-08-11 | Validation Bypass on Authority Replacement (Directive G Task 3/4) — `api/enrich.js:6649-6661` (the 22c-title-revote path) writes a fresh `out.pcProductId`/`out.pcProductName` from a re-queried PriceCharting match WITHOUT re-running the `pc-anchor-gate` year/name/discriminator validation (`api/enrich.js:5045-5126`) that governed the ORIGINAL PC match. A revoted match receives strictly less scrutiny than the first one did. Distinct failure class from Computed-Then-Discarded (nothing here is computed and discarded — a NEW value bypasses a gate that governed its PREDECESSOR); see the Authority Propagation Invariant, `docs/PATTERN-LIBRARY.md`. TRIGGER: a book whose title is revoted mid-scan (unanimous rejection + PC no-match on the original title, per the `[22c-title-revote]` log tag) renders PC evidence that would have failed the original anchor gate had it been checked against the revoted title/year. Not fixed — found during Directive F's Task 1 trace, logged here per Directive G Task 3's instruction; needs its own scoping (does the revote re-run the full gate, or a narrower re-check?) before implementation | aliases:
GK-77A | OPEN | -       | 2026-08-12 | Directive J Task 1 (split from GK-77, Directive I) — `REPRINT_RE` (`src/lib/compHygiene.js:38`, `api/comps.js:1463` Filter 1) has incomplete Nth-print coverage: spelled-out ordinals stop at "fourth print" (no "fifth print"+), numeral forms stop at "5th print" with no 6-9th/2-digit/generic catch-all — unlike the sibling `PRINTING_PATTERNS` catalog (`src/lib/imageSearchIdentity.js:152-160`, added Q116/GrailKey-D03) which already has a general `[6-9]th\|\d{2,}` pattern. A second, independent drifted-duplicate-constant instance (GK-40 class). TRIGGER: real production evidence, Bone #1 (1991) — active-pool listings titled "FIFTH PRINTING" (spelled out, no "5th") and "8TH PRINTING" (no pattern covers 6-9th at all) both pass Filter 1 as if first-print, unflagged (not even via the `reprintFallback`\="keep all" escape hatch, since they never registered as reprints to begin with) — contaminating the comp pool that produced Bone's $18.56 price with a mix of 1st-11th-printing copies spanning ~$18-$1,600. **BLOCKED ON GK-77B. Must not ship independently.** `api/comps.js` Filter 1 removes reprint comps *unless our own book's variant claims an Nth print*. With printing unknown (the normal case — indicia is interior and never photographed), completing the regex causes the filter to treat our book as a first print and remove later-printing comps on that assumption. Completing the regex while the scanned book's printing remains unknown would cause newly-recognized later-print comps to be removed from the pool of an unknown-printing book, and can drive the surviving pool — and therefore the price — upward. Bone #1 (1991) is the concrete risk case: its active pool contained "FIFTH PRINTING" and "8TH PRINTING" listings that the current regex does not recognize. **The exact post-fix pool size and price delta have not been simulated** — the structural risk is what blocks this ticket, not a measured figure. The current gap produces a roughly-correct price by accident; the regex alone replaces a wrong filter that lands near-right with a right filter running on a false premise. Not fixed — pricing-math boundary, needs its own greenlight, and needs GK-77B resolved first. | aliases: GK-77(original)
GK-77B | OPEN | -       | 2026-08-12 | Directive J Task 1 (split from GK-77, Directive I) — scanned-book printing authority is absent entirely. No dedicated printing field exists anywhere in the schema. `confirmedVariant` can carry a printing token only when Vision sees explicit printing text on the front cover (`api/enrich.js:5526-5543`) — which does not happen for the class of book that matters, since 1st and 5th print covers are visually identical and printing is normally determined from the interior indicia, never photographed in a standard scan. `assessPcAnchorTrust` (`src/lib/evidenceEligibility.js:979-999`) takes five parameters (`pcPrice`, `pcYear`, `confirmedYear`, `pcMatchRejectedForYearConflict`, `identityConflictCount`), none of them printing, and grants `EXACT_EDITION` on year drift ≤ 1 alone regardless. GrailKey knows more about other sellers' printing than about the printing of the physical book in front of the camera — `PRINTING_PATTERNS` and `REPRINT_RE` both classify *their* listing titles; nothing classifies *our* book. Not fixed — needs a design decision (Jimmy) on field shape, block-vs-warn, persistence, and unknown-state semantics before any implementation; see Directive I's Task 1 item 6 enumeration (what a printing input would need to reach) and item 4 (six specific open build decisions). | aliases: GK-77(original)
GK-78 | OPEN    | -       | 2026-08-12 | Directive I Task 2, reworded Directive J Task 1 — `computePriceBandsFromSold`'s tier-2.5 branch (`src/lib/priceBands.js:711`, `else if (freshCount === 0 && soldPrices.length >= 5) tier = 2.5`) grants full price authority to the sold pool by COUNT alone, with zero read of `variant`/`confirmedVariant` — unlike the GK-31 mechanism (`activeAnchoredOverFallbackSold`/`isActivePoolVariantConfirmed`, lines 693-707), which performs exactly this active-vs-sold variant-authority comparison but ONLY inside the narrower `soldPoolIsAllVariantFallback` branch (sold pool 100% edition-ambiguous fallback-readmitted rows). A **variant-unverified** sold pool (one whose rows carry no positive signed/variant marker — the logs prove only that zero rows positively matched the signed-title detector, which caused `verifySoldComps`'s keep-all fallback; a title omitting "signed" may be unsigned or may simply not say, so "unsigned"/"verified-clean" is not the correct characterization — see Directive I's own flagged inference) can silently outrank a small, correctly variant-CONFIRMED active pool — the general case is unprotected, only the narrow fallback case is. TRIGGER: real production evidence, NYC Outlaws #1 — `confirmedVariant="signed"`, active pool correctly isolated to 4 signed comps ($45-119, `[comps] signed filter: ... isolated to signed comps`), but tier selection never compares this against the sold pool; a 13-comp variant-unverified sold pool wins tier 2.5 outright at $11.43 market price, well under the cheapest signed ask. **Invariant this ticket enforces: variant-unverified evidence must not silently outrank variant-confirmed evidence.** Desired outcome on fix: demote to lower authority → RESEARCH, never "adopt the active pool price" instead — the 4 active asks ($45-$119) are asks, not sold prices, and do not become truth merely because the sold pool is demoted. Not fixed — pricing-math AND policy boundary (not merely a code-size question), needs its own greenlight; see Directive J Task 3 for the pre-implementation trace of whether the needed signal already reaches `priceBands.js:711`. | aliases:
GK-79A | CLOSED | 1d827e7 | 2026-08-12 | Directive J Task 1 (split from GK-79, Directive I), CLOSED by Directive J Task 2 — display-only mislabel: `src/App.jsx:5772` rendered "Condition confidence: {high\|medium\|low}" from Vision's `confidence`, which `api/grade.js` STANDARD_PROMPT scopes explicitly to "how legible and complete THIS image is" — image legibility, not condition certainty. The value itself was real and honest; the label overstated what it measures. Two concepts conflated under one word: `imageConfidence` (was what we received readable? — EXISTS, real, honest) vs `gradeEvidenceCoverage` (did we receive enough surfaces for this grade to move price? — DOES NOT EXIST, see GK-79B, still OPEN). FIXED: relabeled to "Image legibility confidence" — wording only; `formatConfidence` logic, `api/grade.js` prompt, `gradeMultiplier`, and identity-axis authority weighting at `api/enrich.js:2983` all untouched, verified by `tests/grailkey-directive-j-gk79a-relabel.test.js` (18/18, proves the mislabel against the actual pre-fix committed source via `git show`, proves the fix, confirms scope). | aliases:
GK-79B | OPEN | -       | 2026-08-12 | Directive J Task 1 (split from GK-79, Directive I) — grade authority lacks an evidence-coverage qualification. `gradeMultiplier` (`api/enrich.js:6714-6734`) multiplies price bands from `grade`/`numericGrade`/`isGraded` alone, with no check that the evidence was sufficient to produce that grade. No minimum-evidence threshold exists anywhere in code for grade (repo-wide grep: zero hits for `MIN_PHOTO`/`minPhotos`/`photos.length <`/`requirePhoto`). **Do not gate `gradeMultiplier` on `confidence`.** A tack-sharp front-cover photo legitimately yields `confidence = high` while showing zero evidence about back, spine, interior, staples, or tape — wiring a legibility measure into a coverage decision would create a new defect on top of the one being fixed. A `gradeEvidenceCoverage` concept (surfaces captured vs. surfaces needed for confidence in this specific grade) must be defined before any implementation. The multi-photo capability already exists and is unused for this purpose: `buildImageContent` (`api/grade.js:454-468`) loops the full image array, and `addPhotoToComic` (`src/App.jsx:12260-12276`) already re-grades from all stored photos when the operator manually adds one — the initial scan/price-setting call sends exactly one image at 8+ call sites (e.g. `src/App.jsx:11278`) and nothing automatic ever invokes the multi-photo path based on confidence, evidence gaps, or price magnitude. Not fixed — design-first (Jimmy), needs `gradeEvidenceCoverage` defined before any implementation. | aliases:
GK-80 | OPEN | -       | 2026-08-12 | Directive L Task 1 — two independent per-scan identifiers exist and are never cross-linked. `scanId` (`crypto.randomUUID()`, `src/App.jsx:10608`, minted before any fallible operation, echoed by both `/api/grade` and `/api/enrich`) is never persisted to IndexedDB or logged server-side. `pipelineTraceId` (`randomUUID()`, `api/enrich.js:2231`) is generated independently server-side per enrich call and IS already persisted (`item.pipelineAudit.traceId`/`.buildSha`, 5 merge sites via `mergePipelineAudit`, `src/lib/dataQualityGuard.js:236-249`) but never rendered to the operator (zero UI read sites, grep-confirmed) and never logged alongside `scanId` — no log line or response field connects the two for a given request. TRIGGER: a future correlation attempt (matching a client-side `scanId` a user reports against a server-side log line) finds no shared key, because none exists — the two IDs currently describe the same request from two unlinked vantage points. Not fixed — found during Directive L's Candidate A trace; relevant to (but not required by) any future `assessmentId` build, which would need to pick one as authoritative or explicitly bridge them. | aliases:
GK-81 | OPEN    | -       | 2026-08-13 | Directive N1 identity-resolution trace — `src/lib/imageSearchIdentity.js:1029`'s doc comment claims "eBay search_by_image returns bestMatch order," but the actual HTTP request that populates this array (`api/enrich.js:2089-2091`, `POST .../item_summary/search_by_image?category_ids=63&limit=20`) passes no `sort` parameter at all — unlike `api/comps.js:337`'s separate active-comp search, which explicitly requests `sort=bestMatch` (itself documented elsewhere in this codebase, `api/comps.js:330`, as "a relevance algorithm, not a stable sort" / non-deterministic between calls). The comment's ordering claim for the identity-search endpoint is uncited — no eBay doc reference, no parameter, nothing in this repo establishes what order search_by_image actually returns rows in. TRIGGER: any future fix that treats rank/array-position from this specific array as a meaningful signal (e.g. "items[0] is the closest visual match") is building on an unverified assumption; confirm the endpoint's real ordering semantics against eBay's own docs before relying on it. Not fixed — record only, found while tracing where rank is preserved vs. discarded for Directive N1. | aliases:
GK-82 | OPEN    | -       | 2026-08-13 | Directive O comp-ladder trace, Task 1 item 5 — `api/comps.js`'s attempt loop breaks on `filtered.parsed.length > 0` with no quality/confidence floor beyond nonzero count: a single junk survivor can win an entire attempt and stop the ladder from broadening, identically for a 1-comp junk pool and a 20-comp clean pool. TRIGGER: any attempt (including the now-reordered image-search fallback and any variant-bearing attempt) that returns exactly one filtered survivor by coincidence terminates the ladder there, even when a broader/later attempt would have returned a stronger pool. Not fixed — Directive O's own explicit scope boundary: "Do not change the break condition unless Task 1 item 5 shows a single junk survivor can win — if so, report that as a separate finding and do not fix it in this dispatch." Reported per that instruction, not touched. | aliases:
GK-83 | OPEN    | -       | 2026-08-13 | Directive P Task 1 trace, Blocker 2 — `confirmedVariant` (`api/enrich.js:5348`, `let confirmedVariant = writeConfirmed(...)`) is not yet declared/computed at the point `selectTitleFamilyCandidate`'s title-family decision fires (`api/enrich.js:2867`, ~2700 lines earlier in handler execution order). Confirmed directly on the real 21:16 Sabrina production log: `[title-family] decision=fallback-vision` logged at 21:16:07.402, `[identity-write] field=confirmedVariant to="Dan Parent NYCC variant"` logged 40ms later at 21:16:07.442. TRIGGER: any future dispatch designing a title-family (or other early-Phase-1) decision that wants to corroborate against Vision's confirmed variant, issue, or other fields resolved later in the handler will hit this same temporal wall — those values do not exist yet at that point in execution, and `req.body.variant` (the raw, unconfirmed client value) is explicitly not a legal substitute (can be invalidated/cleared downstream, e.g. `[commit4.3] variant provenance invalidated ... clearing req.body.variant`, using it early would bypass exactly the confirmation process this campaign exists to enforce). Not fixed — Directive P's own explicit non-goals forbid moving variant confirmation earlier or adding new provenance plumbing; this ticket exists so the wall is greppable rather than re-discovered from scratch next time. | aliases:
GK-84 | OPEN    | -       | 2026-08-14 | **STATUS CONFIRMED OPEN (Directive U, 2026-08-14) — this ticket was never actually closed here; the registry line has read OPEN since it was first committed (`d58a697`) and still does. `docs/PATTERN-LIBRARY.md`'s Directive T handoff prose incorrectly stated "GK-84 corrected and closed as a record-only ticket" — that was a documentation error, not a registry state; corrected in the same commit as this line. Migration NOT BUILT.** **CORRECTED (Directive T, 2026-08-14) — narrower and dated, not a general Trading API migration.** `UploadSiteHostedPictures` specifically (`api/list-ebay.js:375-450`, `uploadSiteHostedPicture()`, called before `AddFixedPriceItem` on both listing paths) is decommissioned **2026-09-30**; migration target is the Media API (`createImageFromFile`/`createImageFromUrl`). `AddFixedPriceItem`, `GetItem` (`api/list-ebay.js`), and `EndItem` (`api/delist-ebay.js`) have **no announced Trading-API-wide sunset** — not part of this ticket's scope. Legacy Product API (Aug-15-2026) remains confirmed unused, no exposure. **Implementation target: 2026-09-01** (production time on the replacement before the old call disappears, not the deadline itself). TRIGGER: any listing attempt after 2026-09-30 that still calls `UploadSiteHostedPictures` fails outright — images are a hard prerequisite for `AddFixedPriceItem` on both listing paths (bundle and single-item). Not fixed — migration not scoped or built this dispatch (explicit non-goal); an alternative "supply `PictureURL` directly" path exists in eBay's docs but requires stable public eBay-compatible image URLs, and whether this project has any image hosting is **UNVERIFIED** — check before assuming that path is cheaper than the Media API migration. | aliases:
GK-85 | CLOSED  | pending | 2026-08-14 | **CLOSED (GrailKey Directive T, Task 3, 2026-08-14).** `identityAuthority`, a per-field `facet -> 'OPERATOR_CONFIRMED'` map, now crosses every merge/persistence boundary via `mergeIdentityAuthority` (`src/lib/dataQualityGuard.js`, presence-aware: absent preserves, present-value replaces, present-null clears) and is consulted by `mergeConfirmedIdentity` (the merge function shared by 5 of `App.jsx`'s identity-writing call sites) before accepting any fresh automatic value for a locked facet. Server sets it from `manualCorrectionRequest.validation.acceptedFields` on every valid correction (`api/enrich.js`). `buildCorrectedCatalogueItem` merges it explicitly (its raw spread would otherwise wholesale-replace the map). Per-field, not per-item, by design — see the acceptance test in `tests/grailkey-directive-t-task3-identity-authority.test.js`. Original finding text below, retained for history: no durable operator-authority mechanism exists. `mergeConfirmedIdentity` (`src/lib/dataQualityGuard.js:201-209`, the single title/issue/year/publisher/variant merge function used at the primary scan-save path) is purely presence-aware (`hasKey(enrich, field) ? enrich.field : prior.field`) with zero awareness of whether `prior`'s current value was ever operator-corrected — any later automatic enrich response that includes these fields (nearly all do) silently overwrites a prior manual/operator correction. `out.manualCorrection` (`api/enrich.js:10880`, written once via `buildManualCorrectionProvenance`) is WRITE-ONLY — grep-confirmed zero read sites anywhere in `api/enrich.js` — a historical audit record, not an authority gate; nothing consults it before a subsequent write. TRIGGER: any future correction/picker/operator-confirmation feature that assumes a corrected identity field survives the NEXT routine automatic rescan will silently regress the moment that rescan runs — confirmed structurally, not yet observed in a production log. Not fixed — this is the reported finding itself (Directive S stopped before Task 2 specifically because of this gap); minimum plumbing: an operator-authority marker per identity field (or a coarse per-item flag) that `mergeConfirmedIdentity` and any sibling merge consult before overwriting. | aliases:
GK-86 | CLOSED  | pending | 2026-08-14 | **CLOSED (GrailKey Directive T, Task 2, 2026-08-14).** `extractIdentityFromImageSearch` (`src/lib/imageSearchIdentity.js`) now captures one normalized `imageUrl` field per row (`image.imageUrl ?? thumbnailImages[0].imageUrl ?? null`). All 8 importing files checked for exact-shape assertions an added key could break — none found. Original finding text below, retained for history: the image-search pool reaching the client (`out.imageSearchResults`, `api/enrich.js:9723`, `= visualResult.items`) never carries an image URL. `extractIdentityFromImageSearch`'s per-row parsed shape (`src/lib/imageSearchIdentity.js:402-421`) captures `rawTitle, title, issue, year, variantTokens, price, itemWebUrl, endTime, itemId, legacyItemId, leafCategoryIds, buyingOptions, sellerUsername` — no `image`/`thumbnailImages`/`imageUrl` field, though the raw eBay Browse API `itemSummaries` response this is parsed from does include one. TRIGGER: any future UI (a candidate picker, a visual pool inspector, anything showing "here's what eBay's image search actually found") that expects to render a cover thumbnail per pool row will find the field simply isn't there — not filtered out downstream, never captured at the parse step. Not fixed — record only. | aliases:
GK-87 | CLOSED  | pending | 2026-08-14 | **CLOSED for the picker-required race scope (GrailKey Directive U, Task 2, 2026-08-14).** `gradeBlob`'s own automatic-scan persisted-write path (`src/App.jsx`, the `setCatalogue`+`setSelectedItem` updaters inside its `.then((enrich) => {...})` block) and its transient `setResult` sites (both 'grade' and 'enrich' stages) now reject a stale write whenever the currently-active ownership is specifically a correction that superseded them — `wasSupersededByCorrection` (`src/lib/scanOwnership.js`), a pure predicate keyed on a new `kind: 'scan' \| 'correction'` tag on the ownership object gradeBlob/`submitManualCorrection` already mint (same shape, one field — not a new mechanism). Each of the three write sites computes its `applyScanOwnershipGuard` mode dynamically: `wasSupersededByCorrection(...) ? ENFORCE : CURRENT_SCAN_OWNERSHIP_MODE` — correction-caused staleness always rejects, regardless of the global constant; every OTHER staleness cause (scan-vs-scan included) is completely unaffected, still SHADOW, exactly as before this dispatch (`CURRENT_SCAN_OWNERSHIP_MODE` itself untouched, per the explicit no-global-flip constraint). The persisted write (`setCatalogue`+`setSelectedItem`) is gated as ONE unit inside a single `applyScanOwnershipGuard` call (stage `'enrich-persist'`) — never selectively applied. Reachability, checked directly against real code before implementing: auto-refresh structurally excluded (`if (selectedItem) return;` blocks it for the entire duration a correction UI can be open); back-to-back corrections already protected (Directive T, Task 5's own ENFORCE gate); the scan-to-catalogue `.then()` chain IS reachable (confirmed: `setLoading(false)` unblocks the UI, and `savedId` is already set, well before `/api/enrich` resolves) — this is the case fixed. `refreshMarketData` found to be an INDEPENDENT, unrelated race through a wholly separate guard (`activeCardEnrichIdRef`) with zero relationship to `activeScanRef`/`scanOwnership.js` — out of this ticket's scope, logged separately as **GK-88**, not fixed here. 33 new assertions (`tests/grailkey-directive-u-task2-stale-automatic-write.test.js`), including a DIRECT pre-fix reproduction against the real, unchanged `CURRENT_SCAN_OWNERSHIP_MODE`/`applyScanOwnershipGuard` plus a `git show 1627d06:src/App.jsx` cross-check confirming the persisted write had zero guard at all before this dispatch. Original finding text and Directive T's own PARTIALLY-ADDRESSED note, retained for history: Directive S Task 1 finding — the scan-ownership race guard (`src/lib/scanOwnership.js`, Track B Phase 0 Slice 7) does not protect the persisted catalogue write path at all, by explicit design (`src/App.jsx:10733-10740` comment: "The persisted catalogue write below is UNCONDITIONAL regardless of this guard either way"), and even where `applyScanOwnershipGuard` IS wired (the transient `result`-state write sites only), `CURRENT_SCAN_OWNERSHIP_MODE = SCAN_OWNERSHIP_MODE.SHADOW` (`scanOwnership.js:111`) means it currently only logs what it would reject — every write proceeds unconditionally in production today, by the file's own documented rollout design (SHADOW is a deliberate, temporary, pre-ENFORCE data-collection phase, not itself a defect). TRIGGER: any feature relying on "an older in-flight response cannot overwrite a newer one" for its own correctness (e.g. an operator-confirmation flow) has zero protection today at the persisted-write layer, and only observability (not enforcement) at the transient-state layer. **PARTIALLY ADDRESSED (GrailKey Directive T, Task 5, 2026-08-14).** `submitManualCorrection` (`src/App.jsx`) now mints a fresh ownership identity (reusing the existing `activeScanRef`/`scanGenerationRef`, no new plumbing) the moment a correction begins, and gates its own persisted write (`putComic`+`setCatalogue`+`setSelectedItem`, its one write moment) via `applyScanOwnershipGuard(..., SCAN_OWNERSHIP_MODE.ENFORCE, ...)` — narrowly, at this one new call site only; `CURRENT_SCAN_OWNERSHIP_MODE` (SHADOW, governing gradeBlob's own two existing call sites) is untouched. **Remaining, deliberately deferred:** `gradeBlob`'s own pre-existing automatic-scan persisted-write path (the `setCatalogue` updater inside its `.then((enrich) => {...})` block, `App.jsx:10845` area) still has zero generation check — a stale automatic scan response can still write there regardless of a newer operation having started. Deferred, not fixed, because: (a) it is a separate, larger, riskier change to a different already-live, heavily-used code path than the correction flow touched this dispatch, and (b) Task 3's per-field `identityAuthority` check (GK-85) already protects specifically-locked identity FACETS against a stale automatic overwrite there, independent of race timing — the remaining exposure is for UN-locked fields (price, comps, etc.) on that specific path, not the identity-authority class of bug this campaign has been chasing. TRIGGER for closing the remainder: add the same `scanOwnership`-vs-`activeScanRef.current` generation check (reusing the closure `gradeBlob` already captures) to that one `setCatalogue` updater, ENFORCE-gated the same narrow way, with its own dedicated test and regression pass. | aliases:
GK-88 | OPEN    | -       | 2026-08-14 | **EXPANDED, MOSTLY CLOSED (GrailKey Directive V, Task 1/2, 2026-08-14) — remains OPEN for one explicitly-named remaining gap.** Directive V enumerated every async writer touching identity/pricing/comp/evidence state in `src/App.jsx` (full table: `docs/PATTERN-LIBRARY.md`) and guarded every reachable single-flow one: `refreshMarketData`, `reIdentifyBook`, `addPhotoToComic`, and the duplicate-confirm ("Save Another Copy") handler each now mint their own itemId-scoped ownership via the SAME shared `activeScanRef`/`scanGenerationRef`/`wasSupersededByCorrection` mechanism `gradeBlob`/`submitManualCorrection` already used (no new mechanism). **A real, live defect was found and fixed in the process**: the predicate shipped by Directive U (`GK-87`) was item-BLIND — it rejected a stale write on ANY correction anywhere in the app, discarding valid work for a completely unrelated comic (a global kill switch, not a guard). Fixed by adding `itemId` to the ownership objects (same shape, one field) and requiring a same-item match before rejecting; `gradeBlob` and `submitManualCorrection` retrofitted with this fix too. Proven via a mandatory cross-item control test (`tests/grailkey-directive-v-task2-ownership-perimeter.test.js`, Part 0 reproduces the pre-fix bug directly against the real `a734483` source, Part 2 is the required cross-item control). Auto-refresh reconfirmed structurally excluded (unchanged, `AbortController` + `selectedItem` dependency). **Remaining, deliberately NOT guarded — this is why the ticket stays OPEN**: `handleBulkImport`'s `processFile` (`src/App.jsx`, `CONCURRENCY=3` worker pool) fires its own per-item fire-and-forget `/api/enrich` exactly like `gradeBlob`/duplicate-confirm do, targeting a freshly-created item, with zero ownership check today. It was deliberately NOT wired into the shared `activeScanRef` slot: unlike every other guarded producer (each mutually exclusive by the UI — only one card, one scan, one correction at a time), bulk import runs MULTIPLE items concurrently by design, and the single-slot `activeScanRef` can only ever track one "most recent" ownership — a concurrent bulk worker starting would steal the slot out from under an unrelated correction's own self-check (or vice versa), which is a DIFFERENT collision than the itemId fix closes (that fix protects a stale WRITE from being wrongly rejected/accepted for the wrong item; this is about the SLOT ITSELF being contended by two producers running at the same real time). Wiring bulk import in safely needs a genuine multi-slot or per-item ownership registry (e.g. a `Map<itemId, ownership>`), which is new-mechanism territory explicitly out of this dispatch's scope. TRIGGER for closing the remainder: an operator starts a bulk import, navigates away while it's still processing files in the background, opens and corrects an unrelated existing item, and a still-in-flight bulk-import worker's response for a DIFFERENT item lands after — today, nothing prevents it from writing (not wrongly BLOCKED, just never CHECKED, so this is a strict subset of what GK-87/GK-88 otherwise close: the risk is real but lower-frequency, since bulk-import always targets brand-new items the operator hasn't navigated to yet). Two adjacent, differently-shaped findings from the same trace logged separately, not folded in here: **GK-93** (the residual same-slot risk between two DIFFERENT single-flow guarded producers, inherited from Directive U's own design, not new to this dispatch) and **GK-94** (`listOnEbay`/`syncEbayStatus`'s stale-JS-closure resurrection risk — a different defect shape entirely, not an ownership-check gap). | aliases:
GK-113 | OPEN | d82f853 / 00b10c9 / 5e65244 | 2026-08-15 | **CORRECTED (Directive AJ, 2026-08-15) — was prematurely marked CLOSED.** Closure standard is physical-production evidence; a BUILDING deploy and unit-level fixtures don't meet it. Reclassified OPEN, annotated SHIPPED — production acceptance pending Jimmy's physical scans (Detective Comics #1107, no correction). Directive AJ also found and fixed a real reachability gap in the original fix (see GK-117) and re-verified through the real `/api/enrich` HTTP handler (`tests/grailkey-directive-aj-http-handler.test.js`), not just the unit level. Original finding retained: split-brain identity — `confirmedIssue` could stay null (`identityCore.js`) while `api/comps.js`'s independently-derived `issueNum` (re-parsed from `title` text, not from `confirmedIssue`) still populated the eBay query and `evidenceTarget.issue` with a specific number — two genuinely separate variables, not one read twice, confirmed by direct trace. FIXED: `resolveIdentity`'s new last-resort branch (see GK-115) fills `confirmedIssue` from the first eligible visual row whenever nothing else did, so `issueNum`'s existing `confirmedIssue`-first fallback chain naturally stops diverging — no change to `api/comps.js` itself. `tests/grailkey-directive-ai-visual-first-identity.test.js` Part 1 (Detective Comics #1107); re-verified end-to-end in `tests/grailkey-directive-aj-http-handler.test.js`. | aliases:
GK-114 | OPEN | d82f853 | 2026-08-15 | **CORRECTED (Directive AJ, 2026-08-15) — was prematurely marked CLOSED.** Reclassified OPEN, annotated SHIPPED — production acceptance pending Jimmy's physical scans. Original finding retained: surname-only creator inference (`src/lib/premiumCreators.js`) resolved bare "jimenez" unconditionally to Phil Jimenez even when the pool/visual text carried a full, different, comparably-prominent name ("Jorge Jimenez," current Batman/Detective Comics artist) — never added to the file's own documented ambiguous-surname set (Adams/Lee/Miller/Wood/Davis/Ross) despite qualifying by the same standard. FIXED: split into two full-name-only entries (`aliases: []` each), matching the existing policy exactly. `extractCreatorsFromComps` itself untouched — the registry entry was the whole defect. `tests/grailkey-directive-ai-visual-first-identity.test.js` Part 1; re-verified end-to-end (as `creatorFromCompsSingleton`, since the mock's near-identical comp titles dedup to one survivor) in `tests/grailkey-directive-aj-http-handler.test.js`. | aliases:
GK-115 | OPEN (partial) | d82f853 | 2026-08-15 | Directive AI Task 1/2 — visual-pool eligibility. A seller "variation group" placeholder listing ("Sabrina the Teenage Witch comics select an issue," no single book identity) could win title-family selection purely by occupying rank 0, same for lot/bundle listings surviving via `weightSum`. **Text-pattern heuristic FIXED**: `isEligibleVisualRow`/`VARIATION_GROUP_RE` (`src/lib/identityReconciler.js`) filters `scored` families in `selectTitleFamilyCandidate` (`src/lib/imageSearchIdentity.js`) before any rank/weight logic runs. **Genuinely OPEN, not fixed**: eBay's Browse API `itemGroupType`/`SELLER_DEFINED_VARIATIONS` marker is never captured anywhere in this codebase's parsed visual-pool rows (grep-confirmed zero hits, `api/enrich.js`/`api/comps.js`/`imageSearchIdentity.js`) — the fix is therefore a phrase list (same shape as compHygiene.js's documented `OTHER_VARIANT_DESCRIPTOR_RE` stopgap), not a structural guarantee, and a normal variant cover cannot be reliably distinguished from a variation-group placeholder by text alone in every case. `tests/grailkey-directive-ai-visual-first-identity.test.js` Part 2 (Sabrina). | aliases:
GK-119 | CLOSED | 5e2adcb | 2026-08-15 | **Directive AK — the population-precedence gate, Slice 1's final policy bug.** AJ's own precedence order (`family-consensus > first-eligible-visual > vision`) was tested directly against a Sabrina-shaped fixture: a specific, corroborated, eligible `firstEligibleVisual` candidate ("Sabrina Annual Spectacular #1, Dan Parent, NYCC, Foil") competing against a LARGER, GENERIC "Sabrina the Teenage Witch" population that clears `resolveFamilyIssueConsensus`'s own `'adopted'` mode purely on member count (4/6 = 67%, no prior to compare against, no discriminative corroboration, no hard contradiction). **THE FIXTURE FAILED ON FIRST RUN**: `family-consensus` unconditionally outranked `first-eligible-visual` by raw precedence, producing `confirmedIssue="5"` (the generic population's own vote) instead of `"1"` (the specific physical book actually in hand) — Sabrina's original disease (GK-98, a generic franchise family's population count defeating a specific edition candidate) re-encoded as a precedence rule inside the new issue-facet reconciler, for exactly the case AF's `discriminative-corroboration` fix does not cover (no `opts.visionVariant` token corroboration available at all). FIXED: `identityCore.js` now tags family issue evidence with ONE of two DIFFERENT-PRECEDENCE sources instead of a single unified `'family-consensus'` — `'family-population'` (`resolveFamilyIssueConsensus`'s own `'adopted'` mode, no `outcome` field — a bare vote filling an empty gap) demoted BELOW `'first-eligible-visual'`; `'family-corroborated'` (every other genuine mode, PLUS the retention branch's own legacy-mapped `'adopted'`/`'provisionally-corrected'` outcomes — found and preserved during the fix: those carry a real `outcome`/`authoritativeForCustody` verdict from `decideFieldAuthority`, a confidence-aware correction of an EXISTING prior, e.g. Spawn #351's low-confidence Vision "301" corrected by a 5/5-unanimous family — not a bare population vote, and demoting it broke `tests/q-trackB-commit4.3-winning-family-authority.test.js` on the first regression pass, caught and fixed forward in the same commit) keeps top precedence, unchanged. Governing rule now structurally encoded: population corroborates or contradicts, it does not replace. Three controls verified alongside the blocking fixture: population still corroborates cleanly when it agrees with the specific candidate; population is still usable as a fallback when nothing independent competes (still correctly flagged provisional, matching Detective's own single-source precedent — not a special "population wins by default" exemption); the Spawn #351-class legacy-mapped `'adopted'` is provably NOT demoted (`outcome` field presence is the reliable discriminator). Flash #139 and all prior guards (Adventure Time SDCC, Eternus #2, `zeroSupportRescueDeclined`, Guard 6/contamination) re-verified unchanged. `tests/grailkey-directive-ak-population-precedence.test.js`, 11/11. | aliases:
GK-117 | CLOSED | 5e65244 | 2026-08-15 | **Directive AJ, Proof 1 — the reconciler reachability gap.** Directive AI's own report claimed the new visual-first mechanism was wired in; direct trace (grep for `reconcileIssue`/`createEvidenceSet`/`addEvidence` across `identityCore.js`/`api/enrich.js`/`imageSearchIdentity.js`) found ZERO call sites — the "reconciler" existed only as an exported, unit-tested pure function, never invoked in the production pipeline at all. The shipped branch reimplemented the same guard logic ad hoc, directly in `identityCore.js`, gated on `confirmedIssue == null` — a rescue path for the NULL case only, never examining `firstEligibleVisual` when Vision confidently (even if wrongly) kept a non-null value with merely WEAK, non-zero pool support (the zero-support block requires the ratio to fall below `ISSUE_ZERO_SUPPORT_RATIO_FLOOR` to run at all) — the exact "confidently wrong value the evidence system never gets to examine" shape this whole campaign exists to close. FIXED: `resolveIdentity` now builds an issue evidence set and calls `reconcileIssue` UNCONDITIONALLY on every issue resolution, not gated on null. Upstream resolvers (family-consensus, zero-support override/escalate, retention/rescue) still run exactly as before; their outputs become EVIDENCE at the reconciler's existing precedence (`family-consensus` > `first-eligible-visual` > `vision`) rather than pre-empting the reconciler from running. Flash #139 safety is now a PRECEDENCE property, not an unreachability property — verified two ways: the canonical fixture text (containing "Anniversary") is protected by BOTH the marketing-guard AND precedence simultaneously; a variant fixture without "Anniversary" text confirms precedence alone is sufficient. New required blocking fixture (Fixture P1, `tests/grailkey-directive-aj-reconciler-reachability.test.js`): Vision keeps a weakly-supported (15% support, above the 10% zero-support floor) non-null issue; the reconciler still runs, finds a stronger `first-eligible-visual` candidate, and overturns it — asserted on the `[reconcile-issue]` decision-log artifact (source/justifiedBy/conflicts/authority), not merely the output value, per Proof 1's own instruction not to assert on "the answer looks right" alone. Re-verified end-to-end through the real `/api/enrich` HTTP handler (`tests/grailkey-directive-aj-http-handler.test.js`) — the reconciler fires on a live request for both Detective and Venom, not just the unit fixture. | aliases:
GK-118 | CLOSED | 5e65244 | 2026-08-15 | **Directive AJ, found during Fixture P1/regression sweep for GK-117 — Guard 6 (contaminated family).** `tests/q-trackB-commit4.3-winning-family-authority.test.js`'s "CONTROL C" (a naturally-formed family mixing a raw listing with a slabbed "CGC 9.8" member) regressed once the reconciler became reachable on non-null Vision values (GK-117): with vision.issue present and untouched (bare passthrough, zero-support never fired since `ebay.agreement` was absent), the family's own contaminated first-eligible-visual candidate could now win by precedence over bare 'vision' evidence — exactly the visual-first behavior GK-117 intentionally enables, but wrong here because CONTROL C's family is contaminated (mixes a graded/slabbed row with raw listings, `hasContaminatedMember`: LOT_RE/REPRINT_RE/SLAB_RE/GRADED_RE/SIGNED_RE/IDENTITY_TPB_MARKER_RE). FIXED: `hasContaminatedMember(opts.visualItems, family?.topFamily?.indices)` — the SAME signal `familyAuthorityBaseConditions` already gates retention/rescue on — now also suppresses first-eligible-visual evidence from entering the set at all when the family is contaminated. Safe with no `topFamily` at all (Detective/Venom's own fixtures): `indices` defaults to `[]` internally, returns `false`, never a false suppression. Secondary finding while building this fix: mixing CGC-graded rows into a first-eligible-visual corroboration set (as this dispatch's OWN Detective test fixtures initially did, to satisfy the >=3-row floor) legitimately trips this SAME guard for a realistic "multiple graded copies of a hot book" shape — not a bug, but a real interaction to be aware of; both `grailkey-directive-ai-visual-first-identity.test.js` and `grailkey-directive-aj-http-handler.test.js`'s Detective fixtures were corrected to use plain condition words (NM/VF) instead of CGC-graded text for their corroborating rows. | aliases:
GK-116 | OPEN | -       | 2026-08-15 | Directive AI Task 1/2, found building the fix — `extractIssueCandidate`'s (`src/lib/identityCore.js`) shared hash- and bare-number branches both cap at 999, silently dropping real legacy-numbered issues (Detective Comics restored numbering alone reached #1107 by 2022) for EVERY caller of that function, including `resolveFamilyIssueConsensus` (the Flash #139-protected consensus path). Not widened here — 19+ dependent test files, unknown blast radius on the bare-number branch's year-collision guards, deliberately out of Directive AI's scope. Directive AI's own new mechanism uses a separate, narrower, hash-only, uncapped extractor (`extractHashIssueNumber`, `src/lib/identityReconciler.js`) instead of widening the shared one. TRIGGER: any pre-existing consumer of `extractIssueCandidate` (family-consensus voting, comp title parsing) silently fails to recognize a legacy-numbered "#N" (N>999) issue token — not confirmed to have caused a production defect yet, logged as a found gap. | aliases:
GK-89 | OPEN    | -       | 2026-08-14 | Directive U, Task 2 full regression sweep — `tests/grailkey-directive-q-variant-null-custody.test.js` Part 1b crashes (not fails) with `Error: git stash pop --quiet` / "No stash entries found" whenever `src/lib/manualCorrection.js` has zero uncommitted diff at the moment the test runs. The test unconditionally runs `git stash push --quiet -- src/lib/manualCorrection.js` then, in a `finally` block, unconditionally `git stash pop --quiet` — but `git stash push` with a pathspec silently creates NO stash entry when that path has nothing to stash, so the later unconditional pop throws. Confirmed pre-existing and unrelated to Directive U: `git diff 1627d06 -- src/lib/manualCorrection.js` is byte-empty (the file has been untouched since Directive T), so this crash reproduces identically against pure `1627d06`, before any of this dispatch's changes. TRIGGER: any future test run where `manualCorrection.js` is clean (i.e. any time except immediately after a dispatch that edits it) crashes this file outright instead of reporting its actual pass/fail count — a harness defect that currently masks whatever Part 1b's real assertion result would be. Not fixed — found during Directive U's regression sweep, out of Task 2's scope (`scanOwnership.js`/`App.jsx` gradeBlob wiring only). | aliases:
GK-90 | OPEN    | -       | 2026-08-14 | Directive U, Task 2 full regression sweep — `tests/grailkey-directive-p-task3-variant-on-card.test.js` Part 3 ("`src/lib/dataQualityGuard.js` `mergeConfirmedIdentity` variant merge unchanged") now fails. Confirmed pre-existing and unrelated to Directive U via direct comparison: identical failure reproduces with this dispatch's `src/App.jsx`/`src/lib/scanOwnership.js` changes git-stashed away (i.e. at pure `1627d06`). Root cause (not yet fixed, only diagnosed enough to rule out Directive U): Directive T, Task 3 (GK-85) rewrote `mergeConfirmedIdentity` to consult `identityAuthority` via a new `pick()` helper before accepting fresh values — Directive P/Q's own "unchanged" scope-check assertion in this file was never updated to match the post-GK-85 source, so it now correctly (if noisily) flags a real, INTENDED prior change as an unexpected one. TRIGGER: any future scope-check pass over this file will keep reporting this same false alarm until the assertion's expected snapshot is updated to account for Directive T's Task 3 rewrite. Not fixed — found during Directive U's regression sweep, out of Task 2's scope. | aliases:
GK-91 | OPEN    | -       | 2026-08-14 | Directive U, Task 2 full regression sweep — `tests/grailkey-directive-j-gk79a-relabel.test.js` Part 1 ("pre-fix line does not yet say 'Image legibility confidence'") now fails. Root cause identified directly (not merely diagnosed as pre-existing): the test's own `git show HEAD:src/App.jsx` call (`tests/grailkey-directive-j-gk79a-relabel.test.js:50`) reads the literal string `'HEAD'`, not a pinned pre-fix SHA (the relabel commit itself, `1d827e7`, or its parent) — so the test was only ever going to pass for as long as `HEAD` still pointed at a pre-fix commit. Every commit since (this repo is many dispatches past `1d827e7` now) makes `HEAD:src/App.jsx` already contain the FIXED "Image legibility confidence" text, flipping this "prove the OLD text existed" assertion permanently false — a designed-to-go-stale-by-construction defect, not something any later dispatch's code changes caused. Confirmed unrelated to Directive U via direct comparison: identical failure reproduces with this dispatch's changes stashed away. TRIGGER: this assertion will report a false "pre-fix state not confirmed" on every future run, forever, until pinned to the actual pre-fix SHA instead of `HEAD`. Not fixed — found during Directive U's regression sweep, out of Task 2's scope. | aliases:
GK-92 | CLOSED  | 8d8af44 | 2026-08-14 | **CLOSED (GrailKey Directive V's compaction gate, 2026-08-14, docs-only commit `8d8af44`, before Task 1).** `CLAUDE.md`'s Pattern Library dispatch-index section (was 64,116 of the file's 148,847 chars — 43% of the whole document) had drifted well past the doctrine's own stated "one line + Pattern Library pointer" standard into full paragraphs per dispatch, even though every one of those entries already has a complete writeup in `docs/PATTERN-LIBRARY.md` (confirmed by direct grep before touching anything, dispatch by dispatch — nothing lost). Compacted each entry to name + one-clause result + pointer; every named standing invariant preserved so it stays greppable (Rejection Must Not Create Authority, Authority must be use-consistent, cache correctness is authority correctness, Monotonic Evidence Extension, No Self-Corroboration, a safety gate must not disappear because an asset crossed a persistence boundary, a value must not vote for itself, the Authority Propagation Invariant and its three mechanisms). Doctrine, rules, standing constraints, the test baseline, Open Blockers, and Handoff Pointers verified byte-identical before/after (direct diff, both head and tail of the file, outside the touched range). 148,847 → 98,875 chars (-49,972, -34%) — well under the 150,000 limit again, with real headroom for future dispatches. | aliases:
GK-93 | OPEN    | -       | 2026-08-14 | Directive V, Task 1 ownership-perimeter enumeration — even after GK-88's itemId fix, the single global `activeScanRef` slot can still be stolen mid-flight by a SECOND, unrelated single-flow producer (e.g. `refreshMarketData` for item B starting while item A's own correction is still awaiting its response) — when that happens, the FIRST producer's own self-check (`applyScanOwnershipGuard` comparing its own closure against `activeScanRef.current` at write time) sees a `scanid-mismatch` against the interloper and is wrongly rejected, even though nothing actually superseded it on ITS item. Distinct from GK-87/GK-88's fix: that fix protects a STALE WRITE from being wrongly accepted/rejected for the WRONG item; this is about the SLOT ITSELF being contended between two producers whose async tails genuinely overlap in real time. Confirmed to be inherited from Directive T/U's own original design (gradeBlob vs. submitManualCorrection could already collide this way before this dispatch touched anything) — not introduced by Directive V, and not made worse by it; the itemId fix narrows the practical window somewhat (a same-item collision is now also correctly detected) but does not remove the cross-slot collision itself for different-item cases. TRIGGER: operator starts an async operation (scan/refresh/reidentify/addphoto/correction) for item A, navigates away before A's response returns, starts a SECOND async operation for a DIFFERENT item B, and B's mint overwrites `activeScanRef.current` — when A's own response then arrives, its self-check wrongly reports superseded (scanid-mismatch against B's ownership) even though B never touched item A. Not fixed — genuinely needs a multi-slot or per-item ownership registry (e.g. `Map<itemId, ownership>`), explicitly new-mechanism territory, out of this dispatch's "no new mechanism" scope. Logged for its own future scoping and greenlight decision. | aliases:
GK-94 | CLOSED  | 6db4701 | 2026-08-14 | **CLOSED (GrailKey Directive X, 2026-08-14).** `listOnEbay` and `syncEbayStatus` (`src/App.jsx`) no longer spread the closed-over, pre-request `item` back into catalogue state — both now match `listBundleOnEbay`'s already-correct pattern (same file): `setCatalogue((prev) => prev.map((x) => { if (x.id !== item.id) return x; const updated = { ...x, <only-the-fields-this-handler-mutates> }; putComic(updated).catch(() => {}); return updated; }))`, reading the CURRENT catalogue item fresh at write time, then `setSelectedItem` mirrors the same fresh-`cur`-plus-fields merge. `listOnEbay` writes only `status`/`ebayUrl`/`ebayItemId`/`listedAt`; `syncEbayStatus` writes only `status` plus (conditionally) `soldPrice`/`soldAt`/`buyerFeedback` (sold) or `endedAt` (ended). If the id no longer exists in the catalogue at write time, `prev.map` returns the array unchanged and `putComic` is never called — writes nothing, per the explicit constraint. Reads (the outgoing request bodies) untouched — this was a write-side-only fix, no new mechanism, no ownership tokens. `listBundleOnEbay` verified byte-identical before/after (git-diff confirmed, not just eyeballed — see test Part 5). Original finding text below, retained for history: Directive V, Task 1 ownership-perimeter enumeration — `listOnEbay` (`src/App.jsx`) and `syncEbayStatus` (`src/App.jsx`) both build their post-fetch write (`updated = { ...item, status: ..., ebayUrl, ebayItemId, ... }` / `{...item, status: data.status, ...}`) by spreading the CLOSED-OVER `item` argument captured at call time, not a fresh read of the current catalogue state (unlike `listBundleOnEbay`, confirmed to already use a fresh `prev.map` read as its spread base — genuinely safe, checked directly, not assumed). A different defect shape than GK-87/88's missing-ownership-check class: this is a stale-JS-closure resurrection risk — if a correction updates title/issue/year/variant on the SAME item while an eBay-listing or status-check request is in flight, the eventual `{...item, status: ...}` write silently overwrites the corrected identity fields back to their pre-correction values as collateral damage of setting the listing-status fields. TRIGGER: operator lists a book on eBay (or checks its listing status), then — before that request resolves — corrects the same book's identity; when the listing/status response arrives, the corrected title/issue/year/variant/price/comps are silently reverted to their stale pre-correction values, with only `status`/`ebayUrl`/`ebayItemId` reflecting anything new. | aliases:
GK-95 | OPEN    | -       | 2026-08-14 | Directive Y, Task 1 (P0 false-READY trace) — `decisionEngine.js`'s `computeDecision` has ZERO awareness of `item.pricingSource === 'pc_estimate'` anywhere in the file (grep-confirmed: zero hits for the literal string in `src/lib/decisionEngine.js`, versus explicit checks for `'refused-reprint-thin-pool'`, `'verified_sold_stale'`, `'visual_pool_fallback'`, `'web_search_fallback'`, `'refused-claude-gate'`, `'refused'`, each of which pushes a warning). Tier 4 (`pc_estimate`, PriceCharting-ladder-only, explicitly documented as "no market data" — `src/lib/priceBands.js:623`) receives IDENTICAL blocker/warning treatment to a fully tier-1 verified-sold price. TRIGGER: any book whose price resolves via `pc_estimate` and whose OTHER warning conditions (zero-verified-comps, thin-active-pool, etc.) don't independently fire reaches `LIST_NOW`/`LIST_LOW` with the exact same confidence machinery as a book with real, current, verified market comps — the pricing tier itself carries no decision-time authority discount. Not fixed — investigation-only dispatch, pricing-math/decision boundary, needs its own greenlight. Sabrina Annual Spectacular #1 (Dan Parent NYCC Foil, 2026-08-13 20:19:11 scan) is the real production instance that surfaced this. | aliases:
GK-96 | CLOSED  | fb8bc3c | 2026-08-14 | **CLOSED (GrailKey Directive Z, 2026-08-14, `fb8bc3c`).** `src/lib/actionAuthority.js`'s `deriveActionAuthority` is a new, independent transaction-authority gate that does not share either asymmetry named below — `marketStanding` is derived directly from `pricingSource` (symmetric: `FALLBACK_ONLY`/`NONE` both deny READY, with no HIGH-only or LOW-only special case), and it is now the SOLE listing gate (`contract.listable`, `decisionSafe`, `getListableBooks`, and server-side `/api/list-ebay` all derive from `actionAuthority.state`, not from the old `matchConfidence`-tier-gated mechanisms this ticket describes). The two ORIGINAL mechanisms named below (the HIGH-only confidence cap, the LOW-only thin-pool lock) are UNCHANGED and still carry their documented asymmetry — Z deliberately did not touch `matchConfidence` or `api/enrich.js`'s Ship #21l Fix D, per its own explicit non-goals — but they are no longer the thing standing between a thin/stale/tier-4 book and an enabled List button; `actionAuthority` is, and it does not have this hole. Verified directly: the exact Sabrina production shape (`pricingSource:'pc_estimate'`, `matchConfidence.tier:'MEDIUM'`) now derives `actionAuthority.state === 'REVIEW'`, `contract.listable === false` (`tests/grailkey-directive-z-transaction-authority.test.js` Part 2, "SABRINA REGRESSION"). Original finding text retained below for history. Two closely related tickets remain explicitly OPEN, not fixed by this closure: **GK-95** (`decisionEngine.js` itself is still literally unaware of `pc_estimate` — Z never touched that file, per its own non-goals; the NEW gate simply no longer depends on that file's blindness) and **GK-98** (a confidently-WRONG identity, like Sabrina's, still reads `identityStanding: CONFIRMED` — this closure stops the WRONG book from reaching an enabled List button via the market-standing axis, it does not make the identity itself correct). Original finding: **P0 — Directive Y, Task 1, the false-READY root cause.** Two independent mechanisms exist that could each catch a thin/stale/tier-4 book and neither did, for the same structural reason. (1) `api/enrich.js:9424-9459` ("Ship #21l Fix D" thin-data cap) demotes match-confidence `HIGH→MEDIUM` when the pool is thin/tier-4/zero-verified — but the guard is `finalMc.tier === 'HIGH'` (line 9439): if the BASE match-confidence computation already lands at MEDIUM (a real, intended outcome per the documented "2 comps → MEDIUM if rawScore≥65" rule), this cap never fires and there is no corresponding MEDIUM→LOW demotion anywhere. (2) `src/lib/responseContract.js:198-209`'s `low-tier-thin-pool` soft lock (`out.matchConfidence?.tier === 'LOW' && totalComps < 3`) is the ONE mechanism in the whole pipeline that would have set `contract.listable = false` for exactly this shape (`listable` computed at `responseContract.js:445-449`, purely `locks.length===0 && state∈{PRICED,ESTIMATED} && action.startsWith('LIST')` — no tier/warning/comp-count awareness of its own) — but it is gated on `tier === 'LOW'` specifically, never `'MEDIUM'`. A book that scores MEDIUM from a thin/stale/tier-4 start (never HIGH, so mechanism 1 is a no-op; never LOW, so mechanism 2 never triggers) sails through BOTH defenses untouched. Downstream, `src/App.jsx:464-470`'s "Decision safe ✓" badge is PURELY `decision.action∈{LIST_NOW,LIST_LOW}` — it cannot render unsafe for any LIST_LOW action regardless of underlying evidence quality, and the tri-state "Identity confirmed ✓" badge (`src/App.jsx:425-458`) only checks `identityProvisional`/`listingHardLockReason`/`contract.locks` for `'identity-unresolved'` — none of which get set when a wrong identity wins a NORMAL (non-flagged) title-family consensus vote (see GK-98), so a confidently-wrong identity renders "Identity confirmed ✓" with nothing to contradict it. TRIGGER: any book whose comp pool is thin+stale+tier-4 AND whose base match-confidence score happens to clear the MEDIUM threshold (not HIGH, not LOW) reaches an enabled, one-tap "List on eBay" button with `blockers=0`, `Decision safe ✓`, `Identity confirmed ✓` — confirmed GENERAL, not Sabrina-specific: none of the four mechanisms traced (thin-data cap, low-tier-thin-pool lock, decisionSafe badge, identityConfirmed badge) reference this book's identity, publisher, or variant at all — every one is keyed purely on tier/comp-count/matchConfidence-tier/decision.action. Not fixed — investigation-only dispatch (Directive Y), decision/contract-gating boundary, needs its own greenlight and design decision (symmetric MEDIUM→LOW cap? `low-tier-thin-pool` also gate on MEDIUM+very-thin? pc_estimate-aware blocker per GK-95?). | aliases:
GK-97 | OPEN    | -       | 2026-08-14 | Directive Y, Task 3 (P0 false-READY trace) — `applyVariantPreferenceFilter` (`api/comps.js:579-665`) matches comp listings against `confirmedVariant` tokens PURELY by title-string substring (`t.includes(w)`, `t = String(it.title||'').toLowerCase()`) with ZERO publisher check anywhere in the function, and zero publisher check anywhere else in the documented comp filter chain (title-similarity, reprint, VARIANT_CONTAM_RE, variant preference, cover-letter, lot, half-issue, TPB, slab, signed, grade proximity, creator match, price sanity, dedup — none check publisher). A cross-publisher listing sharing creator-name tokens with our confirmed variant (e.g. "Sabrina: The Teenage Witch #1 **Oni Press** Dan Parent 1:20 Classic Variant PRESALE" admitted into an Archie-anchored identity's comp pool purely on "dan"+"parent" token overlap) is indistinguishable from a same-publisher match. Separately, PRESALE/preorder status is not detected anywhere in the filter chain (grep-confirmed: zero hits for `presale`/`preorder` in `api/comps.js`) — a listing for a book that has not yet shipped can anchor a floor guard as if it were a completed transaction. TRIGGER: any creator whose name appears on variant covers across MULTIPLE publishers (not uncommon for prolific cover artists) risks cross-publisher comp contamination whenever the OTHER publisher's listing title happens to share the creator-name tokens with our confirmed variant text. Likely emptied the comp pool entirely if excluded (would have routed to RESEARCH or fallen to the SAME `pc_estimate` tier-4 path GK-95/96 describe — not independently verified against live data, reported as analysis not measurement). Not fixed — investigation-only dispatch, comp-filter/pricing-math boundary, needs its own greenlight. | aliases:
GK-98 | CLOSED | pending | 2026-08-14 | **RE-CLOSED (GrailKey Directive AG, 2026-08-14) — CORRECTING the AF closure below, which was premature.** AF's `discriminative-corroboration` resolver genuinely works (verified again here) but its output was being silently force-reverted back to Vision's generic title 3ms later by a downstream, independent consumer — `checkAssemblyIntegrity` ("22e", `src/lib/identityCore.js`) — that AF's own trace did not enumerate. Root cause: 22e's Rule 1 zero-support carve-out requires `compTitles.length >= 3` to even evaluate whether missing Vision tokens are legitimately unsupported; a genuinely thin (1-member) discriminative-corroboration family can never clear that floor, so 22e's conservative default (force Vision) fired every time, unconditionally, for exactly the shape AF exists to rescue. Confirmed via production log evidence (10.524–10.532, AF's win at 10.524 immediately reverted by `[22e] FORCED` at 10.532) and reproduced directly against real committed source (`git show HEAD`, pre-AG) in `tests/grailkey-directive-ag-22e-provenance-exemption.test.js` Fixture 1. **AF's own "ADDITIONAL KILL PATHS: none beyond those two" claim was WRONG** — see the new Pattern Library standing rule this dispatch adds ("enumerate every consumer of the changed value, not only competing branches within the module that produces it"). FIXED, narrowly: `shouldSkipAssemblyIntegrityCheck` (`src/lib/identityCore.js`) extended to also exempt `'discriminative-corroboration'` (was: only `'refused-identity-conflict'`) — one line, one new decision value, 22e itself untouched, `checkAssemblyIntegrity`'s Rule 1/2/compound-whitelist logic untouched. Every OTHER consumer of the family-selection result was enumerated and checked: Q141-A's PC-anchor-projection gate was found ALREADY safe (protected by `isCorroboratedIdentitySource`/`FAMILY_OVERRIDE_DECISIONS`, a byproduct of AF's own extension, zero new code needed); Phase 2's separate 22e call site only evaluates Rule 2 (excess-non-consensus-tokens), not the failing Rule 1, so needed no change for this specific defect. `tests/grailkey-directive-ag-22e-provenance-exemption.test.js`, 32/32 (Fixture 1 end-to-end DIRECT+MIRRORED trace, Fixture 2 narrow-scope proof over 7 decision values, Fixture 3 Flash #139 negative control, Fixture 4 issue-contradiction still blocks, Fixture 5 skip branch writes no authority state). **WATCH finding, not fixed here, logged separately as GK-109**: the surviving discriminative title still risks a wrong PriceCharting anchor downstream (1997 can reappear via a different mechanism than the one this ticket fixes). Original AF closure text retained below for history, superseded by the correction above. Fixed by a new discriminative-corroboration check in `selectTitleFamilyCandidate` (`src/lib/imageSearchIdentity.js`), running BEFORE both existing decision branches (top-rank-protection AND weighted-consensus — direct execution against a realistic Sabrina-shaped fixture proved the generic family can win via EITHER path depending on whether it occupies rank 0, so a fix scoped to one branch would have left the other open). A candidate family earns adoption only when (C4) it carries >=2 tokens independently corroborated by `opts.visionVariant` (the raw Vision-supplied variant read, the same `req.body.variant` proxy already used elsewhere pre-`confirmedVariant`) — an uncorroborated descriptor like "Foil" earns nothing — AND (C2, the Flash #139 invariant, unrelaxed) its own issue signal does not contradict Vision's, via `resolveFamilyIssueConsensus` (deliberately not `extractIssueFromTitle`, which suppresses a bare "#1" as marketingContext for exactly this title shape — confirmed empirically, would have silently defeated the fix). Two candidates whose corroboration is internally split within one Jaccard-merged family, or disjoint across two families, yield the EXISTING `refused-identity-conflict` decision (C5/C6) — no REVIEW write, no new mechanism; `FAMILY_OVERRIDE_DECISIONS` (`src/lib/compHygiene.js`) extended by one string so the existing `resolveIdentity` override gate (identityCore.js) and downstream comp-query construction pick up the new decision automatically. Verified DIRECT against the real function: Sabrina resolves to `discriminative-corroboration` / "sabrina annual spectaculer dan parent"; Flash #139 unaffected (no `visionVariant` in that scenario, the new branch's own guard is a no-op). `tests/grailkey-directive-af-discriminative-corroboration.test.js`, 25/25. Original finding retained below for history. Original finding: `scoreTitleFamilies` (`src/lib/imageSearchIdentity.js:1200-1248`) selects the winning identity family purely by rank-weighted vote count (`weightSum`, tiebreak `count`) — every token contributes equally to family-formation consensus (`src/lib/imageSearchIdentity.js:1122-1126`, `tokenFreq`/60%-threshold, no distinction between discriminative tokens like "Annual"/"NYCC"/"Foil"/a specific year and generic franchise tokens like the character/series name). No internal-coherence check exists anywhere in family formation or scoring — a winning family whose members span multiple years/issues/one-shots/lots (self-contradictory on the very facts that would identify a specific book) scores identically to a coherent one, since only title-token overlap is measured, never cross-member agreement on year/issue/series. Real production instance: a 1-member family carrying every discriminating token the physical book showed (Annual, 2024, #1, Dan Parent, NYCC, Foil) lost outright to a 14-member family of generic, mutually-contradictory franchise-token rows (spanning 1997–2022, one-shots, a multi-issue lot) — confirmed via direct trace: nothing in `scoreTitleFamilies` or its inputs computes or consults per-family internal consistency, and nothing distinguishes token specificity. Relationship to the named "measuring coherence against the wrong population" disease class (`docs/PATTERN-LIBRARY.md`, four instances, GrailKey Dispatch 25): related in shape (a metric correct for "which family has more raw votes" silently answers the different question "which family is the correct, unique identity") but NOT a literal instance of the four cataloged occurrences (those are all INTRA-family agreement computations — unanimity/majority/silence-vs-dissent; this is INTER-family selection by raw vote count) — a fifth, distinct manifestation of the same general class, not a duplicate. Related to GK-97 (Task 3) as the SAME general class (evidence population not verified against the book's actual population) at a DIFFERENT pipeline stage — GK-97 is comp/pricing-pool contamination for an already-confirmed identity, this is identity-pool selection itself, upstream of GK-97. Not fixed — investigation-only dispatch. Explicit non-goal: no resolver change (top-rank-guard/q140/weightSum/Q38/Flash #139) proposed or implied — this is a report of mechanism only; Jimmy rules on remedy, and any remedy must be checked against the standing Flash #139 invariant (issue-axis consensus must never be rank-weighted) before any change to this file. | aliases:
GK-99 | OPEN    | -       | 2026-08-14 | Directive Y, Task 2 (P0 false-READY trace) — TRACED, PREMISE PARTIALLY REFUTED, DEEPER GAP FOUND. The directive's stated concern (correction form pre-populates every field, submitting locks unedited facets as OPERATOR_CONFIRMED) does NOT match the code: `src/App.jsx:6048-6054` computes `correctedFields` as a genuine client-side diff (`trimmed !== String(item[f] ?? '').trim()`) — an untouched input (even though it visually displays the item's current value via the `??` fallback at line 6026) is correctly excluded from the payload's `manualAuthority.correctedFields`, and the server's `validateManualAuthority` (`src/lib/manualCorrection.js:165-186`) derives `acceptedFields` (and therefore `identityAuthority` locks, GK-85) from THAT client-supplied list, not from raw field presence in the wire payload (which always includes all 5 fields per `buildManualCorrectionPayload`, `src/lib/manualCorrection.js:693-715` — a WIRE-FORMAT convenience, not a lock-scope signal). Blanking a field is also safe (normalizes to null, lands in `emptyFields`, never `acceptedFields` — neither "clears the facet" nor "locks an empty string," it's simply not submitted). **However**: `validateManualAuthority` has NO independent server-side verification that a field genuinely changed — it trusts `manualAuthority.correctedFields` outright; the `App.jsx` comment claiming this "mirrors, does not replace" a server-side check (`src/App.jsx:6044-6046`) is itself inaccurate — there is no such server-side re-derivation, only allow-list + non-empty-value filtering. **The deeper, real gap**: the correction form's own visibility is gated by `getCorrectableFields` (`src/lib/manualCorrection.js:306-311`), which returns the UNION of `item.identityMissingFields` and `item.identityProvisionalFields` — both EMPTY for a confidently-but-WRONGLY-resolved identity (the exact Sabrina failure class, GK-98), since the wrong identity won a normal, non-flagged consensus vote, never triggering either missing-field or provisional-adoption bookkeeping. `correctableFields.length === 0` → the "Correct identity" form (`src/App.jsx:6012`) never renders at all for this class of book. There is no other correction entry point in the codebase (grep-confirmed: `onManualCorrect` wired at exactly one render site, `src/App.jsx:13292`, consuming exactly this one form). TRIGGER: an operator who discovers a confidently-resolved-but-wrong identity (no missing fields, no provisional flags — the pipeline itself doesn't know anything is wrong) has NO UI path to correct title/issue/year/publisher/variant at all; `reIdentifyBook` (re-run Vision on the stored photo) is the only other identity-touching action available, and it re-runs the SAME resolution pipeline that produced the wrong identity, with no guarantee of a different outcome. Not fixed — investigation-only dispatch, needs its own scoping (widen `getCorrectableFields` to unconditionally offer all 5 fields regardless of missing/provisional flags, vs. a separate "something's wrong, let me fix it" entry point). | aliases:
GK-100 | OPEN   | -       | 2026-08-14 | Directive Z (transaction authority boundary) — `api/list-ebay.js`'s new server-side `deriveActionAuthority` enforcement gate (GK-95/96 fix) is wired into the single-item listing branch only. The bundle-listing branch (`item.bundle === true`, `api/list-ebay.js:700-754`) builds and submits `AddFixedPriceItemRequest` directly from `item.items[]` with no `actionAuthority`/lock check of any kind — a bundle containing a non-READY book (e.g. a `pc_estimate`/thin-pool/wrong-identity book like the Sabrina production instance) can still be listed via the bundle path even though the identical book would be rejected with `403 ACTION_AUTHORITY_NOT_READY` through the single-item path. Client-side, `getListableBooks` (the bulk-listing/bundle-selection filter, `src/App.jsx`) DOES route every candidate through `passesContractGate`/`actionAuthority` before it becomes selectable for a bundle — so the gap is server-side defense-in-depth only, not (currently) reachable through the normal UI flow; it is reachable by any direct/replayed request to `/api/list-ebay` with `bundle:true` and a hand-built `items[]`. TRIGGER: a bundle-listing request whose `items[]` includes a book with `pricingSource:'pc_estimate'` (or any other FALLBACK_ONLY/NONE marketStanding, or CONFLICTED/UNRESOLVED identityStanding) lists successfully with no rejection, while the same book alone through the single-item path is blocked. Not fixed — found and reported per Directive Z's Task 2 output, explicitly out of the directive's stated scope (single-item `/api/list-ebay` enforcement was the named deliverable); needs its own greenlight for whether to gate per-item (reject the whole bundle on any non-READY member) or per-bundle-with-exclusion (silently drop non-READY members) — a real product decision, not a mechanical port of the single-item check. | aliases:
GK-101 | CLOSED | cb987d1 | 2026-08-14 | Directive AB — evidence applicability custody. `deriveMarketStanding` (`src/lib/actionAuthority.js`) read `pricingSource` alone, so a pool that reached "current" tier (`active_ask_derived`) could be granted `EXACT_CURRENT` even when `api/comps.js`'s Filter 1c (`applyVariantPreferenceFilter`) found zero comps matching `confirmedVariant` and fell back to the broader, variant-blind "keeping all" pool — Computed-Then-Discarded, 7th instance; the fact was computed and logged at the filter but never carried past that block. Real production instance: Sabrina Anniversary Spectacular #1, Dan Parent NYCC Foil, 2026-08-14 08:38, `d7ba062` — `actionAuthority.state: READY`, List button enabled, priced off 14 generic 1997 comps none of which were the confirmed foil variant. Fixed by threading a new `matched`/`variantApplicability` signal (`CONFIRMED`/`UNVERIFIED`/`null`) from Filter 1c through `api/comps.js` → `api/enrich.js` (`out.variantApplicability`, unconditional own-property, presence-aware at all 5 App.jsx merge sites + `api/list-ebay.js`'s synthetic re-derivation) → `deriveMarketStanding` (floors `EXACT_CURRENT` to `SIMILAR_ONLY` when `UNVERIFIED`) → the EXISTING Z state machine (no parallel denial path) → a new soft `market-standing-variant-unmatched` lock (`VARIANT_UNMATCHED_POOL` reason code) mirroring the FALLBACK_ONLY/NONE locks Z already added. Pricing math, `mode=any` keeping-all behavior, and the sold-path `variantMismatch:comp_has_user_none` enforcement are all untouched (verified: `src/lib/soldVerification.js` zero-diff). `identityStanding` for this exact book remains `CONFIRMED` (wrong) — GK-98 is unchanged, explicitly out of scope. `tests/grailkey-directive-ab-evidence-applicability.test.js`, 35/35. | aliases:
GK-102 | OPEN   | -       | 2026-08-14 | Directive AB Task 1e — traced, narrower than the preflight's framing. `⚠️ NEEDS REVIEW` badge (`src/App.jsx` `getReadinessStatus`) is gated by `checklist.decisionSafe.status`, which — WHEN `item.contract?.actionAuthority` is present — already derives directly from `authority.state` (`READY`→pass, `REVIEW`→caution, `LOCKED`→fail; `src/App.jsx:491-500`, shipped by Directive Z). The production "NEEDS REVIEW banner + ACTION AUTHORITY: READY" co-occurrence this ticket was opened to explain was GK-101 itself (authority.state was WRONGLY READY pre-fix; once genuinely READY, the badge and the authority panel do agree, confirmed no other code path can diverge them for that case) — not a second, independent contradiction bug. The one real residual gap: `decisionSafe`'s fallback branch (`item.contract.actionAuthority === undefined` — a catalogue item scanned before actionAuthority existed) uses a `decision.action`-only heuristic (`LIST_NOW`/`LIST_LOW`→pass) with no authority concept at all, so a legacy item could in principle still show a badge/panel mismatch if it also somehow renders an actionAuthority panel from stale/partial contract data. Not reproduced, not fixed — logged for completeness per Directive AB's own preflight instruction, scope is legacy pre-Z catalogue items only, not a live current-scan defect. **CORRECTED (Directive AC, 2026-08-14): originating premise refuted by trace, not confirmed — the surviving, unreproduced scope above (pre-Z legacy fallback branch only) is the entirety of what remains open on this ticket.** | aliases:
GK-105 | OPEN   | -       | 2026-08-14 | Directive AD preflight — logged verbatim per the directive's own instruction, not yet investigated. Pre-enrich price render: a card showed "$40.00" alongside "Loading market data…" with no actionAuthority block, and this state persisted 2+ minutes on 2026-08-14 (build 45b0515). P0-candidate pending its own dispatch — not traced or fixed here (explicit non-goal, Directive AD). | aliases:
GK-106 | OPEN   | -       | 2026-08-14 | Directive AD preflight — logged verbatim per the directive's own instruction, not yet investigated. A "VERIFIED" banner rendered at the top of a card whose `actionAuthority.state` was `REVIEW`. Trace-only, not fixed here (explicit non-goal, Directive AD). | aliases:
GK-107 | CLOSED | a501ded | 2026-08-14 | Directive AE — cache-key identity coverage. AD's `STALE CACHE REACHABLE? NO` claim was too broad. Confirmed by direct source read: `pc:` (`buildPriceChartingCacheKey`, `src/lib/cacheKeys.js`) and `ac:` (`buildActiveCompCacheKey`'s `filterContextFingerprint`, same file) never encoded publisher at all — only `cv:` (`buildComicVineCacheKey`) did. Year WAS already covered by all three (confirmed via Control A, differential proof) — the real gap was publisher-only, not year-only. Actionability traced: the DIRECT route (`isFromPC` variant/key multiplier, gated to `VARIANT_MULT_ELIGIBLE_SOURCES = {pricecharting, pc_estimate, browse_api}`, `api/enrich.js:8362-8367`) cannot reach `EXACT_CURRENT`/READY — none of those three sources are in `EXACT_CURRENT_SOURCES` (`src/lib/actionAuthority.js`). The INDIRECT route is genuinely actionable: `ac:`'s cached object IS the fully filtered, PRICED active-comp pool (per `cacheKeys.js`'s own header doctrine — "NOT raw evidence"), so a publisher-only correction could return a pool fetched under the OLD publisher's eBay query text (`pubKeyword` in the Attempt-0 template, `api/comps.js`), sourced `active_ask_derived`/`verified_active` — EXACT_CURRENT-tier, reachable READY — for a population never actually queried under the corrected publisher. A live false-READY route through the GK-99 recovery path, per this dispatch's own decision framework. Fixed minimally: `publisher` added to `buildFilterContextFingerprint`'s hashed inputs (`ac:`) and to `buildPriceChartingCacheKey`'s template (`pc:`, both the full-title and stripped-title/`pcKey` variants) — one segment each, positioned matching `cv:`'s existing `title|issue|publisher|...` convention, no cache-layer redesign. `COMP_FILTER_VERSION` 11→12, `PC_FILTER_VERSION` 2→3 (same version-salting precedent as Dispatch 36/Dispatch 03), forcing a clean MISS on every old entry rather than risking a silent stale read. `parseCacheKeyIssueSegment` unaffected (title/issue positions unchanged; nothing downstream reads segments positionally beyond those two, confirmed by grep across every test file). Secondary, NOT independently fixed (covered by the same `ac:` fingerprint fix): a stale `pcYear` feeds `resolveYear()` unconditionally (`api/enrich.js:4765-4773`, runs regardless of `identitySource`) and could shift `confirmedYear`, which the era-consistency filter then consumes — narrow risk window (only when the stale value is within `resolveYear`'s own ±2y tolerance of the operator-supplied year), not chased further. `tests/grailkey-directive-ae-cache-identity-atomicity.test.js`, 31/31. | aliases:
GK-108 | OPEN   | a501ded | 2026-08-14 | Directive AE — Q41 acknowledge vs correction-pending lock. Client-side atomicity gap CLOSED this dispatch (see below); ticket stays OPEN because the server-side half is a deliberately unfixed STOP GATE, not a resolved property. AD's own handoff flagged this as "noted not chased." Traced: TWO independent Q41 acknowledge paths exist in `src/App.jsx`'s CollectionDetail listing-action IIFE, not one — Directive AD's `correctionSubmitting` gate covered only the `researchAckNeeded` path (`🔍 {action} recommended...`). The SECOND path (`item.contract.locks.length > 0` branch, `"Engine could not verify a price..."`) was entirely unguarded: it reads `item.priceOverridden` (which flips `true` on every keystroke in the List-price input, via an unconditional `onUpdateField` call with zero `correctionSubmitting` gate anywhere nearby) and `item.q41Ack` (settable by that same button's own onClick, also unconditional) — together, typing a price and clicking that button during OR after a pending/failed correction produces a fresh, valid `q41Ack` that satisfies `q41Unlocked` downstream, re-enabling the List button. `correctionSubmitting` alone could not have closed this even if added to that button too — it resets to `false` the moment the fetch settles (success OR failure), while `submitManualCorrection`'s pending lock (`listingHardLockReason:'correction-pending'`) is deliberately never reverted on failure (Directive AD's own design). **CLIENT FIXED**: a single top-of-render guard (`item.listingHardLockReason === 'correction-pending'`), placed before both Q41 branches and the List button in source order, preempts all three uniformly — reuses the existing field AD already introduced, no new mechanism, closes both the in-flight window and the post-failure window in one check. **SERVER — STOP GATE, deliberately not fixed**: `api/list-ebay.js`'s `syntheticOut` (the server's independent re-derivation object, Directive Z's C3) has zero visibility into `listingHardLocked`/`listingHardLockReason` — confirmed by direct source read, no such field anywhere in that object's construction. The pending-correction state is a pure client-side value with no server round-trip or independent verification path at all. Per this dispatch's own explicit instruction, this is NOT closed by accepting a new client-supplied `correctionPending` boolean at `/api/list-ebay` (that would reproduce GK-103's exact trust defect one level sideways). Recorded as the SAME underlying shape as GK-103 (client-supplied/client-only state at the listing trust boundary) — "spoof away the correction-pending state" is one more concrete instance of GK-103's already-logged Trigger A/B, not a new independent gap. A genuine server-side close would require a server-owned session/KV tracking mechanism — explicitly out of scope ("no new mechanism"). `tests/grailkey-directive-ae-cache-identity-atomicity.test.js`, 31/31 (shared file with GK-107). | aliases:
GK-103 | OPEN   | -       | 2026-08-14 | Directive AC — direct/replayed-request trust-boundary gap in `/api/list-ebay`'s server-side re-derivation, one layer beneath Directive Z's C3 verdict-forgery protection. `api/list-ebay.js:791-804` builds the synthetic re-derivation object (`syntheticOut`) directly from client-sent request fields — `pricingSource: item.pricingSource \|\| null` (793), `matchConfidence: item.matchConfidence \|\| null` (794), `rawComps: item.rawComps \|\| null` (795), `variantApplicability: item.variantApplicability \|\| null` (804, added by Directive AB) — then derives `deriveLocks`/`deriveActionAuthority` from it honestly. Z's forged-READY acceptance test (`tests/grailkey-directive-z-transaction-authority.test.js`) proves a client cannot send `actionAuthority:{state:'READY'}` directly and be believed; it does not prove a client cannot send FALSE INPUTS the server then honestly derives READY from — "the client cannot forge the verdict" is narrower than "the client cannot forge the evidence the verdict is derived from." **Trigger A — asserted evidence:** a direct/replayed request supplying `variantApplicability:'CONFIRMED'` (or a favorable `pricingSource`) on a book that is genuinely variant-unmatched reproduces the exact GK-101 P0 through the gate built to stop it. **Trigger B — absence collapsed into not-applicable:** AB's tri-state is explicit (`null`=not applicable, `'UNVERIFIED'`=checked-and-unconfirmed) but the `\|\| null` coalesce at line 804 cannot distinguish an ABSENT property from a genuine `null` — a legacy/pre-AB catalogue item with `confirmedVariant` present, `variantApplicability` absent, and a current `pricingSource` is silently treated as not-applicable rather than not-proven. The fresh-scan path is NOT affected (`api/enrich.js` sets `out.variantApplicability` as an unconditional own-property; App.jsx's five merge sites are presence-aware) — the loss is specifically in this server reconstruction. Classification: NOT a normal-UI-reachable P0 (client-side `getListableBooks`/`passesContractGate` route every candidate through `actionAuthority` before it becomes selectable, same as GK-100's bundle-path framing), NOT an AB regression (AB extended `api/list-ebay.js`'s pre-existing Z-era synthetic-reconstruction pattern along its own established shape, same class of gap as GK-100), a PRE-EXISTING trust-boundary shape Z's C3 named but did not fully close. Same trust boundary as Z's C3, one layer beneath the verdict, related to GK-100 (server-side listing-endpoint gaps) as a sibling finding, not a duplicate. Not fixed — no validation, shape check, or presence check added to `api/list-ebay.js` in this dispatch, deliberately. Not in scope for GK-99. TRIGGER for reopening: before relying on legacy pre-AB catalogue items for listing, or before `/api/list-ebay` is exposed to any non-first-party caller. **Widened by Directive AD (GK-99, 2026-08-14):** GK-99 widened operator access to `validateManualAuthority`'s entrance (reachable "Correct identity" flow now available on any completed card, not just missing/provisional-flagged ones) — GK-103's own gap is unchanged (still server-side trust of client-sent fields, still not fixed), but a wider population of correction requests now flows through the same entrance, since the widening is reachability-only, not an authority-model change. Recorded, not fixed — still explicitly out of scope for GK-99. | aliases:
GK-109 | CLOSED | fd1ba6b / f093af1 / 1b214c2 | 2026-08-16 | **CLOSED (Directive AM, physical production evidence — Sabrina rescan, 2026-08-16): physical year 2024, catalog year 2022 retained as reference, operator's own book present in its own comp pool.** Prior history: **PARTIAL FIX, Directive AL.** Original Directive AG WATCH finding (traced 2026-08-14, three PC-anchor-trust gaps: `mainToken` overlap checks only the first tokenized word, year-gap validation skipped entirely when `comicYear` is null, `assessPcAnchorTrust` returns `'COMPATIBLE_REFERENCE'` not `'REJECTED'` on null year) is UNCHANGED — none of those three gaps were touched. What Directive AL fixed is a DIFFERENT, narrower manifestation found in fresh production evidence (Sabrina Annual Spectaculer #1, build `2bb1b01`, 2026-08-16 01:59): the N2 re-anchor block (`api/enrich.js` ~5686) reassigned `priceCharting` to a NEW, correct variant candidate but left `out.pcProductId`/`pcProductName`/`pcEbayEpid`/`pcLastUpdated`/`out.pc*Price` and `confirmedYear` stamped from the OLD, stale base-entry anchor (1971) — deleting the operator's own book from its own era-filtered comp pool. Fixed: N2 now atomically re-projects every `out.pc*` field and re-derives `confirmedYear` (same `resolveYear` policy, guarded against stomping an intervening more-specific correction) in the same step the anchor changes. `tests/grailkey-directive-al-anchor-variant-authority.test.js`, 17/17. Stays OPEN (not CLOSED) — the original three-gap AG finding above is still live, and closure requires physical-scan confirmation per this repo's standing evidence bar (GK-113/114 precedent), not unit fixtures alone. **EXTENDED (Directive AL continuation, same day, 4e)**: physical-vs-catalog year custody now separated at the N2 reprojection site — `reconcilePhysicalYear`/`extractFirstEligibleYearCandidate` (`identityReconciler.js`) give the scan's own first-eligible-visual row's year first look before the catalog (PC anchor) year is allowed to stand in; Sabrina's confirmedYear now resolves to 2024 (physical), not 2022 (catalog-because-PC-said-so), with 2022 retained separately as `out.physicalYearFacet.catalogYear`. Falls back to the pre-existing `resolveYear` policy (unregressed) only when no physical candidate exists at all. Still stays OPEN — physical-scan confirmation required. | aliases:
GK-110 | CLOSED | pending | 2026-08-14 | Directive AG (GK-98 kill path 3) — the ticket for AG's own fix, logged per the directive's instruction. Full description merged into the corrected GK-98 entry above (same fix, same commit, same test file) rather than duplicated here — see GK-98's Directive AG correction paragraph for the complete root cause, fix, and verification. Standing rule this dispatch adds to the Pattern Library (`docs/PATTERN-LIBRARY.md`): a trace that enumerates kill paths for a changed value must enumerate every CONSUMER of that value (every place downstream code reads/acts on it), not only competing BRANCHES within the module that produces it — AF's trace enumerated the two decision-branch kill paths inside `selectTitleFamilyCandidate` correctly but never asked "who else reads `familyCandidate.decision`/`confirmedTitle` after this function returns," which is exactly where 22e was silently reverting the result. | aliases:
GK-111 | CLOSED | pending | 2026-08-15 | **P0 — Directive AH, third false-READY.** Different path than GK-96 (tier-4 pc_estimate) and GK-101 (tier-3 active_ask_derived): tier-2 blend (`sold_active_blend_30`). Production instance (2026-08-15 00:33, build `f52c92f`): the operator's actual book ("...NYCC Foil LTD 50 By Dan Parent...", $24.99, SOLD) was rejected by `src/lib/soldVerification.js`'s OWN variant fallback as a variantMismatch, then silently re-admitted (`variantVerified:false`) and blended (70/30) against a DIFFERENT book's single active ask ("...Pink Lava Foil...LTD 5", $109.95) that matched only on the shared bare-substring token "nycc" — `actionAuthority.state:READY`, `$65.88` List button enabled. Root cause, two independent gaps, both fixed: (1) Filter 1c's active-pool applicability signal (AB/GK-101) has a SIBLING on the sold side — `verifySoldComps`'s own variant fallback — that was never stamped anywhere `deriveMarketStanding` (`src/lib/actionAuthority.js`) could see; a second, independent "measuring coherence against the wrong population" site. Fixed via `soldPoolFallbackConsumed` (`src/lib/priceBands.js`, `computePriceBands`) — scoped to whether THIS tier's own `market` value actually consumed a variant-fallback sold pool (never merely "one existed upstream" — proven false-negative-free by Fixture 3b, the unconsumed-fallback negative control: `soldPoolTooThinToOverride`/GK-34's active-dominant branch correctly does NOT trip it) — folded by `api/enrich.js` into the SAME `out.variantApplicability` field AB already wired into `deriveMarketStanding` (zero changes to that function), with a new `out.variantApplicabilitySoldFallback` own-property for reason-code precision (`src/lib/responseContract.js`: `market-standing-sold-variant-fallback` / `SOLD_VARIANT_FALLBACK_POOL`, discriminated from AB's original `market-standing-variant-unmatched` / `VARIANT_UNMATCHED_POOL`). (2) `marketStanding=EXACT_CURRENT` has no pool-size floor — deliberately left alone (a genuinely exact/current N=1 comp IS exact and current; demoting the LABEL to force a lock would make it dishonest, not safer) — instead a new, tier-independent `single-comp-pool` soft lock (`SINGLE_COMP_POOL`) fires whenever an EXACT_CURRENT-standing price is backed by fewer than 2 total comps, deliberately NOT keyed to `matchConfidence.tier` the way the pre-existing `low-tier-thin-pool` lock is — this dispatch's own production case scored `matchConfidence.tier=LOW` at `totalComps===3` exactly, one comp above that lock's `<3` floor, so it never fired; a MEDIUM/HIGH-confidence book with the same 1-comp pool would have cleared it untouched too (the same tier-dependency shape GK-96 already named, HIGH-only cap / LOW-only lock / MEDIUM-slips-through). **Server boundary (1f), found tracing, not a new mechanism**: `variantApplicability` was NEVER actually included in the single-item `/api/list-ebay` request body (`src/App.jsx`) despite the server already reading `item.variantApplicability` since Z/AB shipped — every listing request silently sent `undefined` for it, meaning AB's own server-side GK-101 protection was never actually reachable through the normal listing UI at all. Fixed alongside this dispatch's own new field (same raw-evidence-field convention, GK-103's trust boundary unchanged/not touched) — without this fix Fixture 7 (server denial) could not have passed regardless of anything else here. `tests/grailkey-directive-ah-sold-fallback-authority.test.js`, 54/54 (Fixture 1 production shape end-to-end, Fixture 2 sufficiency floor isolated, Fixture 3 fallback-demotion isolated, Fixture 3b unconsumed-fallback negative control, Fixture 4 four-way monotonicity incl. a demonstrated upward route, Fixture 5 no-over-fire on a healthy book, Fixture 6 AB active-path lock unregressed and correctly discriminated from the new sold-path lock, Fixture 7 server-side independent denial). No pricing math, comp-filter reordering, or resolver changes (C2/C6). GK-112 logged for the matcher-looseness root cause (deferred). Sabrina rescan is Jimmy's own next step, not verified by this dispatch's suite. | aliases:
GK-112 | OPEN    | -       | 2026-08-15 | Directive AH Task 1d, traced not fixed (explicit non-goal, C6) — `applyVariantPreferenceFilter`'s active-pool matcher (`api/comps.js:579-678`, Filter 1c) is a pure substring matcher over `classifyVariantTokens`' registry-driven `specificWords`, with two compounding gaps proven directly against the production Sabrina shape: (1) `classifyVariantTokens` (`src/lib/imageSearchIdentity.js:235`) never recognized "Dan"/"Parent" as registry tokens at all (a creator name, not a member of the variant-taxonomy registry it draws from) — for `confirmedVariant="Dan Parent NYCC variant"` the entire match reduced to `specificWords.length===1` (bare "nycc"), `matchMode='any'`, `orMatch(['nycc'])` — a single generic convention token, confirmed by direct trace of the real matching code, not inferred. (2) print-run tokens ("LTD 5"/"LTD 50", ratio fractions) and finish-descriptor tokens (Pink Lava vs. plain Foil) are entirely absent from the registry this matcher consults — even a token-exact (not substring) rewrite of the SAME matcher could not distinguish them, because `confirmedVariant` itself is never populated with those tokens by Vision's variant capture in the first place. TRIGGER: any two variant editions of the same book sharing a creator name and/or convention token (a common shape for numbered/limited convention exclusives — the exact class this ticket's production instance belongs to) will continue to cross-match on the shared token alone regardless of GK-111's authority-side fix, which demotes standing but does not and cannot correct WHICH pool the price is drawn from. Not fixed — comp-filter/matcher-rewrite boundary, needs (a) extending Vision's variant capture to record print-run/finish qualifiers when visible, and/or (b) a genuinely discriminative matcher design, both requiring their own scoping and greenlight; explicitly out of Directive AH's scope per its own C6. | aliases:
GK-120 | OPEN | fd1ba6b / f093af1 / 1b214c2 / 6c92c93 | 2026-08-16 | **PARTIAL FIX, Directive AL.** Uncorroborated Vision variant claims could win PC-anchor selection via "best of zero" scoring — Venom Separation Anxiety #1 production evidence: `confirmedVariant="Tyler Kirkham variant"` (zero pool support, zero comp support) scored 0 against the sole deferred candidate "[Mayhew Virgin] #1 (2024)" yet still won and anchored pricing/cache/comp-query to a hallucinated creator. Fixed at the PC-anchor consumer only: `selectBestVariantCandidate` (`src/lib/identityCore.js`) now hard-vetoes a candidate naming a DIFFERENT registered creator than `confirmedVariant` (`hasCreatorConflict`, reusing `premiumCreators.js`'s registry via new `matchCreatorCanonicals`) and requires a positive token-overlap score to accept; refusal returns `null` (no PC anchor) rather than an arbitrary candidate. **NOT closed** — `confirmedVariant` itself is never reconciled/vetoed at its source (no evidence-reconciler built this dispatch, deliberately deferred per the directive's own DECISION GATE). `api/comps.js`'s eBay comp-search query construction (Attempt-0's full-variant-string embed, the `ARTIST_PATTERNS`-driven artist-specific attempt) still reads raw `confirmedVariant` text unconditionally — a hallucinated creator name can still reach the outgoing eBay comp query even after this fix. `tests/grailkey-directive-al-anchor-variant-authority.test.js` + `tests/q-pc-variant-score.test.js`, 25/25 combined. **CORRECTED (Directive AL continuation, same day)**: the prior handoff's "PASS with caveat" grading of B3 was wrong — re-graded FAIL, since the stated blocker (no Kirkham in the outgoing comp query) was unmet. **EXTENDED (4a)**: `reconcileVariantFacet` (`identityCore.js`) now extends Slice 1's evidence architecture to the variant facet — a bare, uncorroborated Vision claim (`variantIdentitySource==='vision'`, meaning none of the other 6 pipeline mechanisms fired) is overridden by a genuinely corroborated first-eligible-visual candidate when one exists; Kirkham is now demoted to recorded conflict evidence and the reconciled value (e.g. "Mike Mayhew virgin") is what `confirmedVariant` becomes BEFORE `fetchComps` reads it — proven via a real mocked-eBay-network test capturing the actual outgoing query string (`tests/grailkey-directive-al-4a-4e-variant-year-custody.test.js`, 37/37): Kirkham confirmed absent, Mayhew confirmed present. Deliberately conservative scope, documented not hidden: only intervenes on the bare-vision-only case (the other 6 mechanisms — CGC cert, eBay pool consensus, edition-warning printing, canonical-projection residue, family/publisher/imprint routing, manual correction — are untouched); when first-eligible-visual has no recognized creator/specific-token signal at all (generic-only, e.g. bare "foil"), the existing pipeline value is kept unchanged rather than degraded (found and fixed forward in this same dispatch: an earlier draft would have wrongly downgraded Sabrina's own correct "Dan Parent NYCC Foil variant" to a bare "foil" candidate). B3 now genuinely PASSES. **STILL OPEN — PHYSICAL ACCEPTANCE FAIL, Directive AM (2026-08-16 10:54-11:03).** Production disproved the prior "closed" framing on TWO axes: (1) **provenance laundering by the pipeline itself** — `firstEligibleVisual` was computed from `variantSourceItemsForReconciliation`, a FAMILY-NARROWED pool (Ship 26.3B); when the title-family resolver picked the wrong family (Venom: Lethal Protector instead of Separation Anxiety — itself driven by raw, pre-reconciliation Vision tokens feeding `selectTitleFamilyCandidate`'s `visionVariant` param, GK-98/AF), the reconciler could only ever bless a row FROM that wrong family as "first eligible," never the scan's true rank-1 row. **FIXED**: both reconciliation call sites now read `parsedVisualRows` (the same full, unbiased, pre-family-decision pool the ISSUE facet already correctly used) — proven directly against the real verbatim production row ("Venom - Separation Anxiety 1 Virgin Signed/Remarked by Mike Mayhew..."). (2) **Computed-Then-Discarded, reconciler edition** — a NONE reconciliation result left the prior Vision value standing untouched; **FIXED**, NONE now clears `confirmedVariant` (C8 applied consistently). `tests/grailkey-directive-am-variant-custody-real.test.js`, 28/28. **NOT achieved this dispatch, reported honestly rather than papered over**: the title-family resolver itself (which fed Kirkham's tokens into picking "Lethal Protector") reads raw, pre-reconciliation Vision variant text and cannot structurally wait for reconciliation (chicken-and-egg with family selection); a real fix requires either a title-family-scoring rewrite (explicitly out of scope per the directive's own non-goals) or evidence this dispatch does not have (the real Venom pool's rows 5/14 were never quoted verbatim, only summarized) to design a narrower gate. B4-1's "FORBIDDEN: family = Lethal Protector" sub-clause is therefore UNVERIFIED — the reconciler no longer LENDS AUTHORITY to whichever family won, but the family selection itself can still be wrong. See GK-121 (upgraded, same dispatch) for the identity-authority half of this defect. **CORRECTED (Directive AN, 2026-08-16)**: the AM-continuation-2 trace's T4/T5 hypothesis was itself investigated against the ACTUAL production evidence and found wrong for the ktl2r request specifically — ktl2r's real Vision title was "Venom" (not a constructed "Venom Lethal Protector"), the family resolver correctly reached `weighted-consensus`, and identity resolved correctly to Mayhew/Separation Anxiety. ktl2r's own defect was GK-120's Kirkham-poisoning of the PC variant path (F-1/F-3, already fixed). The actual "Lethal Protector" misidentification came from a DIFFERENT request (`wfvvb-1786903446411`) via a THIRD mechanism (GK-121's discriminative-corroboration token source, not title-family weak-overlap) — now fixed, see GK-121. **GK-120's family-selection half is SHIPPED-PENDING PHYSICAL by this same fix** (corrected from an earlier "CLOSED" claim — closure requires physical rescan, per this repo's standing bar) — the mechanism that could feed a wrong family (Vision-only discriminative-corroboration tokens) is the one GK-121 gates. **ACCEPTANCE-CORRECTION PASS (Directive AN, same day)**: the "wfvvb/dzq9h complete pools were never supplied" claim was WRONG — both were in `comic-vault-log-export-2026-08-16T19-05-10.csv` the whole time; the refusal to fabricate filler rows was correct process, but the underlying claim was false. Full end-to-end `selectTitleFamilyCandidate()` re-run against both complete, verbatim 20-row pools: **dzq9h (Dell'Otto) PASSES cleanly** — correctly selects the 7-member "amazing spider man dell otto gabriele" family (weight 9.0), Inhyuk/Lee excluded, Ultimate Spider-Man never selected. **wfvvb (Venom) surfaces a NEW, real, previously-hidden finding, logged not fixed**: Kirkham is correctly excluded and Lethal Protector is correctly never selected via discriminative-corroboration — but the fallback `weighted-consensus` path then selects **"ariel diaz venom carnage"**, a 3-member cluster of "Ariel Diaz Artbook" listings (a companion art print/portfolio product, not a comic-book printing of the item at all) — outweighing the single real Mayhew Separation Anxiety row (3 members/weight 5.5 vs 1 member/weight 5.0). This was always latent in the pool; the pre-fix Lethal Protector bug fired FIRST and masked it entirely, so it was never visible in production before now. Root cause: neither `isEligibleVisualRow` (identityReconciler.js, lot/variation-group only) nor `buildTitleFamilies`'s own `NON_GENUINE_COPY_RE` filter (photocopy/USB/digital-archive/scan-disc only) excludes an "artbook"/companion-print listing from family clustering or weighted-consensus eligibility. Not fixed this dispatch — a new finding requiring its own scoping, per this directive's own explicit instruction not to fold new discoveries in silently. | aliases:
GK-121 | OPEN | 1b214c2 / 6c92c93 | 2026-08-16 | **CORRECTED (Directive AN acceptance-correction pass, 2026-08-16): status reverted from CLOSED to OPEN, annotated SHIPPED-PENDING PHYSICAL.** The standing evidence bar for this registry has been physical production confirmation since GK-113/114 — closing on unit fixtures alone regresses that standard. Full mechanism fix and verification below stands; only the closure claim was wrong. Directive AL, record-only per the directive's own instruction — production title contamination, three instances observed same session: `9.8 Amazing Spider-Man #1 Dell'otto Virgin Variant` (seller's own 9.8 grade token embedded in the canonical title of a 9.6 book), `sabrina annual spectaculer dan parent`, `mike mayhew venom separation anxiety` (creator/variant metadata embedded in the title string rather than captured as separate fields). Not traced, not fixed. Narrow safety rule authorized by the directive if GK-109/GK-120 needed it (they did not, this dispatch): grade tokens and separately-extracted creator/variant metadata must not count as title-overlap evidence or PC variant-match evidence merely because they were embedded in a seller title. Full title-facet single-writer migration is its own future slice. **UPGRADED (Directive AM, 2026-08-16): no longer cosmetic — a real identity-authority defect.** Production evidence: Vision said "Amazing Spider-Man #1"; the pool held a 9-member Dell'Otto/Gabriele family, but a single Ultimate Spider-Man / Inhyuk Lee outlier row won because "inhyuk, lee, virgin" tokens counted as discriminative corroboration (GK-98/AF's own mechanism), and the reconciler then blessed that outlier row as `first-eligible-visual` — card showed "Ultimate Spider-Man Red #1 · Inhyuk Lee virgin" instead of the physical Dell'Otto ASM book. Stayed safe only because pricing separately refused a thin pool — identity custody itself never caught it. Directive AM's F-1 fix (immutable, unbiased `firstEligibleVisual`, see GK-120) removes the mechanism by which the outlier row got RELABELED as first-eligible after the fact — the authority-bearing half of this defect — but does NOT fix the underlying title-family SELECTION (why the Inhyuk Lee family won at all), which is explicitly out of scope (a scoring rewrite) and remains this ticket's own open work. **SHIPPED-PENDING PHYSICAL (Directive AN, 2026-08-16) — mechanism confirmed and fixed, not merely a scoring rewrite after all; closure reserved for Jimmy's post-deploy physical rescans, per this repo's standing evidence bar.** Two independent production instances (Venom `wfvvb-1786903446411`, Dell'Otto `dzq9h-1786903446411`) both traced to the SAME root cause: `selectTitleFamilyCandidate`'s discriminative-corroboration branch (`src/lib/imageSearchIdentity.js:2489-2545`) counted a token as "corroborated" whenever it appeared in BOTH Vision's own variant guess AND *any* family member's own raw text — including a family describing a completely different book (Venom: Lethal Protector for the Mayhew Separation Anxiety scan; Ultimate Spider-Man for the Dell'Otto ASM scan). Fixed: a token now ALSO must be present in the frozen rank-1 eligible visual row (F-1's own mechanism, `selectFirstEligibleVisual`, called on the identical `items` array F-1 uses downstream — byte-for-byte the same frozen row). Verified against all 4 production shapes: Sabrina (all 3 tokens physically present — survives), Venom wfvvb (only "virgin" survives, below the 2-token C4 floor — dies), Dell'Otto dzq9h (same — dies), Venom ktl2r (unaffected, byte-identical decision). `tests/grailkey-directive-an-physical-corroboration.test.js`. Real side effect found and fixed forward: AF's own pre-existing test fixtures (`grailkey-directive-af-discriminative-corroboration.test.js`) had the corroborating row at array index 1, not index 0 — an inaccurate approximation of the real (verified) Sabrina production ordering, which genuinely has the NYCC row at rank 1. Reordered to match reality; one fixture (C5, disjoint-candidate conflict) had its own premise narrowed by the fix itself — see the Pattern Library entry for the honest scope note on what C5 can no longer test. | aliases:
GK-122 | OPEN | af79f80 / 1b214c2 | 2026-08-16 | Directive AL continuation, found during a user-directed post-B3-R provenance audit. `extractVariantTokensByAxis` (`compHygiene.js`) has NO authentication axis (only coverType/distribution/coverLetter/printing/artist) — a real, physically-present "Signed" attribute in a first-eligible-visual row's own raw text is silently dropped when `reconcileVariantFacet` (`identityCore.js`) builds its candidate. Confirmed directly against the real Venom production row ("Mike Mayhew Signed Venom Separation Anxiety Variant Cover Marvel Comic NM"): extraction yields `candidate="Mike Mayhew"`, losing "Signed" entirely. Does not invalidate B3-R (Kirkham absence / Mayhew presence both still hold) but is a real information-loss gap in the SAME reconciler this dispatch shipped — "Signed" is a price-relevant attribute (see the standing Q-SS open item, CLAUDE.md) that a genuinely evidence-faithful reconciler should not drop. Not fixed — `extractVariantTokensByAxis` is a shared function (also consumed by `soldVerification.js`, `evidenceEligibility.js` per its own header comment); adding an authentication axis needs its own scoping/regression pass across those consumers, not a rushed patch under this audit. Discovered honestly rather than hidden: the test fixture that WOULD have masked this (a fabricated row containing "Virgin," which the real production row never said) was itself corrected in the same pass — see GK-122's companion provenance-laundering note in the Pattern Library. **PARTIAL CLOSE (Directive AM, 2026-08-16)**: four new axes (event/convention, print-run/limitation numbering, color-finish, authentication) added LOCALLY to `identityCore.js` (deliberately NOT the shared `extractVariantTokensByAxis` in `compHygiene.js` — same "needs its own scoping across other consumers" caution as before). Proven against the real USM/Dell'Otto verbatim row ("ULTIMATE SPIDER-MAN #1 CGC 9.8 INHYUK LEE FAN EXPO PHILLY WHITE VARIANT LE 800"): all four facets (Inhyuk Lee, Fan Expo Philly, White, LE 800) now retained, none silently dropped. The ORIGINAL finding ("Signed" dropped) is also now closed for real production text ("...Virgin Signed/Remarked by Mike Mayhew...") via the new authentication axis. Still not merged into the SHARED `compHygiene.js` function — remains this ticket's own deferred work if `soldVerification.js`/`evidenceEligibility.js` are ever found to need the same axes. | aliases:
GK-123 | OPEN | 598553c / 26da33b | 2026-08-16 | Directive AN acceptance-correction pass, found running a full end-to-end `selectTitleFamilyCandidate()` re-verification against the complete, verbatim 20-row wfvvb-1786903445323 pool (Venom Separation Anxiety). GK-121's fix correctly excludes Kirkham and correctly prevents "Venom: Lethal Protector" from winning via discriminative-corroboration — but the pool's fallback `weighted-consensus` path then selects a THIRD, unrelated, non-comic identity instead: "ariel diaz venom carnage," a 3-member cluster of "Ariel Diaz Artbook-Venom & Carnage" listings (a companion art-print/portfolio product, not a comic printing of the scanned item at all), outweighing the single real Mayhew Separation Anxiety row (3 members/weight 5.5 vs 1 member/weight 5.0 — a razor-thin margin). This defect was always latent in this exact pool; the pre-GK-121 Lethal Protector bug fired first and masked it completely, so it was never observed in production before this verification. Root cause, confirmed by source read: `isEligibleVisualRow` (`src/lib/identityReconciler.js`) only excludes lot/bundle/variation-group listings; `buildTitleFamilies`'s own `NON_GENUINE_COPY_RE` filter (`src/lib/compHygiene.js`, via `identityCore.js`) only excludes photocopy/USB/digital-archive/scan-disc listings — neither recognizes an "artbook"/companion-print/portfolio listing as ineligible for title-family clustering or weighted-consensus authority. Not fixed — explicitly deferred per the directive's own instruction not to fold a new finding into an acceptance-verification pass; needs its own scoping (a new eligibility exclusion pattern, likely alongside `NON_GENUINE_COPY_RE`, requires collecting more real examples before designing the regex to avoid over/under-matching). **SHIPPED-PENDING PHYSICAL (Directive AO, 2026-08-16).** Fixed: a third eligibility class, `COMPANION_PRODUCT_RE` (`src/lib/identityReconciler.js`, alongside `LOT_OR_BUNDLE_RE`/`VARIATION_GROUP_RE` inside the shared `isEligibleVisualRow`) — explicit tokens only (art book/artbook, art print, portfolio, sketchbook, poster), deliberately excludes bare "art" to avoid false-positiving on real comic titles ("Art Ops," "The Art of War," both verified still eligible). Single source of truth propagates to all 3 existing consumers (`selectFirstEligibleVisual`/F-1's freeze, `countCorroboratingEligibleRows`, `selectTitleFamilyCandidate`'s Rule 1/C1 family filter) with one change. Verified against the complete, verbatim wfvvb pool: idx 1/6/13 (the artbook rows) excluded, "ariel diaz venom carnage" no longer appears in family election at all, final result is `decision=fallback-vision, selectedTitle=null` — an honest contested/refused state, not a wrong confident winner (Mayhew's own family has weak token-overlap with Vision's bare "Venom" guess, so it doesn't auto-win either — this is a correct, safe outcome, not a further defect). dzq9h regression clean (zero companion-product exclusions, Dell'Otto family unchanged). Sabrina and NYCC/LTD/EXCLUSIVE-alone controls confirm no false exclusion. `tests/grailkey-directive-ao-companion-product-eligibility.test.js`, 19/19. Sibling-path finding, traced not fixed: `buildTitleFamilies`'s own separate pre-clustering filter (`NON_GENUINE_COPY_RE`, `compHygiene.js`) still does not recognize companion products — the artbook rows still get individually clustered into their own family internally, they just no longer survive `selectTitleFamilyCandidate`'s own post-clustering family-representative eligibility check. Verified sufficient for this defect (family-level filtering catches it regardless of pre-clustering awareness) but the two filters remain genuinely independent — not unified this dispatch, per its own minimal-scope instruction. | aliases:
GK-124 | SHIPPED-PENDING PHYSICAL | (pending) | 2026-08-16 | **Directive AP — fourth false-READY: cleared/unknown variant != base edition.** Production: Amazing Spider-Man #1, Dell'Otto virgin variant, CGC SS 9.6. Vision's "virgin variant" claim was correctly CLEARED by `reconcileVariantFacet` (authority=NONE, conflicts=[vision], AM/F-3 working as designed) — but a cleared variant and a NEVER-CLAIMED variant both collapsed to `out.variantApplicability===null` at the custody site (`api/enrich.js`), so `deriveMarketStanding` (`src/lib/actionAuthority.js`) read the null as a plain base edition and granted EXACT_CURRENT to a 4-comp pool with ZERO Dell'Otto comps (Kith variant / a variant lot / Crain SS / base edition) — `actionAuthority=READY`, List button live. Sibling to AB/GK-101 (fires on confirmed-variant + zero pool match) and AH/GK-111 (fires on consumed sold-fallback) — both require a confirmed variant to exist; clearing it walked under both. FIXED: `api/enrich.js` now reads the already-written `out.variantReconciliation` (authority=NONE + a recorded conflict = edition specificity was evidenced, never confirmed) and sets `out.variantApplicability='UNRESOLVED'` instead of null — custody, not recomputation. `deriveMarketStanding` floors 'UNRESOLVED' to SIMILAR_ONLY (same floor as AB's 'UNVERIFIED', new reason code VARIANT_UNRESOLVED_EDITION, `src/lib/responseContract.js`) — falls through Z's EXISTING state machine to REVIEW, no parallel denial path. Reconciler/F-3 untouched (C3); no pricing math touched (C2); server boundary (`api/list-ebay.js`) inherits the fix with zero code change (`item.variantApplicability \|\| null` passes 'UNRESOLVED' through unchanged). `tests/grailkey-directive-ap-variant-unresolved-authority.test.js`, 37/37 — includes a DIRECT (not mirrored) PRE-AP-vs-POST-AP proof using the real `reconcileVariantFacet` on the real production pool shape, base-edition no-over-fire (B2), cleared-vs-absent isolation (B3), all 4 monotonicity directions (B4) with a demonstrated real upward route (a genuinely corroborating first-eligible-visual row), AB/AH sibling fixtures unregressed (B5), and the server-side synthetic re-derivation boundary. SHIPPED-PENDING PHYSICAL per this registry's standing evidence bar — closure reserved for Jimmy's post-deploy rescan of the Dell'Otto ASM book (expect SIMILAR_ONLY/REVIEW/VARIANT_UNRESOLVED_EDITION, List button disabled) plus a Sabrina regression check (expect unchanged LIST_LOW/REVIEW). Clears the false-READY halt on deploy; does NOT itself close GK-121/GK-123/GK-120's family half (those remain HOLD on their own separate physical-scan evidence). | aliases:
GK-125 | OPEN | - | 2026-08-16 | Directive AP Task 1 trace (§1c), logged not fixed per the directive's own scope boundary. The winning title-family for the Dell'Otto ASM #1 production scan carried the discriminator in its own name (`"amazing spider man dell otto gabriele"`, weight 10.0, 9 members) but the canonical title collapsed to bare "Amazing Spider-Man" — the family resolver got the family right; canonicalization dropped the creator discriminator that would have produced a Dell'Otto-bearing eBay comp query. A secondary contributor to GK-124's production shape (a Dell'Otto-aware query might have surfaced real Dell'Otto comps instead of the generic 4-comp pool), but independent of and not required by GK-124's own fix — the authority-layer fix (GK-124) refuses the book regardless of whether the query is generic for this reason or any other. Not scoped, not fixed. | aliases:
GK-126 | OPEN | - | 2026-08-16 | Directive AP preflight, logged not fixed. `responseContract.js`'s `single-comp-pool` lock (AH/GK-111) requires a ≥2-comp floor to reach EXACT_CURRENT authority — structurally, a book whose entire eBay presence is a single listing can never independently form a "family" or clear this floor. Sibling gap to GK-123's companion-product eligibility class (both are "this population cannot legitimately establish authority" shapes) — distinct mechanism, not yet traced against a real production instance. Record only. | aliases:
GK-127 | SHIPPED-PENDING PHYSICAL | (pending) | 2026-08-16 | **Directive AQ — canonical facet authority, one-commit-boundary consolidation.** `reconcileIssue` (Slice 1, `src/lib/identityReconciler.js`) already correctly emitted `authority='CORROBORATED'` on Wolverine #90's real evidence (Vision #90, visual 12/16 #90, family 5/7 #90) — but a SEPARATE, older, parallel authority system (Commit 3/4/4.1/4.3, `src/lib/issueAuthority.js` + `api/enrich.js`, predates Slice 1) independently re-derived `out.issueAuthority` from `familyIssueConsensus`'s own `mode`/`outcome`/`authoritativeForCustody` flags — never from `reconciledIssue` — and overwrote CORROBORATED with `status='conflicted'`. **CORRECTED mid-build from the original trace's hypothesis**: `axisAgreement`'s internal-unanimity test (`identityCore.js` ~2793, requiring BOTH competing title-families to be internally 100% unanimous, not merely agree in plurality) is **not** the defect — a plurality-only rewrite was drafted, then reverted before push, after `tests/grailkey-dispatch-25-fix2c-axis-check.test.js` Section 5 ("P0 hole closed") proved it regresses a real, deliberately-designed safety property (a live dissenting row within either competing family must still flag, even when that family's plurality agrees — Fix 2c/Dispatch 25 already tried and explicitly rejected the plurality-only approach for this exact reason). Wolverine #90's bug needed no change to that test at all: the evidence-set feed inside `resolveIdentity` already adds the preserved prior as `'family-corroborated'` evidence whenever `familyIssueConsensus.mode==='conflict-locked'`, independent of WHY `axisAgreement` went false — so `reconcileIssue` was already computing the correct verdict; the bug was entirely in the separate legacy WRITE mechanisms. **FIXED, CORRECTED SCOPE CLAIM (same day)**: normal visual-resolution custody now projects `out.issueAuthority` once (`api/enrich.js`, immediately after `resolveIdentity` returns) as a pure projection of `identity.reconciledIssue` via `projectIssueAuthority` (`src/lib/issueAuthority.js`) — the commit4/commit4-rescue/commit4.3 blocks (writers 1-4) no longer derive or write it; `q140-terminal`'s `out.issueConsensusConflict` construction became a post-commit validator (`normalize(current)===normalize(family)` → same-value-agreement, never alters); the YEAR-axis retention-conflict branch (formerly `issueAuthority.js:613-624`, now orphaned/unreachable from the real pipeline) had its genuine `identityProvisionalFields:['year']` side effect preserved via a small targeted inline check in `api/enrich.js` that touches ONLY the year facet — zero path to issue authority (closes the "Wolverine Revenge" cross-facet shape). This is **improved custody, not completed single-writer custody** — the original "written EXACTLY ONCE" framing overclaimed: three separately-scoped exceptional mutation paths remain, each a genuine `out.issueAuthority = ...` write, tracked explicitly as Slice 2 work-order items (eventual destination: evidence → reconciler → projection, or a pre-reconciler entry path terminating in the same owner) — writer 5 (`escalateIssueAuthorityOnConflict`, fires on later-arriving pool data unavailable at initial commit, the sole mechanism for barcode/manual-identity/CGC-cert scans which never call `resolveIdentity` at all), writer 6 (manual-correction provenance, genuine new evidence per the directive's own ruling — "not a bypass write"), and writer 7 (`checkCrossPopulationPromotionGuard`, kept as an explicitly-reclassified defensive safety-net validator — already did real value comparison, logs `[commit4.3-validator] SAFETY-NET FIRED` distinctly if it ever actually fires; **a validator that executes `out.issueAuthority = ...` is a writer regardless of its name**). Operator-confirmed issue corrections now enter the evidence set at maximum weight (`source='user'`, added to `ISSUE_SOURCE_PRECEDENCE`'s top slot in `identityReconciler.js`, threaded via `resolveIdentity`'s new `opts.issueOperatorConfirmed`, scoped to the issue facet only). `tests/grailkey-directive-aq-canonical-facet-authority.test.js`, 31/31 — DIRECT PRE/POST proof using the real, still-existing `deriveIssueAuthorityFromAdoption` against the real Wolverine #90 fixture (built via real title-family clustering machinery, never hand-set weights), the Revenge cross-facet shape, operator-correction evidence weighting, and source-presence checks confirming exactly 4 justified `out.issueAuthority` writers remain. `tests/q-trackB-commit4.3-winning-family-authority.test.js` updated (CONTROL T6(c), 4 assertions rewritten to anchor on the new single-projection wiring instead of the removed `deriveIssueAuthorityFromAdoption` call site — same safety outcome, new mechanism); `q-trackB-commit4.3.1-retention-decline-fail-closed.test.js` and `grailkey-dispatch-25-fix2c-axis-check.test.js` needed NO changes (both test `resolveIdentity`'s own pure output, unaffected by the `api/enrich.js` wiring change) — both re-verified passing byte-identical. SHIPPED-PENDING PHYSICAL per this registry's standing evidence bar — closure reserved for Jimmy's post-deploy rescan of the Wolverine #90 book (expect CV/PC research to proceed, honest pricing or thin-pool REVIEW, never `ISSUE_AUTHORITY_CONFLICTED` on unanimous evidence) plus a Sabrina regression check. | aliases:
GK-128 | P0 SHIPPED-PENDING PHYSICAL | (pending) | 2026-08-16 | **CLOSED, code-complete — the fifth false-READY sibling (GK-96, GK-101, GK-111, GK-124, GK-128), FIXED.** Original entry's "not a live safety hole today" classification was wrong: `identity.familyIssueConsensus`'s own `outcome`/`reason` fields correctly report `'conflicted'`, but that signal was never read by anything on the transaction-authority path. **Boundary-test chain, direct end-to-end proof, real code, `assembleContract` called directly** on the real Batman-#213-class near-miss fixture (`tests/grailkey-dispatch-25-fix2c-axis-check.test.js` Section 5's own construction: top family plurality 213 unanimous, runner-up population 213/213/300, genuine live dissent = "300") — **PRE-FIX**: `reconcileIssue.authority=CORROBORATED` (the "300" dissent absent from `justifiedBy`/`conflicts`), `out.issueAuthority=null`, `identityStanding=CONFIRMED`, `marketStanding=EXACT_CURRENT`, `actionAuthority.state=READY`, `contract.listable=true`, CV/PC exact-lookup gates both `true`. **RULE INSTALLED**: every materially asserted issue value in an eligible conflicting family must reach the issue evidence set — a family's plurality winner is not a substitute for its dissenting evidence. **FIXED** (`src/lib/identityCore.js`, revocation-only): the near-miss branch now carries the runner-up title-family's own full `runnerUpAssertedIssues` set on `familyIssueConsensusResult`; at the evidence-set builder, every runner-up value that does NOT match the preserved/adopted value is fed into `issueEvidence` as a genuine `reportConflict` entry (provenance `'family-runnerup-dissent'`) — `reconcileIssue`'s own existing conflict logic then correctly computes CONTESTED, no new authority-derivation mechanism, only a missing evidence input restored. Deliberately scoped to the RUNNER-UP's own dissent only, never the top family's own internal minority (verified required, not a convenience: Wolverine #90's real shape has TOP-family dissent with a clean unanimous runner-up — feeding top-family dissent would resurrect GK-127 verbatim). **POST-FIX**: `reconcileIssue.authority=CONTESTED`, `conflicts=[{value:"300",source:"family-runnerup-dissent"}]`, issue candidate retained (`value="213"` unchanged), `out.issueAuthority.status='conflicted'`, CV/PC exact lookups both blocked, `marketStanding≠EXACT_CURRENT` (`NONE`), `actionAuthority≠READY` (`LOCKED`), `contract.listable=false`. Two rejected alternatives recorded: feeding the runner-up's own PLURALITY (winner) instead of dissent (wrong value, tried and reverted twice in this campaign); having `projectIssueAuthority` consume `familyIssueConsensus.outcome` directly (dishonest — Wolverine #90 also carries `outcome:'conflicted'` despite genuine agreement, would resurrect GK-127). **Item 2 (null-defaults-to-trusted audit)**: traced, no additional live gap found. Two candidate shapes checked — a lone `family-population` winner (already demoted by `projectIssueAuthority`'s own existing check) and a lone `first-eligible-visual` winner (already independently blocked via `identityProvisionalFromVisualFirst` → `out.identityProvisional=true` → `identityStanding=CONFLICTED`, a separate pre-AQ mechanism, verified by direct source read of both write and read sites) — both already safe. `tests/grailkey-directive-aq-followup-gk128-evidence-completeness.test.js`, 23/23 (PRE-FIX mirrored/historical vs POST-FIX DIRECT, full chain, plus the Wolverine #90 no-self-conflict-regression control); `tests/grailkey-directive-aq-canonical-facet-authority.test.js` B1b updated to assert the fixed behavior (32/32). Controls re-verified byte-identical: `q140-issue-consensus-corrective.test.js` (124/124, Flash #139 unaffected), `grailkey-dispatch-25-fix2c-axis-check.test.js` (60/60, Section 5's own safety property intact), `q-trackB-commit4.3.1-retention-decline-fail-closed.test.js` (73/73), `q-trackB-commit4.3-winning-family-authority.test.js` (263/263), `grailkey-directive-ap-variant-unresolved-authority.test.js` (37/37). **P0 SHIPPED-PENDING PHYSICAL** — closure reserved for Jimmy's post-deploy rescan of a genuine near-miss book (the Batman #213/#300-class shape, if reproducible, or any book whose runner-up title-family genuinely disagrees on issue) confirming LOCKED/not-listable in production, alongside the Wolverine #90 (GK-127) and Dell'Otto ASM #1 (GK-124/AP) rescans. Halt remains active until all three physical rescans land. | aliases:
GK-129 | SHIPPED-PENDING PHYSICAL | 89fa9ae | 2026-08-17 | **Directive AR Part B — sixth false-READY: CONTESTED cannot price EXACT.** Production: Venom Separation Anxiety #1, Mike Mayhew — `reconcileVariant` (`src/lib/identityReconciler.js`) correctly computed `authority=CONTESTED` (an independent source disagreed with the adopted value), but `api/enrich.js`'s custody of `out.variantApplicability` only distinguished UNVERIFIED/UNRESOLVED/null — CONTESTED fell through to whatever `api/comps.js` Filter 1c said about the disputed value (which had genuinely matched comps, CONFIRMED) — `actionAuthority=READY`, $48.86, List button live, while the card's own copy read "SIMILAR listings, not exact matches." FIXED: `api/enrich.js` now custodies `out.variantReconciliation.authority==='CONTESTED'` into `out.variantApplicability='CONTESTED'` (placed above the Filter 1c read, so CONTESTED overrides even a CONFIRMED pool match — a pool matching a disputed guess is not evidence the guess is right); `src/lib/actionAuthority.js`'s `deriveMarketStanding` floors it to SIMILAR_ONLY (same principle as AB/AP); own reason code `VARIANT_CONTESTED_EDITION` (`src/lib/responseContract.js`). Server boundary (`api/list-ebay.js`) inherits the fix with zero code change. `tests/grailkey-directive-ar-evidence-authority.test.js`, 42/42 (shared with GK-130) — DIRECT PRE/POST proof on the real production shape, corroborated-variant upward route (B-B2), operator-correction escape hatch (B-B3), AP-sibling mutual exclusivity (B-B4), no-new-READY sweep (B-B5), server re-derivation boundary, and a combined Part A + Part B integration fixture. SHIPPED-PENDING PHYSICAL per this registry's standing evidence bar — closure reserved for Jimmy's post-deploy rescan of the Venom Mayhew book. | aliases:
GK-130 | SHIPPED-PENDING PHYSICAL | 89fa9ae | 2026-08-17 | **Directive AR Part A — the pattern-list veto.** Production: Absolute Batman #19, Ben Oliver Variant Cover, 1st Scarecrow, DC 2026 — the physical book's cover artist is unregistered in `ARTIST_PATTERNS` (`src/lib/compHygiene.js`), so `extractPoolArtistTokens` never populated `poolArtistTokens` with 'ben'/'oliver' regardless of independent pool support — `applyDualAxisGate` (`src/lib/imageSearchIdentity.js`) vetoed a correctly-identified, 10-member family purely on registry absence (`[Q84] override-blocked reason=non-creator additions [ben,oliver]`), pricing the book from the generic pool. FIXED: `findPhysicallyCorroboratedTokens` grants a non-registry token the same standing a registered creator gets from `poolArtistTokens` when (a) it is physically present on the frozen rank-1 eligible visual row (`identityReconciler.js`'s `selectFirstEligibleVisual`, reused byte-for-byte — Directive AN's own discipline) AND (b) at least 3 independent (unique-seller, `issueAuthority.js`'s `checkDistinctItemIdAndSeller`, reused not reinvented) family members also name it — new provenance `creator-lane-physical-corroboration`, checked before the narrower registry-adjacency recovery chain, which stays untouched for partial-corroboration cases. `q84Gate`/`applyDualAxisGate` signatures extended with two new, defaulted-null trailing params (`familyIndices`, `items`) — both call sites (top-rank-protection, weighted-consensus) updated; every pre-existing call shape (old positional arg count) is unaffected. `tests/grailkey-directive-ar-evidence-authority.test.js`, 42/42 (shared with GK-129) — DIRECT PRE/POST proof on the real production shape (both the isolated gate and full `selectTitleFamilyCandidate` end-to-end), three negative controls (below-floor independent-seller count, token present but absent from the frozen row, same-seller relist anti-injection), and the registered-creator (Mayhew) path confirmed structurally untouched. Pre-existing suites re-run byte-identical: `q84-dual-axis` (17/17), `grailkey-directive-af-discriminative-corroboration` (25/25), `grailkey-directive-ag-22e-provenance-exemption` (32/32), `grailkey-directive-an-physical-corroboration` (11/11), `grailkey-directive-ao-companion-product-eligibility` (19/19), `grailkey-directive-ap-variant-unresolved-authority` (37/37), `grailkey-directive-aq-canonical-facet-authority` (32/32), `q140-issue-consensus-corrective` (124/124). SHIPPED-PENDING PHYSICAL per this registry's standing evidence bar — closure reserved for Jimmy's post-deploy rescan of the Absolute Batman #19 book. | aliases:
GK-131 | OPEN | - | 2026-08-17 | Directive AR preflight, logged not fixed per the directive's own non-goal (pricing math, needs its own greenlight). "1st <character>" key-issue signal present in the Absolute Batman #19 rank-1 row and comp titles (1st Scarecrow) but `keyIssue=null`/multiplier=1 — the existing `extractKeyFromComps`/Ship #12a key-detection machinery (display-only per CLAUDE.md's Multi-key extraction section) apparently did not fire or was not consulted for this title. Not traced further this dispatch — report-only. | aliases:
GK-132 | SHIPPED-PENDING PHYSICAL | 694b7a5 | 2026-08-17 | **Directive AS — "the candidate always enters."** Production: Venom Separation Anxiety #1, Mike Mayhew, forced `ID_REQUIRED` despite the frozen rank-1 row naming the exact physical book/issue/creator — two independent gates in `src/lib/identityCore.js`'s unconditional issue-evidence builder (Directive AJ Proof 1) were still blocking entry, not merely consensus override: (1) `MINIMUM_CORROBORATING_ROWS`/`countCorroboratingEligibleRows` (`identityReconciler.js`) gated ENTRY into the evidence set, and the Venom pool had only 1 genuinely corroborating eligible row (all others ineligible companion-product/Crain-token/no-issue-number rows); (2) `hasContaminatedMember` (`compHygiene.js`, SIGNED_RE match) flagged the SOLE member of a 1-member family as "contaminated" — a category error, since mixture is structurally impossible at n=1. FIXED: the `corroboratingRows >= MINIMUM_CORROBORATING_ROWS` condition removed from entry (kept computed, diagnostic-only, threaded into `visionZeroSupport.corroboratingRows`, I13); the family-contamination guard now requires `>=2` members before running the full `hasContaminatedMember` check (a lone member can never be a mixture) — the shared function itself and its other 2 call sites are untouched. Three NEW, narrower guards found necessary by running the FULL existing regression suite after each change (not merely the fixtures the directive named) and iteratively refined against real regressions each one caused: (a) an own-row `REPRINT_RE`/`IDENTITY_TPB_MARKER_RE` check on the candidate's OWN row (`tests/q-vision-zero-support.test.js`'s pre-existing "True Believers" facsimile-renumbering control, Test 7 — a reprint routinely relists as "#1" regardless of the real issue); (b) a deference guard for a GENUINE prior `resolveFamilyIssueConsensus` 'no-consensus' verdict, scoped narrowly to `family.decision` NOT in `FAMILY_OVERRIDE_DECISIONS` (a real title win) AND `assertedIssues.length > 0` (evidence was actually seen, not merely uncapturable) — required to avoid regressing Eternus #2 (`tests/q131-refused-identity-conflict-provisional.test.js`, Q140 corrective dispatch precedent: 2 unique rows below the family's own ≥3-row floor even at 100% agreement must stay null), AK's population-precedence fixture (`tests/grailkey-directive-ak-population-precedence.test.js`: a bare family-population vote must still be demotable by a specific candidate via reconciler precedence, not pre-blocked), CONTROL 3 (`tests/q-trackB-commit4.3-winning-family-authority.test.js`: a qualified-but-inconclusive retention-branch verdict), and — found via the REAL `/api/enrich` HTTP handler, not the hand-built unit fixture (`tests/grailkey-directive-aj-http-handler.test.js`, the same "AG lesson" this campaign keeps re-learning) — Detective Comics #1107 itself, where `resolveFamilyIssueConsensus`'s OLDER, 999-issue-cap extractor genuinely cannot see "#1107" at all (`assertedIssues=[]`, a false-negative 'no-consensus', not a considered refusal — the exact GK-116 gap this whole mechanism exists to route around); (c) a pool-WIDE (not just winning-family) Vision-title-token-overlap check, required to distinguish a real-but-refused family that is STILL genuinely about the right book (Detective: "Batman" appears elsewhere in the same raw pool even though not in the winning "Detective Comics" cluster) from one that shares NOTHING with Vision's read anywhere (`tests/q-trackB-commit4.3-winning-family-authority.test.js`'s own CONTROL E: "Quux Anthology #9" pool vs Vision title "Something Else Entirely" — zero relationship at all, adopting its issue number risks pricing a completely different book). `tests/grailkey-directive-as-candidate-always-enters.test.js`, 31/31 — DIRECT proof on the real production shape, Detective/AI-Fixture-4 regressions unregressed, a genuine no-candidate control (ID_REQUIRED survives), the title-facet floor (Q38, imageSearchIdentity.js) confirmed untouched, and no-listing-unlock derivation chains through `actionAuthority`. Full 217-file suite re-run clean, byte-identical FAIL/TIMEOUT list to the prior baseline. **Registry correction**: this directive's own preflight described GK-126 as covering this exact defect ("≥3-member floor discards a 1-member correct family") — that is FALSE; GK-126 is an already-registered, DIFFERENT ticket (`responseContract.js`'s `single-comp-pool` pricing floor, Directive AP). This fix is filed under GK-132 only; GK-126 is untouched by this dispatch and remains open as originally scoped. SHIPPED-PENDING PHYSICAL per this registry's standing evidence bar — closure reserved for Jimmy's post-deploy rescan of the Venom Mayhew book. | aliases:
GK-133 | SHIPPED-PENDING PHYSICAL | 4379faf | 2026-08-20 | **Directive AV Part A — "the last facet."** Was OPEN/report-only since Directive AS (2026-08-17): `confirmedTitle` stayed Vision's own raw "Venom" even after `confirmedIssue` was correctly rescued to "1" (GK-132) — Q38's title-family consensus-override floor (`imageSearchIdentity.js`) refused the 1-member "Separation Anxiety" cluster as a TITLE adoption, and no mechanism promoted the frozen rank-1 row's own candidate into `confirmedTitle` when family election refused. FIXED: new `reconcileTitleFacet`/`reconcileTitle` (`identityCore.js`/`identityReconciler.js`, mirrors `reconcileIssue`) adopt `family.topFamily.title` (the family-clustering pipeline's own token-consensus-cleaned candidate, verified NOT `.rawTitle` — that field is unstripped seller boilerplate) as CONTESTED evidence when `family.decision==='fallback-vision'` and nothing else already assigned a real title. Explicitly excludes `family.titleAxisOnlyBlock===true` (the Q84-dual-axis-blocked shape — a qualified family deliberately trimmed of non-consensus content) — found as a real regression against `tests/q140-coherent-content-token-lane.test.js` mid-build, fixed before shipping. `out.titleAuthority==='CONTESTED'` floors `deriveMarketStanding` to SIMILAR_ONLY (`TITLE_CONTESTED` reason code), AR/AT's per-facet law extended to the fourth and last facet. Both `22e` integrity checks needed an added OR-guard (`identityTitleAdoptedContested`) to stop reverting the adoption before PC/CV ever saw it. `tests/grailkey-directive-av-handler-smoke.test.js` (28/28) proves the real Venom production shape end-to-end via the actual `/api/enrich` handler: title reaches PC/CV, price computed, never ID_REQUIRED. SHIPPED-PENDING PHYSICAL per this registry's standing evidence bar — closure reserved for Jimmy's post-deploy rescan of the Venom Mayhew book. | aliases:
GK-139 | SHIPPED-PENDING PHYSICAL | 4379faf | 2026-08-20 | **Directive AV Part B — "the floor that believed a ghost."** New finding, same dispatch as GK-133. `getMegaKeyEntry`/`passesIdentityGates` (`api/mega-keys.js`) are pure title+issue+publisher+year VALUE/tolerance matching — zero awareness of whether that value was ever corroborated. Real production shape: a Dell'Otto Amazing Spider-Man #1 (a modern virgin variant, comp pool averaging ~$30) matched the `"amazing spider man|1"` 1963 grail entry ($300,000 at grade 9.4) despite its own year facet being CONTESTED (AU/GK-137's single-source-ceiling — a lone PriceCharting catalog match) and its own variant facet being CONTESTED (AU/GK-136 — a modern virgin variant, not the 1963 original). Safety held downstream (LOCKED/DO_NOT_LIST) but the FLOOR NUMBER itself was a fabrication the card's own evidence directly contradicted. FIXED: new `isMegaKeyIdentityCorroborated` (`api/mega-keys.js`) gates the floor's two existing firing sites (`api/enrich.js ~8930/~9415`) on three conditions — title identity corroborated (`isCorroboratedIdentitySource`, or an independently-authoritative manual/barcode/cgc_cert path), `yearAuthority!=='CONTESTED'`, `variantApplicability!=='CONTESTED'`. `MEGA_KEYS_FLOOR`/`getMegaKeyEntry`/`passesIdentityGates` themselves are UNTOUCHED — this is a firing-condition gate, not a table change. On failure the floor stands down (`out.megaKeyIdentityUnresolved`/`megaKeyIdentityUnresolvedName` advisory retained, honest pre-floor price shown, `[mega-key-floor] STOOD DOWN` logged) — never silently. `tests/grailkey-directive-av-handler-smoke.test.js` proves both directions through the real handler: the Dell'Otto ghmn7 pool (reused verbatim from `grailkey-directive-au-handler-smoke.test.js`) now prices at $6,661.46, never $300,000; a genuine corroborated 1963 ASM #1 fixture (manual identity, CORROBORATED year, no variant claim) still floors at $300,000, full force — the C5 negative control this fix must never break. SHIPPED-PENDING PHYSICAL — closure reserved for Jimmy's post-deploy rescan of the Dell'Otto book (expect a sane price + advisory chip, no $300K ghost) alongside a genuine vintage-key control if one is available. | aliases:
GK-140 | SHIPPED-PENDING PHYSICAL | a185425 | 2026-08-20 | **Directive AW — "the adopted title asks a clean question."** AV (GK-133) made the rank-1 title candidate win VERBATIM in the fallback-vision void — production showed the cost: raw seller text ("Venom Separation Anxiety By Mike Mayhew Poker Chip") over-narrowed the PC/CV/comps queries built from it (no catalog product matches a seller's flourish) and polluted the card display. FIXED: new `canonicalizeTitleCandidate` (`identityCore.js`) is strip-only (never adds/guesses/substitutes) — attribution clause (confirmed via `matchCreatorCanonicals`, the SAME registry the variant facet already uses — C2's "title drops what variant keeps" follows directly), merch/w-clause, parenthetical, grade/condition (SLAB_RE + two narrow additive bare-condition/bare-decimal patterns), generic finish/descriptor words (VARIANT_CONTAM_RE, reused verbatim), a standalone duplicate issue number, a trailing bare publisher+year suffix. Wired into `reconcileTitleFacet` against `topFamily.rawTitle` (NOT `.title` — buildTitleFamilies' own weaker cleaning had already destroyed the structural markers this needs, which is exactly why "by mike mayhew poker chip" survived AV's own adoption: tokenizeTitleFamily's ARTIST_PATTERNS strip has no "Mayhew" entry). Verbatim row preserved forever in `justifiedBy[].verbatim` (C3, `identityReconciler.js`'s `reconcileTitle`). Authority/C4 unchanged (still CONTESTED); cooperative election paths (weighted-consensus/discriminative-corroboration) never reach this function at all (C5, proven byte-identical). Two real bugs found and fixed mid-build, neither named in the directive: a TDZ circular-import hazard (`new RegExp(SLAB_RE.source,...)` at identityCore.js's own module top level depended on compHygiene.js's init order, throwing `ReferenceError` for some test entry points and not others) and a global-regex `lastIndex` statefulness bug (a shared `g`-flagged singleton's `.test()` call mutates state a later call on a different string could silently trip over) — both closed by a shared `applyStrip` helper that builds a fresh RegExp per call and uses `.match()` instead of `.test()`+`.match()`. `tests/grailkey-directive-aw-title-canonicalization.test.js` (24/24) + `tests/grailkey-directive-aw-handler-smoke.test.js` (16/16, real handler — the clean query reaches a real 8-item comps pool where the pre-fix noisy query matched nothing; PriceCharting's own product lookup not firing at all in this fixture, and `api/comps.js`'s own 4 post-filter survivors not reaching final `out.rawComps`, are both diagnosed-directly, pre-existing, out-of-scope gaps — C7 excludes pricing-math plumbing — not asserted on). SHIPPED-PENDING PHYSICAL — closure reserved for Jimmy's post-deploy rescan of the Venom poker-chip photo (card should read "Venom Separation Anxiety") and Detective #1107. | aliases:
GK-141 | OPEN | - | 2026-08-20 | **Phase 0.3 (2026-08-21) trace: premise overturned, NOT a wiring gap — no fix attempted this pass.** Full trace of the handoff chain `fetchComps() (api/comps.js:998) → compsPromise (enrich.js:6454) → compsFromEbay (destructured :6702) → rawComps (:6775) → out.rawComps (:11157-11180)` found every step correctly shaped — no wrong variable, no dropped field, no shape mismatch. `out.rawComps.count` is 0 if and only if `fetchComps` itself already returned `count: 0`. The 4 survivors are discarded INSIDE `fetchComps`, after the `[comps] attempt N post-filter=4` log line (`api/comps.js:2102`), by one of two later-stage gates: (1) an issue-verification gate (`:2114-2128` — if no survivor's title contains `#<issue>` on the last attempt, `parsed=[]`), or (2) the D1 evidence-eligibility reclassifier (`:2304-2399`, `buildEvidencePopulations`/`buildPricingEligibleRows`). Both are comp-pool composition/eligibility-filter logic, not plumbing — a real fix here requires touching filter logic, which T1-b's own greenlight explicitly did NOT authorize ("no filter changes"). Per this repo's standing pricing-math-greenlight protocol, that precondition isn't met by what the trace found, so no fix was implemented; this needs its own dedicated greenlight naming the filter-logic change explicitly. **Same-as-GK-142? No — independent.** GK-142's mechanism is the QUERY never finding enough in the first place (over-narrow search string); GK-141's is the search already succeeding and a later stage discarding the results anyway — different stage, different code, different fix class. Do not merge. **Second mechanism (PriceCharting never firing) not re-verified this pass** — 4 call sites found (`enrich.js:4163,4200,4672,7212`) but the specific gating condition for the fixture's path was not traced; flagged, not claimed. A dedicated diagnostic handler-smoke run against `api/enrich.js` (fixture: `title='Venom', issue='28'`, a thin fallback-vision-triggering image-search pool paired with a SEPARATE, richer 8-item eBay comps-search pool for the clean canonicalized "Venom Separation Anxiety" query, PriceCharting stubbed at `pricecharting.com/api/products` to return a real product "Venom: Separation Anxiety #1 (2024)") surfaced two independent, unexplained mechanisms, neither introduced by AV or AW: (1) `lookupPriceCharting` never fires at all for this fixture — confirmed by grepping the full fetch log for the run: zero requests to `pricecharting.com/api/products`, despite a stub configured and ready to answer one; (2) `api/comps.js`'s own eBay text-search DID fire correctly against the clean title and returned `itemSummaries=8` with `4` post-filter survivors (`[comps] browse itemSummaries=8` / `[comps] attempt 2 post-filter=4` in the captured log), yet the FINAL response still showed `rawComps:0`, `pricingSource:'refused-no-data-sources'`, `yearAuthority:'NONE'`, `decision.action:'DO_NOT_LIST'` — meaning comps.js's own successful 4-survivor result never reaches `out.rawComps` at all. Both mechanisms are real, observed, reproducible against the fixture described above — not assumed or inferred. AW's own dispatch deliberately did not chase either: fixing them would mean touching pricing-derivation wiring this repo's standing pricing-math-greenlight protocol requires explicit instruction for, and neither is a title-cruft problem (the query itself is already clean — see GK-140). Needs its own dedicated trace-then-build dispatch with explicit greenlight before any code changes. | aliases:
GK-142 | CLOSED | 081cd3b | 2026-08-21 | **CLOSED (2026-08-22): physical closure criterion met — Detective Comics #1107 rescan, 2026-08-22 03:06, production build `551db6b`. Comps non-empty via `deriveSeriesCoreQuery` (2 live comps, the book's own market), priced $10.00 — the prior scans' empty-pool/tier-4-RESEARCH state (r5v6b, $8.42-vs-nothing) is resolved.** **Phase 0.3 — "the series-core question."** BUILD-NOW verdict, implemented same pass. Trace found 3 query-construction sites, each with different/no cleaning: PC (`enrich.js:4163`, raw `confirmedTitle`), ComicVine (`enrich.js:3974`, `cleanTitleForComicVine` — its own bespoke, hand-grown noise lists, no creator-registry awareness), comps (`enrich.js:6607` → `api/comps.js:1073`, `cleanTitleForSearch` — punctuation-only). `canonicalizeTitleCandidate` (GK-140/AW) already ran but couldn't catch this row: `stripAttributionClause` requires the literal word "by" (absent here — confirms GK-142's own hypothesis), and its only descriptor strip (`VARIANT_CONTAM_RE`) has no "corner"/"box" entry. FIXED: new `deriveSeriesCoreQuery(confirmedTitle, confirmedIssue)` (`src/lib/identityCore.js`) — a query-ONLY projection (A5: never touches display/adopted identity) built on `canonicalizeTitleCandidate`'s existing pipeline plus two additions: (1) an unconditional `matchCreatorCanonicals`-driven creator-name strip, position-mapped through the source text via NFD-decompose + combining-mark strip (`\p{Mn}`) so a canonical registry form without an accent (e.g. "Jorge Jimenez") can still locate and remove the accented form actually in the text ("Jorge Jiménez") — length-guarded (skips the strip, safe no-op, if the folded/original lengths ever diverge) rather than guessing at an offset, the same discipline GK-143 (this dispatch) established; (2) `NO_PREMIUM_COVER_DESCRIPTORS` (`compHygiene.js`) — a separate literal, not an import, from `api/enrich.js`'s own pricing-math `NO_PREMIUM` array (untouched, never modified — the pricing-math-greenlight protocol still applies to that file's actual pricing gate; centralizing into one shared, imported constant was considered and declined for the same reason — it would require editing that protected declaration site, even for a byte-identical hoist). Drift between the two lists is caught mechanically, not by convention: `tests/grailkey-gk142-no-premium-parity.test.js` reads `api/enrich.js`'s actual source text, extracts the live `NO_PREMIUM` array, and fails the moment it diverges from `NO_PREMIUM_COVER_DESCRIPTORS`; this is a separate, query-only vocabulary that happens to share the same already-vetted token list). Same C6-style over-strip guard as `canonicalizeTitleCandidate`: empties or collapses to a stopword → falls back to the un-projected input verbatim (A5 mandatory). A real ordering bug found and fixed mid-build: an early draft stripped the creator name BEFORE `canonicalizeTitleCandidate`'s own attribution-clause detection ran, which broke that detection's own `matchCreatorCanonicals(clause)` gate and left orphaned "By ... Poker Chip" text behind on the AW/Venom production shape — reordered (canonicalize first, catch-all creator strip second) and reverified against that exact case. Wired at all 3 call sites: PC gains a third fallback tier (only engaged after the existing full-title and subtitle-stripped tiers both return zero — no currently-working case's query changes; no durable KV write for this tier, a new cache-key scheme is a separate undecided question); ComicVine's input swapped from raw `confirmedTitle` to `seriesCoreQuery` (complementary to, not replacing, `cleanTitleForComicVine`'s own bespoke cleaning); comps' `title` param swapped outright (zero behavior change for the common case, since `seriesCoreQuery === confirmedTitle` whenever nothing needed stripping). `tests/grailkey-gk142-series-core-query.test.js` (24/24, including the r5v6b repro, the AW/Venom non-regression check, the A5 fallback guard, and proof every `NO_PREMIUM_COVER_DESCRIPTORS` token actually strips) + `tests/grailkey-gk142-series-core-query-handler-smoke.test.js` (12/12, GK-138 real handler proof — the r5v6b Detective fixture: display title KEEPS "Corner Box Jorge Jiménez" in full (A5 verified end-to-end, not just at the unit level), the comps query drops both and reaches a real 8-item pool where the pre-fix query would have matched nothing). CLOSED 2026-08-22 — the Detective Comics #1107 rescan cited at the top of this entry satisfies the standing evidence bar. Prior trace, retained for history: **Trace-first, report-only — no query-construction patch, no registry addition, this pass.** Production request `r5v6b` (2026-08-20 22:59:26 UTC, log export 2026-08-20T23-02-15, build `938dfdb`): `[reconcile-title] value="Detective Comics Batman Corner Box Jorge Jiménez"` — the AW/GK-140 canonicalizer's attribution-clause stripper requires the literal word "by" before a recognized creator name (`stripAttributionClause`, `identityCore.js`); this row's raw text apparently names the creator WITHOUT that marker, so neither the creator name nor the "Corner Box" cover-descriptor gets stripped. Downstream: `soldPool=0 activePool=0 · tier-4 · RESEARCH` — the SAME physical book priced `$8.42` in a prior session from a cleaner query/pool. **The adopted title can retain variant/cover-class descriptors which are valid identity evidence but are not part of the series-core query, causing downstream catalog/comps searches to become over-constrained.** Candidate fix shapes for later investigation only (neither decided nor built): (a) determine whether "Corner Box" belongs in the same canonical descriptor taxonomy `VARIANT_CONTAM_RE` already recognizes, extending the canonicalizer's finish-descriptor strip to cover it; (b) separate canonical DISPLAY identity from a narrower `seriesCoreQuery` used specifically for PC/CV/comps, so a retained descriptor pollutes the query without needing to be stripped from the display title at all — note this shape also addresses GK-141's class (a clean-vs-unclean-query distinction) and should be traced together with it, not separately. DO NOT add "Corner Box" to any registry now; DO NOT patch query construction now. | aliases:
GK-143 | CLOSED | 081cd3b | 2026-08-21 | **CLOSED (2026-08-22): physical closure criterion met — Detective Comics #1107 rescan, 2026-08-22 03:06, production build `551db6b`. "Jorge Jiménez" renders intact on both the card and the comps query — no corruption, not "Jorge énez."** **Phase 0.3 — "jim inside Jiménez."** Reproduced with certainty, fixed same pass. Root cause: `sanitizeSeriesTitle`'s `NOISE_PATTERNS[0]` (`src/lib/identityCore.js:226`) is built from `LEGACY_CREATOR_NOISE_WORDS` (includes bare `'jim'`, from "Jim Lee" — deliberate by the list's own design, mixing bare first names with surnames) using a plain `\b(...)\b` regex. JS's `\b` is defined relative to the ASCII-only `\w` class; `é` (U+00E9) is not `\w`, so the m→é transition in "Jiménez" reads as a legitimate word BOUNDARY and "jim" false-positive-matches inside "Jim|énez," stripping to "...Jorge énez" — byte-identical to the production log. Nothing to do with charset/encoding (the GCD MySQL utf8mb3/utf8mb4 finding, `docs/DATA-0B-1-SCHEMA-RECON.md`, is a wholly different data source and pipeline — explicitly ruled out per this dispatch's own A4 framing). Confirmed via direct execution of the real, unmodified function — not approximated. Full enumeration of every transformation between `[reconcile-title]` (`identityCore.js:2663`) and the confirmed-identity write (`enrich.js:4491`) traced and classified per A4 (regex-sensitive/substring-sensitive/Unicode-preserving/etc.); every OTHER touch point (22e integrity checks, `sanitizeComicTitle`'s 9 regex passes, co-title append, `cleanTitleForComicVine`, subtitle strip) individually ruled out — this is the sole culprit. FIXED: `NOISE_PATTERNS[0]`'s boundary rebuilt with Unicode-aware lookarounds — `(?<![\p{L}\p{N}_])(...)(?![\p{L}\p{N}_])` with the `u` flag — so any Unicode letter counts as a word character; "m"→"é" is no longer a boundary, closing the false match, while genuine bare-word matches ("Jim Lee," surrounded by whitespace) are unaffected. Narrowest fix at the ONE proven site — the other 6 `NOISE_PATTERNS` entries carry the same latent ASCII-`\b` risk class but are explicitly NOT touched (none proven to have fired in production — flagged, not fixed, same discipline as every other "found the sibling risk, stayed in scope" note in this registry). `tests/grailkey-gk143-title-corruption.test.js`, 14/14 — the exact production string byte-for-byte, the é/ü/ñ/' round-trip control corpus A4 specified (José García, Müller, O'Neil straight/curly, Dell'Otto, Peña — all unaffected), a negative control proving genuine "Jim Lee" bare-word stripping still fires (fix does not over-suppress), and a second noise word ("alan" inside "Alanís") proving the fix is the boundary mechanism itself, not a one-off carve-out for "jim." Verified genuinely non-vacuous: reverting only this file and re-running the test produces exactly 5 of 14 failures (the corruption-specific assertions), 9 unaffected controls still pass — confirmed via `git stash`, not assumed. CLOSED 2026-08-22 — the Detective Comics #1107 rescan cited at the top of this entry (card reads "Jorge Jiménez," not "Jorge énez") satisfies the standing evidence bar. Prior trace, retained for history: **Trace-first, report-only — no fix, hypotheses only, this pass.** Same production request `r5v6b`/build `938dfdb` as GK-142. Two log points, same field: `[reconcile-title] value="Detective Comics Batman Corner Box Jorge Jiménez"` (creator name intact, correct UTF-8) versus the later confirmed-identity write, `confirmed="Detective Comics Batman Corner Box Jorge énez"` — "Jorge Jim" (9 characters) is missing from the multibyte creator string, corrupted somewhere between the two log points. **A valid multibyte creator string is intact at reconcile-title and corrupted later, before/during the confirmed identity write.** Recorded precisely, not overclaimed: the corruption window is PROVEN to be downstream of reconcile-title; it is NOT YET proven which specific operation caused it. Regex slicing, character-count/byte-count handling, string normalization (NFC/NFD), substring/window logic, and serialization are HYPOTHESES ONLY, not findings — none has been traced or confirmed against the real code path yet. Ticket requires a future trace of every title transformation between the two log points (candidate touch points, given this session's own work: the 22e/22e-LOSS integrity checks, q141-a canonical projection, any subsequent `writeConfirmed` call between title adoption and the terminal identity write) before any fix is proposed. No fix now. | aliases:
GK-144 | OPEN | - | 2026-08-20 | **Summit Phase 1 (2026-08-22): OPEN, governed by `docs/adr/ADR-ASYNC-001-async-execution.md` — ADR adoption does not close this ticket (amendment A2).** The ADR resolves the architectural question (outbox → queue → worker → DLQ, `waitUntil` as a latency optimization only, never a correctness mechanism) but building the outbox/worker mechanism is separate, not-yet-done implementation work. **Phase 0.3 (2026-08-21) verification: AT-LEAST-ONE-FAIL — stays synchronous, not implemented.** Per A6's mandatory gate, all 4 questions checked with evidence: Q1 delivery semantics FAILS — `@vercel/functions` is not even a project dependency (absent from `package.json` and `node_modules`), so `waitUntil`'s behavior on this deployment cannot be verified, only assumed. Q2 failure observability FAILS (alone sufficient to sink the gate per A6) — today's `console.warn` (`api/kv-cache.js:89,118`) fires reliably because the write is awaited before the response; moved inside a `waitUntil` callback, that same log would only fire if the callback runs to completion, and no independent out-of-process signal exists to distinguish "succeeded silently" from "cut off mid-write, never logged." Q3 runtime lifecycle FAILS — no `runtime`/`config` declaration anywhere in `api/enrich.js` (grepped, zero hits), only `vercel.json`'s `maxDuration: 120`; Node.js serverless confirmed, but nothing in-repo confirms post-response execution survival under cold-start teardown for this function specifically. Q4 scope PASSES trivially (confirmed: only the existing `kvSet(scanLogKey,...)`/`kvZAdd(...)` pair near `enrich.js:12022-12023`; no DATA-0F shared machinery considered). **A slow honest write beats a fast lost one — scanlog stays synchronous.** Re-open only if `@vercel/functions` is added as a real dependency and Q2's observability gap is separately closed. Prior trace, retained for history: **Trace-first, report-only — no implementation, this pass.** From the DATA-0 pre-flight Q5 finding (2026-08-20): the existing scanlog persistence (`api/enrich.js`, ~line 11995, `await kvSet(scanLogKey, scanLogRecord, ...)` / `await kvZAdd(...)`) runs ON the customer-response path — awaited before the handler responds — even though its own failure handling is already non-fatal (wrapped in try/catch, logs a warning, never throws into the real response). **Existing telemetry persistence is on the customer-response path even though failure is already non-fatal. Investigate moving scanlog persistence to post-response execution together with DATA-0 shadow telemetry.** Candidate mechanism: `waitUntil()` (`@vercel/functions`, Fluid Compute's post-response background-execution primitive). Do NOT change the existing write in this ticket-opening pass. Before any implementation, the future trace must verify: delivery/persistence semantics of `waitUntil` work specifically on this project's current Vercel plan/runtime; failure observability once the write moves off the synchronous response path (a silently-dropped background write is a worse failure mode than today's awaited-but-logged one); whether post-response execution reliably survives the relevant Vercel function lifecycle (cold start teardown, concurrent invocation reuse under Fluid Compute); and whether scanlog and the new DATA-0 shadow-lookup record (see DATA-0 pre-flight Q5) should share one post-response task or remain isolated writes — do NOT automatically combine the two implementations merely because both are background work; that is a decision for the future trace, not assumed here. | aliases:
GK-134 | OPEN | - | 2026-08-08 | **Backfilled 2026-08-17 (CLAUDE.md compaction audit).** GrailKey Dispatch 32's own STEP A report (commit `13879b4`) named an 8th defect — "Multi-book lot titles confirmed to have no identity-level detection anywhere in the pipeline — logged, unscoped" — that lived ONLY in CLAUDE.md's own Dispatch-32 bullet, was never given a GK number, and was never written into `docs/PATTERN-LIBRARY.md` at all (confirmed by direct grep across both files before backfilling this row — zero hits for "lot titles"/"multi-book" outside this line and the git history of the original commit). The 2026-08-13 CLAUDE.md compaction (`8d8af44`) then further summarized the Dispatch 32 entry down to a version that dropped this specific clause — the exact failure shape this compaction pass was asked to audit for (a governance record with no Pattern Library fallback, silently lost by a size-driven compaction). No investigation performed this pass — status and description are the verbatim original finding, backfilled into the registry so it is no longer only one clause inside one CLAUDE.md bullet. | aliases:
GK-135 | SHIPPED-PENDING PHYSICAL | 62e72f1 | 2026-08-17 | **Directive AT — "year candidates always enter."** Production: Venom Separation Anxiety #1 (issue/variant already correctly adopted by AS/AM/AR) and a Dell'Otto book (price bands genuinely computed) both LOCKED on "year missing" — `src/lib/identityGate.js`'s `assessIdentityConfidence` treats year as a plain required field, and `resolveYear` (`identityCore.js`) has no path to the pool's own year evidence when Vision/PC/CV all come up empty (a routine shape for modern covers, which print no year at all). FIXED: new `reconcileYear` (`src/lib/identityReconciler.js`, mirrors `reconcileIssue`'s own shape) fed FOUR raw signals as independent evidence — Vision's year, the catalog anchor (PC/CV), the pool's own year-consensus (`poolYearHint`, already computed, previously fed only CV query construction), and the frozen rank-1 row's own year token (`extractFirstEligibleYearCandidate`, AL-4e's existing extractor, previously only reachable from the narrow N2 PC-re-anchor trigger) — wired in `api/enrich.js` immediately after `resolveYear`'s own call, which is left completely untouched (same inputs, same precedence, same tests). When `resolveYear` finds nothing, `reconcileYear`'s own winner (if any) is adopted as the rescue (`confirmedYear` populated, `out.yearAuthority` set); when `resolveYear` already found a value, only the AUTHORITY is reported, never the value, keeping `resolveYear`'s own well-tested precedence undisturbed. `out.yearAuthority==='CONTESTED'` floors `deriveMarketStanding` to `SIMILAR_ONLY` (`actionAuthority.js`) with a distinct `YEAR_CONTESTED` reason code (`responseContract.js`) — AR's per-facet law (exact standing requires CORROBORATED on every facet the pool was filtered by) extended to year. `applyEraConsistencyFilter` (`api/comps.js`) gains an advisory mode (`yearIsContested`, new 5th param, default `false` — every pre-existing caller/test byte-identical): a CONTESTED year no longer hard-rejects comps against it, closing the "1971 disease" risk of deleting the operator's own book on a disputed anchor. Real regression found and fixed forward in the same pass: `tests/grailkey-directive-am-variant-custody-real.test.js` had a hardcoded `matches.length === 2` count assertion (own-row `parsedVisualRows` usage) that this dispatch's own THIRD, legitimately-correct call site broke — floor changed to `>= 2`. `tests/grailkey-directive-at-year-evidence.test.js`, 31/31 — DIRECT proof via the real `/api/enrich` handler for the Venom production shape (rescue fires, `identity-gate` `missing=[]`, `statusCode=200`), plus reconciler/actionAuthority/era-filter unit-level fixtures for Dell'Otto, the no-candidate control, the CONTESTED-cannot-price-exact law, and the era-filter advisory negative control. Full 218-file regression sweep: 195 PASS / 19 FAIL / 4 TIMEOUT, byte-identical FAIL/TIMEOUT list to the prior baseline (194/19/4/217) — zero new regressions, delta fully accounted for by the one new passing test file. SHIPPED-PENDING PHYSICAL per this registry's standing evidence bar — closure reserved for Jimmy's post-deploy rescans of Venom, Dell'Otto, and Detective Comics #1107 (all three should show a price at REVIEW with `YEAR_CONTESTED` where applicable, correction form offering the year field alone). | aliases:
GK-145 | OPEN | c825192 | 2026-08-21 | **DATA-1 readiness dispatch, Task 2 — collection-record correlation wiring.** `collectionItemId` (the IndexedDB collection record's own `item.id`) now threads through every collection-originated `/api/enrich` request (`refreshMarketData`, auto-refresh, `reIdentifyBook`, `submitManualCorrection`/`buildManualCorrectionPayload`, bulk import, gradeBlob/duplicate-confirm post-save) into `buildScanLogRecord` (`src/lib/scanLog.js`, `api/enrich.js`). **Explicitly NOT `gkAssetId` and NOT proof of physical-copy identity — temporary client-record correlation only**, until DATA-1 defines a real durable asset identity; see `docs/DATA-1-READINESS.md` B2. Null on every free-standing scan not yet saved to the collection (Watch Mode preview, barcode identify, an unsaved buyer-mode preview) — legitimate by design, not a gap. Measurement-only: read at `api/enrich.js`'s request-body destructure solely to snapshot into the scanlog record; drives no identity, pricing, or decision logic. `tests/grailkey-dispatch-2026-08-21-gk145-gk146-measurement.test.js` — see this dispatch's own return-format report for exact pass counts and full-regression-sweep totals. | aliases:
GK-146 | OPEN | c825192 | 2026-08-21 | **DATA-1 readiness dispatch, Task 2 — scanLog outcome/valuation measurement fields.** `scanLog` records (`src/lib/scanLog.js`) now snapshot `out.decision.action`, `out.pricingSource`, `out.price` (a `fmtUsd()` currency string, same shape as every other price read per GK-74), and `out.gradeMultiplier` under a new `outcome` field — additive, no version bump, following this file's own established convention (Fix 32-C, Dispatch 33: a purely additive optional field does not require one). Makes the "Picture-to-Action" outcome-class denominator (`docs/DATA-1-READINESS.md` E1) measurable from real production data going forward — it was previously unmeasurable from any artifact in this repo. **Explicitly does NOT capture the comp-pool snapshot, `priceDerivationTrace`, or the full pricing rationale — does not make "why did it say $X" reconstructable**; see `docs/DATA-1-READINESS.md` B1/B3. `tests/grailkey-dispatch-2026-08-21-gk145-gk146-measurement.test.js`. | aliases:
GK-148 | CLOSED | 38ee71d | 2026-08-21 | **CLOSED (Phase 0.3, 2026-08-21): physical closure criterion met — the 02:50 rescan on production `48b44b4` shows zero Larsen anywhere in the response/logs, honest variant clearing, and a price derived from the 24-sold grade-proximate pool. The registered SHIPPED-PENDING PHYSICAL bar (Jimmy's post-deploy rescan of the actual Creepy #1 book) is satisfied by this citation.** "the publisher is not the artist." Creepy #1 scan, 2026-08-21 01:23: `[reconcile-variant]` produced "Frank Frazetta Erik Larsen" with `justifiedBy=[{ebay-pool-row-0: "Erik Larsen"}]` against a row titled "Creepy #1 CGC 7.0 (Warren 1964)..." — Erik Larsen appears nowhere in the pool. **Confirmed mechanism, full path traced (trace pass):** `reconcileVariantFacet` (`src/lib/identityCore.js:1269`) calls `extractFirstEligibleVariantCandidate` (`:940-957`) per pool row, which passes the ENTIRE raw row title (no field isolation — publisher parenthetical included) to `matchCreatorCanonicals` (`premiumCreators.js`). The exact-regex pass fails on "warren" (correctly — no creator is named that), falling through to the AU 4a-i fuzzy fallback (`fuzzyAliasMatches`), which word-tokenizes the WHOLE title and slides a 1-3-word window across it. The single-word window "warren" hits Erik Larsen's registered alias `'larsen'` at Levenshtein distance 2 (`w→l`, `r→s`) — both normalize to exactly 6 characters, clearing `MIN_FUZZY_ALIAS_LEN=6` with zero margin and landing exactly at `FUZZY_MAX_DISTANCE=2`, not a near-miss. A fuzzy hit on ANY alias pushes the creator's FULL canonical name — explaining why the log shows "Erik Larsen," not a partial string. AU's own B2 pairwise check only ever tested registry-alias-vs-registry-alias collisions, never arbitrary non-creator text. **FIXED:** `PUBLISHER_STOP_LIST` (`src/lib/premiumCreators.js`, ~70 comic publisher/imprint names, normalized the same way as everything else in the file) checked inside `fuzzyAliasMatches`'s window loop BEFORE the Levenshtein distance is computed — a window that IS a stop-listed token can never win a fuzzy match, regardless of distance. Does not touch the exact `SEARCH_INDEX` regex pass, and does not touch fuzzy matching for any window that isn't itself stop-listed (the Dell'Otto class — a genuine creator alias mangled by a seller — is structurally unaffected, confirmed: `tests/grailkey-directive-au-dellotto-1963.test.js` re-run byte-identical, 24/24, DEL O'TT match still fires). **B2-extended found two MORE real collisions beyond the reported one, not previously known**, both now closed by the same stop-list: "Boom"/"Boom Studios" ↔ John Broome's alias `'broome'`, and "Disney" ↔ Will Eisner's alias `'eisner'` / Simon Bisley's alias `'bisley'` (all distance ≤2). Verified as a genuine (non-vacuous) proof by confirming the new test suite fails as expected with the fix reverted (12 failures including all three publisher classes), then passes clean restored (12/12). `tests/grailkey-gk148-publisher-stoplist.test.js`, 12/12 — Part 1 direct repro against the real Creepy #1 row text (`extractFirstEligibleVariantCandidate`/`reconcileVariantFacet`, both zero-Larsen, genuine Frazetta credit unaffected), Part 2 B2-extended (210 checks across 70 publisher names × realistic listing shapes, zero false merges, plus a Levenshtein-distance corpus scan proving the test exercises the real mechanism rather than being vacuous), Part 3 real `/api/enrich` handler smoke (GK-138) — no exception, HTTP 200, zero "Larsen" anywhere in the final response or logs. Full 226-file regression sweep: 203 PASS / 19 FAIL / 4 TIMEOUT, byte-identical FAIL/TIMEOUT list to the prior 225-file baseline (202/19/4) — delta is exactly the one new, fully-passing file. SHIPPED-PENDING PHYSICAL per this registry's standing evidence bar — closure reserved for Jimmy's post-deploy rescan of the actual Creepy #1 book (card should show Frazetta only, no Larsen anywhere). | aliases:
GK-149 | CLOSED | - | 2026-08-21 | **CLOSED — NOT REPRODUCED / PHYSICAL EVIDENCE OVERRIDES ADVISORY (Phase 0.3, 2026-08-21).** Resolution: one-off Vision-generated advisory anomaly, consistent with a visual misread; not reproduced on rescan; physical inspection confirms the 1964 original. Jimmy physically inspected the Creepy #1: the cover does NOT say "Collector's Edition" — it is the 1964 original. The raw 01:23 Vision `reason` string was never recovered, so the observed mechanism at 01:23 is NOT asserted — only that it did not reproduce at 02:50 and that physical inspection confirms the book. `detectEditionWarning` behaved as a cautious advisory, which is its design; no code change. Prior trace, retained for history: **Trace-first, report-only — hypothesized defect NOT CONFIRMED against the actual code; the dispatch's premise does not match what the code does.** Creepy #1 scan, 2026-08-21 01:23: a sanity flag reading "'COLLECTOR'S EDITION' — not the original" was suspected of firing on a COMP/pool listing's title text ("FIRST APP. COLLECTOR ISSUE," seller hype meaning collectible, not a reprint marker) rather than the operator's own physical evidence — a violation of the AN doctrine ("corroboration must be physical," GrailKey Directive AN 2026-08-16) if true. **Traced: the one live mechanism capable of producing this exact text is `detectEditionWarning` (`api/grade.js:179-215`), specifically the `not-original` pattern (`:188`, `/\bNOT\s+the\s+(?:original\|rare\|first\|1st)\b/`), called from `api/enrich.js:2440` as `detectEditionWarning(req.body?.reason)`.** `req.body.reason` is Vision's OWN free-form condition-report text about the physically scanned book (produced in `api/grade.js`, three internal sites all reading Vision's own output — `:596`, `:763`, `:805` — and echoed back by the client) — Vision runs BEFORE any eBay/comp-pool fetch in the pipeline and never sees comp listings. This is legitimate physical evidence per the AN doctrine, classification (a) not (c). `detectEditionWarning` tests ONE string against 8 fixed regexes once — it does not loop over comp/pool rows, frozen-rank-1 or otherwise, at all. Cross-checked `REPRINT_RE` (`compHygiene.js:38`, the actual comp/pool-title reprint filter in `api/comps.js` Filter 1) — it only matches `collector'?s?\s+box` (a subscription-box product line), not bare "collector's edition"/"collector issue," and would not have matched either cited title. `editionType` (the other reprint-adjacent field) is purely client-supplied advisory, never derived from title text. **No fix proposable against code as it exists — the hypothesized wrong-evidence-source bug was not found.** Two live possibilities, neither traced further this pass: (1) Vision's own `reason` text for this specific scan genuinely contained "collector's edition"/"not the original" wording — in which case this is a Vision-transcription-accuracy question (did the physical book actually say that, or did Vision misread/hallucinate), not a wrong-evidence-source bug; (2) the dispatch's flag description was a paraphrase that conflated two separate things on the card (a comp title mentioning "COLLECTOR ISSUE" seen elsewhere, unrelated to this specific check firing). **Needs the actual raw Vision `reason` string / log line from this specific scan before any further trace or fix can be scoped** — requesting it from Jimmy before proceeding. | aliases:
GK-147 | OPEN | - | 2026-08-21 | **Summit Phase 1 (2026-08-22): OPEN, exit-gate governed by `docs/adr/ADR-ADAPTER-001-adapter-contract.md` (Ruling 31) — ADR adoption does not close this ticket (amendment A2).** The ADR makes this ticket's own "the ticket is the fence" framing literal and binding: no second adapter (BookAdapter/CardAdapter) may start until every item named below is fixed or explicitly re-scoped. **DATA-1 readiness dispatch, Task 3 — report-only, no fixes.** Proven domain-leakage debt against future ComicAdapter/domain-separation work, found during the DATA-1 readiness interrogation (`docs/DATA-1-READINESS.md` D2) — confirmed even inside modules CLAUDE.md documents as already-universal (AssetCore): `decisionEngine.js:137-151` hard-gates identity-completeness on comic-specific `issue`/`publisher` presence; `pricingEngine.js:126` carries a live "comic-specific — revisit in ComicAdapter Step 5" TODO, contradicting its own :7-8 domain-agnostic header claim; `pricingEngine.js:111` hardcodes the 1956 Silver Age boundary inside a claimed-domain-agnostic module; `pricingEngine.js:46,183` exposes `isMegaKey` as if it were a universal pricing primitive. No fixes this pass. Purpose: DATA-1's generic asset layer must not inherit these domain assumptions — this ticket is the fence, not a repair. | aliases:
GK-150 | OPEN | - | 2026-08-21 | **Summit Phase 1 (2026-08-22): OPEN, remediation path is `docs/adr/ADR-ADAPTER-001-adapter-contract.md` (Ruling 32, listing-as-projection) — ADR adoption does not close this ticket (amendment A2).** The ADR ratifies exactly the fix this entry already proposed (`gkBundleId` → `external_map`, external ID never the primary key); implementation is separate, not-yet-done work. **Asset Network census (`docs/ASSET-NETWORK-CENSUS.md`) — architectural debt, report-only, no fix.** `bundleId = ebayItemId` (`src/App.jsx:12109`, `listBundleOnEbay`) — a GrailKey grouping concept (a bundle of items sold as one unit) derives its own identity directly from an external marketplace's ID scheme, with no independent existence of its own. A Whatnot, local-sale, or any future non-eBay bundle has no eBay ID to reuse and cannot be represented under this scheme. **Correct future direction, not built here:** `gkBundleId` (a durable, GrailKey-owned identity) → `external_map` (per DATA-0A's own generic crosswalk table, `db/data0/0001_generic_substrate.sql`) → the eBay `ItemID` as ONE mapped external reference among possibly several — never the reverse (an external ID standing in as the primary key). No fix now — same standing as GK-147: the ticket is the fence, so this coupling isn't silently carried forward into DATA-1's schema by accident. | aliases:
GK-151 | OPEN | - | 2026-08-21 | **Summit Phase 1 (2026-08-22): OPEN, HARD GATE, formalized by `docs/adr/ADR-AUTH-001-principal-owner-custodian.md` (Rulings 12–15, 36) — ADR adoption does not close this ticket (amendment A2).** The ADR preserves this entry's own four-step chain verbatim as binding ("ALL FOUR required, in this order, before any multi-user commerce ships") and adds the ownership/custody/delegation model the chain depends on; implementation remains not-yet-done. **Asset Network census (`docs/ASSET-NETWORK-CENSUS.md`) — pre-commerce security gate, report-only, no fix.** 11 of 14 `api/*.js` endpoints carry zero access-gate check of any kind (confirmed by direct trace, Section K1a/P1 of the census) — including `api/list-ebay.js`/`api/delist-ebay.js`, the two endpoints that create and cancel REAL, live eBay listings. The 3 endpoints that DO check (`enrich.js`/`comps.js`/`grade.js`) validate one shared secret (`ACCESS_CODE`/`vault_key`), not a per-user principal — it cannot distinguish requester A from requester B. Compounding: "list on eBay" today means "list on the one operator's eBay account" (`EBAY_AUTH_TOKEN` etc., one static credential set for the whole deployment) — there is no per-user marketplace-credential concept anywhere. **Acceptable today, as a single-operator prototype under Jimmy's own sole use. Categorically unacceptable the moment a second real user exists** — with no per-request identity, User A's actions are indistinguishable from User B's, both would operate under the same shared secret and the same external eBay account, and 11 endpoints have no gate at all to even ask the question. **Standing hard gate for multi-user commerce, stated here so it cannot be skipped or fixed piecemeal later:** authenticated principal → authorized asset (does this principal have a real ownership/authorization relationship to this specific `gkAssetId`) → authorized marketplace account (does this principal's OWN eBay/Whatnot/etc. credential authorize this action, not a shared deployment-wide one) → authorized mutation (does the specific write being attempted fall within what this principal is allowed to do to this asset) — ALL FOUR required, in this order, before any multi-user commerce ships. Not fixed here, not scoped as an implementation — the ticket exists so this bar is greppable and cannot be quietly lowered by a future dispatch that only closes part of it. | aliases:
GK-152 | CLOSED | 6ecbc5d | 2026-08-22 | **CLOSED (2026-08-22 rescan, build `6ecbc5d`): the eligibility half of this ticket's own pending-physical bar — whether a comp genuinely agreeing with the rescued issue number actually reaches pricing (not just the rescue mechanism setting `confirmedIssue` itself, already the SHIPPED half) — is now confirmed: the agreeing comp passed, rejected=0. This half depended on GK-158 (evidenceEligibility.js's TARGET_ISSUE_PROVISIONAL_AUTHORITY admission fix, shipped after GK-152 itself), which is why it stayed pending until now rather than closing alongside the rescue mechanism.** Absolute Wonder Woman #16 Talavera virgin, real production scan (correlationId `f2b89bbf-5444-42c9-8fa2-97f32de4f000`, 2026-08-22T07:02:03Z, build `3f2bdad`). Trace (T1, verbatim from real Vercel runtime logs): Vision misidentified the book entirely as "Dark Nights: Death Metal" #1 (zero token overlap with the real title); the raw 20-item eBay image-search pool was a genuine mix of Absolute Wonder Woman #16/#17/#18/#20 virgin-variant covers by different artists — title-family clustering correctly refused to adopt any single issue from that pool at only 43% family-level agreement (below the standing 60% bar, Flash #139 precedent, NOT a defect). `[vision-zero-support] ESCALATE` then correctly nulled `confirmedIssue` (0% raw-pool support for Vision's own wrong "1" guess). **The real defect (T2):** by the time comps were fetched, `api/comps.js`'s own variant/cover-letter/multi-issue/slab filter chain — a materially stronger, more specific signal than the raw family clustering — produced a genuinely UNANIMOUS 5/5 "#16 Talavera" active comps pool; that later, stronger evidence never fed back into the issue facet. Commit B's market-evidence gate (`api/enrich.js`) then discarded an already-computed real price ($12.91) and hard-locked the card (`TARGET_ISSUE_UNRESOLVED`/`PRICING_REFUSED`/`LOCKED`). Confirmed DISTINCT from `TARGET_ISSUE_PROVISIONAL_AUTHORITY`/`marketCustodyConflicted` (GK-141's own territory) — those gate a present-but-uncertain issue value; this scan's `out.issueAuthority` was literally `null` (no value at all), a different precondition. **Fix (T3, shipped):** `rescueIssueFromCompsPoolConsensus` (`src/lib/identityCore.js`) — a new, later-stage, deliberately conservative rescue: fires only when `confirmedIssue` is null and the FINAL pricing-eligible comps pool is unanimous (not majority) on one issue number with a real row-count floor (`MINIMUM_CORROBORATING_ROWS`); reuses `projectIssueAuthority` (never a competing authority mechanism) with `authority: 'CONTESTED'` always, never `CORROBORATED`. Wired into `api/enrich.js` right after `out.rawComps` finalizes, before Commit B's own check. `deriveMarketStanding`/`responseContract.js` (`src/lib/actionAuthority.js`) gained the MISSING fourth per-facet CONTESTED floor (issue, alongside the existing variant/year/title siblings AR/AT/AV already shipped) — `out.issueAuthority?.status === 'conflicted'` now floors standing to `SIMILAR_ONLY`, matching AR's own gates: REVIEW, never READY, from this path. Does NOT touch vision-zero-support/Guard 7/reconcileIssue's own Phase-1 logic, which stays correct for the genuinely-ambiguous-raw-pool case it already handles. Real-handler proof (GK-138): `tests/gk152-handler-smoke.test.js`, 11/11 — the real handler, real fetch stubs matching the actual production title mix, confirms the rescue fires, `out.issue="16"`, `out.issueAuthority.status="conflicted"`, Commit B's own gate does not fire, `listingHardLocked` stays true (listable=false), `decision.action` never reaches `LIST_NOW`. Unit coverage: `tests/gk152-comps-issue-rescue.test.js` (11/11, including the genuine no-issue/empty-pool/non-unanimous/sub-floor/graded/partial-unanimity controls — AS C3's own "ID_REQUIRED survives when no candidate exists anywhere" unchanged) + `tests/gk152-market-standing-issue-contested.test.js` (6/6). Full 234-file regression sweep re-run clean: 211 PASS / 19 FAIL / 4 TIMEOUT, all 19+4 confirmed pre-existing/already-documented (GK-89/90/91 and the 2026-08-11-C sweep's own known set) — zero new failures, zero regressions. One pre-existing count-assertion updated as a real, expected consequence (`grailkey-directive-aq-canonical-facet-authority.test.js`: 4→5 legitimate `out.issueAuthority` writer sites, GK-152's rescue being the new, independently-justified 5th, reusing the same `projectIssueAuthority` projection, never a competing mechanism). **PENDING PHYSICAL** per this project's own standing bar — awaiting a real rescan confirming the card now shows issue #16, priced, REVIEW, listable=false. | aliases:
GK-153 | CLOSED | 6ecbc5d | 2026-08-22 | **CLOSED (2026-08-22 19:46 rescan, build `6ecbc5d`): physical closure criterion met — the card's title is compound-protected, "G.I. Joe" intact, no corruption.** G.I. Joe #5 Tyler Kirkham 616 virgin, real production scan (2026-08-22 07:07:15/07:08:06, build `3f2bdad`) — "22e didn't misfire, it caught real upstream corruption." Trace (same dispatch train as GK-152/154/155/156): `familyCandidate.topFamily.title` = "616 gi joe cobra commander" (correct, unstripped) but `familyCandidate.selectedTitle` = "616 gi cobra commander" (missing "joe") — TWO different strings for the SAME family. Root cause: `applyDualAxisGate` returned `provenance: 'creator-lane-physical-corroboration'`, so `buildGatedTitleSource` (`src/lib/imageSearchIdentity.js`) reconstructed the candidate title from `agreedTokens + admittedTitleTokens` ("g i joe 616 gi cobra commander") instead of using `topFamily.title` verbatim; that reconstruction then passed through `sanitizeSeriesTitle` (`src/lib/identityCore.js`), whose `NOISE_PATTERNS[0]` regex strips every bare word in `LEGACY_CREATOR_NOISE_WORDS` — which includes literal `'joe'` (added for creator credits like "Joe Jusko"/"Joe Casey"). Confirmed via direct execution: `sanitizeSeriesTitle("g i joe") === "g i"` before this fix — same bug CLASS as GK-143 ("jim" inside "Jiménez") but a plain bare-word match, not a Unicode-boundary edge case, hitting a real franchise title instead of a coincidental substring. `22e` (`api/enrich.js`) then correctly detected the missing Vision token ("joe") against the corrupted assembled string and forced Vision's raw title back — **22e is explicitly EXONERATED by this trace: it is the safety net that worked, not the defect.** The registry record this ticket exists to correct is any future dispatch assuming 22e "wrongly vetoes" a weighted-consensus election on this book's shape. **FIX:** `'g i joe'` / `'g.i. joe'` / `'gi joe'` added to `COMPOUND_WHITELIST` (`src/lib/compHygiene.js`) so `sanitizeSeriesTitle`'s `protectedHit` check (Q70's own mechanism, already protecting "X-Men," "Captain Marvel," etc.) returns before the noise-word regex ever runs. Does NOT remove `'joe'` from `LEGACY_CREATOR_NOISE_WORDS` — negative controls (Joe Jusko, two separate unrelated titles) confirmed still stripping cleanly. **KNOWN, NOT-FIXED RESIDUAL GAP (flagged, out of scope):** `protectedHit` is a prefix-only match (`bareTitle === entry \|\| bareTitle.startsWith(entry + ' ')`) — a string like bare `"616 gi joe cobra commander"` (prefixed by "616," not "gi joe") would NOT match and would still lose "joe" if it ever reached `sanitizeSeriesTitle` directly outside the `buildGatedTitleSource`-intercepted path traced here. `tests/gk153-gijoe-compound-whitelist.test.js`, 12/12. Full detail: Pattern Library. | aliases:
GK-154 | CLOSED | 6ecbc5d | 2026-08-22 | **CLOSED (2026-08-22 19:46 rescan, build `6ecbc5d`): physical closure criterion met — the card confirms 2025, not the wrong 1987 PriceCharting anchor.** G.I. Joe #5 Tyler Kirkham 616 virgin, same scan as GK-153/155/156 — "a rejected anchor is rejected everywhere." PriceCharting matched "G.I. Joe Special Missions #5 (1987)" — an unrelated 1987 Marvel mini-series sharing only the base "G.I. Joe" name (`reason=base-entry`, no plausibility check). `resolveYear` (`api/enrich.js`) had no user year at all (this modern virgin-variant cover carries no printed year) and adopted PC's year unconditionally per its own documented branch (c)/(f) gap (CLAUDE.md "Year Override Guard") — `confirmedYear` became 1987. ~500 lines later, `q141-a` independently concluded this SAME anchor's own projected title ("G.I. Joe Special Missions") doesn't match the corroborated `confirmedTitle` and skipped the TITLE write ("evidence the ANCHOR is wrong, not the title") — but nothing retroactively invalidated the YEAR the same anchor had already supplied; year and title facets were evaluated by two separate, un-cross-referenced mechanisms, only one of which had an anchor-mismatch guard. **FIX (narrow, per the report's own design constraint):** a new gate, immediately before the `resolveYear` call site, reuses the IDENTICAL predicate `q141-a` runs (`projectCanonicalTitleFromAnchor` + `isCorroboratedIdentitySource`) to decide whether `pcYear` should even be OFFERED to `resolveYear` — when the anchor already fails title corroboration, `pcYear` is withheld (passed as `null`), so `resolveYear` falls through to its own unmodified precedence instead of adopting the rejected anchor's year. `q141-a` itself (its own log lines, its own `confirmedTitle` write) is completely untouched — a second, independent consumer of the same pure check, not a refactor of it. The catalog year remains visible as CONFLICT evidence: `catalogYearForEvidence` (unchanged) still reads the real, un-gated `pcYear`, so `reconcileYear` can still see and report the disagreement — never silently discarded (I13). **Combined with GK-156** (below — `poolYearHint` now actually reports a real 2025 signal instead of always-null): end-to-end, `resolveYear` finds nothing (gate withheld pcYear, no CV/eBay-consensus match either) → `null`; `reconcileYear`'s pre-existing RESCUE branch (GK-135/AT, untouched) adopts `poolYearHint`'s 2025 (source=`'pool-consensus'`, sole-authority-capable per GK-137) over the withheld 1987, landing `confirmedYear=2025, yearAuthority=CONTESTED` — proven via the real handler, `tests/gk153-156-gijoe-handler-smoke.test.js`. **FLAGGED, NOT FIXED (inherited from `q141-a`, out of this narrow fix's scope):** the shared predicate is an exact-STRING comparison, so a genuinely-matching anchor whose projected title differs from `confirmedTitle` only by CASE would still be (incorrectly) "rejected" — this risk pre-dates this fix (same predicate, reused verbatim) and is documented, not introduced, here. `tests/gk154-year-anchor-gate.test.js`, 9/9 (predicate unit coverage + genuine-anchor negative controls + live-source wiring proof that `q141-a` itself is byte-identical). Full detail: Pattern Library. | aliases:
GK-155 | CLOSED | 6ecbc5d | 2026-08-22 | **CLOSED (2026-08-22 19:46 rescan, build `6ecbc5d`): physical closure criterion met — the variant string is deduped, no repeated tokens.** G.I. Joe #5 Tyler Kirkham 616 virgin, same scan as GK-153/154/156 — "the variant string that ate its own tail." `extractConfirmedVariant` (`src/lib/variantIdentity.js`) pushed Vision's own full variant string verbatim ("exclusive limited signed virgin") into `parts`, then separately pushed the pool's independently-corroborated `consensus.exclusive` ("exclusive") and `consensus.limitation` ("limited") with no check for whether those words were already present — producing `"exclusive limited signed virgin exclusive limited"`, byte-identical to both real production scans. Not cosmetic: the duplicated string reached the LIVE eBay search query (`q="g i joe #5 exclusive limited signed virgin exclusive limited 1987"`, confirmed in the real log) and the active-comp cache key, not just the display field. **FIX:** token-set idempotent build (`pushDedupedVariantPhrase`) — tracks already-added words (case-insensitive, whole-word) and only appends the NEW words each phrase contributes; a phrase contributing nothing new is dropped entirely, order preserved. Comp-side AND-match filtering itself was unaffected (it already tokenizes/dedupes down to a token set before matching — `[exclusive,limited,signed]` in the real log, not the raw duplicated string), so pricing correctness on this specific scan was not shown broken by this bug; the query and cache pollution were real regardless. **Registry note (independence "n/a" cleared as a non-bug):** the `[coverType-consensus]` log's `independence.pass=false`/`uniqueItemIdCount=n/a` fields were a FALLBACK placeholder, not a computed result — `coverTypeIndependence` is only computed when `coverTypePromotion.promote` is true, and here promotion was already declined for an unrelated, legitimate reason (`runnerUp-present`) before independence ever ran. Log line improved to print `independence=skipped(promotion-declined)` instead of placeholder zeros that read like a second, failed check. **Open follow-up, logged not built:** variant-axis canonicalization (GK-142/AW's `deriveSeriesCoreQuery` law, currently title-only) remains unbuilt for the variant axis — the query string still consumes raw, uncanonicalized `confirmedVariant` text; this fix dedupes what's there, it does not canonicalize it. `tests/gk155-variant-dedup.test.js`, 7/7 (real-shape reproduction, no-overlap control proving dedup doesn't suppress genuinely new tokens, case-insensitive dedup control). Full detail: Pattern Library. | aliases:
GK-156 | CLOSED | 6ecbc5d | 2026-08-22 | **CLOSED (2026-08-22 19:46 rescan, build `6ecbc5d`): physical closure criterion met — the card prices from its own real market (rawTitle-sourced era-lock/poolYearHint feeding a genuine comps pool), not a starved/empty one.** Systemic, found while tracing GK-154 — "the field that was never the field it looked like." `eraLock` (Ship #22a) and `poolYearHint` (Q121), both `api/enrich.js`, scan `r.title` across `parsedVisualRows` for 19xx/20xx year tokens — but on a `parsedVisualRows` row, `.title` is `extractSeriesTitle(rawTitle)` (`src/lib/imageSearchIdentity.js`), a SANITIZED series-name projection, not the raw listing text a year token actually lives in. Verified by direct execution against the real 20-item G.I. Joe pool: 5/20 raw titles carry an explicit "(2025)"/"2025" token — comfortably above every threshold both mechanisms need — but only 2/20 survive in the sanitized `.title` field, silently starving `poolYearHint` below its own `>=3` floor (and `eraLock` to exactly 0, its stricter near-issue/first-half patterns needing more surrounding context than the sanitized field retains) on every affected scan, with NO log line at all in either case (confirmed absent from two independent real production runs of this exact book, not assumed). `.rawTitle` is the correct field — already used correctly elsewhere in the same file (e.g. the pc-anchor-gate discriminator check). **This is a systemic, live bug, not scoped to G.I. Joe** — it silently disabled two independent safety signals (era-lock's hard/advisory decade check, and the `poolYearHint` that `pc-anchor-gate`'s year axis and GK-154's new anchor-year gate both depend on) on every scan where the extraction has ever run, for however long this has been the case. **FIX:** `r.title` → `r.rawTitle` at both sites, plus a log line for each previously-silent null-return case (`[22a] SKIPPED`, `[cv-pool-year-hint] SKIPPED`) so a thin/empty extraction is visible from here on instead of being indistinguishable from "never checked." `tests/gk156-poolyearhint-eralock-rawtitle.test.js`, 7/7 (real-pool reproduction proving both the fix and the bug it replaces, plus a live-source-text check guarding against silent regression back to `.title`). Downstream consequence proven end-to-end in GK-154's own handler-smoke fixture. Full detail: Pattern Library. | aliases:
GK-157 | CLOSED | 6ecbc5d | 2026-08-22 | **CLOSED (2026-08-22 19:46 rescan, build `6ecbc5d`): physical closure criterion met — same rescan as GK-153/154/155/156, confirming the word-boundary compound protection and elected-title canonicalizer projection both hold in production.** GK-153's own registered residual gap, closed — "the whitelist that only ever checked the front of the line." `sanitizeSeriesTitle`'s `COMPOUND_WHITELIST` `protectedHit` check (`src/lib/identityCore.js:177`) was `bareTitle === entry \|\| bareTitle.startsWith(entry + ' ')` — PREFIX-ONLY. GK-153's own fixture happened to prefix-match by coincidence (the `buildGatedTitleSource` reconstruction it traced led with the compound's own tokens); a raw family-election candidate that does NOT lead with the compound — e.g. `family.topFamily.title` itself, "616 gi joe cobra commander" (year-like token first) — still loses "joe" to the bare-word noise strip (`LEGACY_CREATOR_NOISE_WORDS` includes `'joe'`, for creator credits like "Joe Jusko"/"Joe Casey") if that raw shape ever reached `sanitizeSeriesTitle` directly. Confirmed via direct execution: `sanitizeSeriesTitle('616 gi joe cobra commander')` returned `'616 cobra commander'`-shaped output (joe lost) pre-fix. Separately, and independently: `resolveIdentity`'s `FAMILY_OVERRIDE_DECISIONS` branch (title-family election WINS — weighted-consensus/top-rank-protection) wrote `sanitizeSeriesTitle(family.selectedTitle)` directly as `confirmedTitle`, never passing it through `canonicalizeTitleCandidate`'s richer pipeline (attribution clause, merch/W-clause, parenthetical, grade/condition, generic finish-descriptor, standalone issue, publisher+year suffix — AV/AW, GK-133/GK-140) — that pipeline was reachable ONLY via `reconcileTitleFacet`'s OPPOSITE branch (`family.decision==='fallback-vision'`, election REFUSES). An elected family key that legitimately WINS got none of that cleanup. **FIX (both in `src/lib/identityCore.js`):** (1) `protectedHit` now checks the prefix form FIRST (byte-identical for every existing fixture, prefix always wins when both would match — "Ms Marvel Team-Up Captain Marvel Special" still resolves to "Ms Marvel," not "Marvel Team-Up"), then falls back to a word-boundary (Unicode-aware, `\p{L}\p{N}` boundaries — same discipline as GK-143) match ANYWHERE in the string when no prefix hit exists, extracting the compound and dropping noise on BOTH sides (previously only trailing noise was dropped, since a prefix match starts at index 0 by construction). (2) The `FAMILY_OVERRIDE_DECISIONS` branch now passes its `sanitizeSeriesTitle` output through `canonicalizeTitleCandidate` before assigning `confirmedTitle` — idempotent, over-strip-guarded (C6), operating on the ALREADY-GATED sanitized title (never raw pool/topFamily text, which would bypass Q84/Q119/Q142's own non-consensus-token exclusion); logs `[title-canon] elected family title canonicalized` only when it actually changes something (a genuine no-op on already-clean input, confirmed: zero strip categories fire, no log line). Negative controls confirmed byte-identical: X-Men/Captain Marvel prefix fixtures, Joe Jusko/Joe Casey creator-noise stripping (no G.I. Joe collision), a title naming BOTH "G.I. Joe" and "Joe Casey" still resolving to clean "G.I. Joe." `tests/gk157-elected-title-canonicalization.test.js`, 13/13. Full detail: Pattern Library. | aliases:
GK-158 | SHIPPED-PENDING PHYSICAL | pending-backfill | 2026-08-22 | **"the gate that couldn't tell agreement from silence" — `TARGET_ISSUE_PROVISIONAL_AUTHORITY` stopped discarding evidence that agreed with it.** `evidenceEligibility.js`'s `classifyEvidenceRow` (`src/lib/evidenceEligibility.js:295-342`), the branch gating a marketplace-only-adopted (provisional/conflicted, not yet independently corroborated) target issue, was an UNCONDITIONAL blanket demotion: EVERY row was pushed to reference-only the instant `issueAuthorityStatus` wasn't `'confirmed'`, regardless of whether the row's own title actually agreed with the contested issue number — `hasIssueNumber` was never even evaluated in this branch. That discarded genuinely corroborating evidence: a comps pool unanimous on the same (still-unconfirmed) issue reading — e.g. GK-152's own `rescueIssueFromCompsPoolConsensus` shape, or any other path landing `out.issueAuthority.status` at `'conflicted'`/`'provisional'` via `projectIssueAuthority` before this gate runs — is real, usable pricing evidence for that reading; it simply hasn't cleared the bar for CONFIRMED. **FIX:** `hasIssueNumber` is now evaluated even inside this branch. A row whose own title AGREES with the contested value is identity-ELIGIBLE (tagged `PROVISIONAL_ISSUE_MATCH`, zero rejection codes on the issue axis — may reach `rawPricingEligible`/pricing, never silently promoted to look as trustworthy as a confirmed-issue row) and lands in `buildEvidencePopulations`'s `rawPricingPool`, not `provisionalAuthorityReferences`. A row whose own title DISAGREES is still rejected outright (`WRONG_ISSUE`, `identityEligible=false`) — disagreeing with an unconfirmed guess is a genuine conflict on top of an already-uncertain read, not "more of the same signal." **Untouched by design:** `deriveMarketStanding`'s GK-152 per-facet floor (`src/lib/actionAuthority.js` — `SIMILAR_ONLY` whenever `out.issueAuthority?.status === 'conflicted'`) and commit4-terminal's own listing lock (`computeIssueAuthorityContractPatch`, `src/lib/issueAuthority.js`) both key purely off `issueAuthority.status`, independent of which rows fed the price — this fix only decides whether a MATCHING row may contribute to the price at all, never how confident the resulting card is allowed to look; REVIEW/`SIMILAR_ONLY`, never READY/`EXACT_CURRENT`, from this path regardless. **Side finding, logged not fixed here (GK-35, this file's own pre-existing entry updated with the detail):** `PRICING_GATE_CODES` (`evidenceEligibility.js:832`) does not include `WRONG_ISSUE` — a plain issue-mismatch row is not, on its own, excluded by `buildPricingEligibleRows`/`isPricingMathEligible`, the real gate both `api/comps.js` and `soldVerification.js` call. This is NEWLY REACHABLE as a consequence of this fix specifically (previously such a row always carried the gate-listed `TARGET_ISSUE_PROVISIONAL_AUTHORITY` code instead; now it carries the non-gate-listed `WRONG_ISSUE` code) but confirmed currently DORMANT in production: both real call sites have their own separate, earlier issue-matching filter that already excludes a mismatched row before `isPricingMathEligible` is ever consulted. Not fixed here — out of this dispatch's narrow scope; tracked under the existing GK-35 entry, not a new ticket. **Real-handler wiring proof (GK-138):** `tests/gk157-158-handler-smoke.test.js` — Part 1/2 re-run the byte-identical GK-153-156/GK-152 production fixtures through the real `/api/enrich` handler (zero regression; Part 2 additionally surfaces an organic, non-hand-crafted confirmation: this trimmed fixture's own internal comps re-fetch genuinely reaches `evidenceTarget` with `issue="16"` + `issueAuthorityStatus="conflicted"` already set, and the one title-matching row is admitted, excluded only by an unrelated mechanism, WRONG_VARIANT, never WRONG_ISSUE); Part 3 calls the real, exported `api/comps.js:fetchComps` directly with issue pre-resolved to a contested value — 2 genuinely matching comps price (count=2), a 3rd mismatching comp does not, and `deriveMarketStanding`/`deriveLocks` still floor to `SIMILAR_ONLY`/`market-standing-issue-contested` alongside the real price. Unit coverage: `tests/gk158-provisional-issue-match.test.js`, 24/24 (matching-row admission, mismatching-row rejection, `buildEvidencePopulations` pool composition, the untouched floor, and a genuine no-consensus-junk negative control). Two large pre-existing integration suites updated as real, expected consequences of this fix (their old assertions encoded the now-corrected blanket-demotion behavior): `tests/q-trackB-commit4-adoption-provisional.test.js` (156/156) and `tests/grailkey-dispatch-25-fix1-instrumentation.test.js` (25/25). Full detail: Pattern Library. | aliases:
GK-159 | SHIPPED-PENDING PHYSICAL | pending-backfill | 2026-08-22 | **"commit4-terminal floors instead of clears" — the last contested facet that still cleared instead of flooring.** `computeIssueAuthorityContractPatch` (`src/lib/issueAuthority.js`, this codebase's own "commit4-terminal") unconditionally nulled price/bands and forced ID_REQUIRED-class contract state the instant `issueAuthority.status==='conflicted'`, regardless of whether a real price had already been computed — the one facet still CLEARING when every sibling contested facet (variant AR/GK-129, year AT/GK-135, title AV/GK-133, and issue itself via GK-152's own `deriveMarketStanding` floor, `src/lib/actionAuthority.js`) already FLOORS to `marketStanding=SIMILAR_ONLY`/`actionAuthority.state=REVIEW` instead. Compounding: even setting that aside, overwriting `pricingSource` with the literal `'refused-issue-authority-conflicted'` string made `deriveMarketStanding`'s own `/^refused/` short-circuit return `NONE` before the GK-152 conflicted-issue floor below it ever got a chance to run — a second, independent way the same clear defeated that floor. **FIX, scoped to `issueConflicted` only** (`issueProvisional`/`yearOnlyProvisional` unchanged — a bare marketplace ADOPTION with zero external corroboration is a materially weaker read than a genuine disagreement the pool itself already priced through; widening to those is a separate, not-made-here decision): when `priorOut` already carries a real, computed price, the function now returns `{ marketStandingFloored: true }` — a patch touching NOTHING price-related (price/priceLow/priceHigh/priceBands/matchConfidence/pricingSource/confidenceLevel all stay exactly as the pipeline computed them, the honest floored source, never the synthetic refused string) and setting neither `identityConfident`/`refusedToPrice`/`listingHardLocked`. Deliberately NOT setting `listingHardLocked`: that flag always produces a hard, `'integrity'`-class lock (`responseContract.js` `deriveLocks`), which unconditionally forces `actionAuthority.state` to `LOCKED` regardless of `marketStanding` (Q41's own law, untouched) — the opposite of the REVIEW outcome this fix exists to produce. Listing stays gated anyway: `marketStanding` never reaches `EXACT_CURRENT` while `issueAuthority.status` is `'conflicted'` (GK-152's own floor), so `deriveActionAuthority`'s READY condition can never be met — state lands at REVIEW, listable stays false, without a hard lock's help. `responseContract.js`'s own pre-existing `'market-standing-issue-contested'` soft (insufficiency-class) lock (also GK-152) fires on exactly this shape and supplies the ISSUE_CONTESTED-class card banner — nothing new needed to surface it, only the absence of a preceding hard lock that would otherwise suppress it (gated on `preStandingLockCount === 0`). Genuine no-price scans (no computed price at all) are completely unchanged — falls through to the existing hard-clear branch exactly as before. `api/enrich.js`'s real call site (the `authorityPatch` block, ~line 12010) gained a third log branch (`authorityPatch.marketStandingFloored === true`) so this shape is logged distinctly from both the hard clear and the pre-existing P1 commit-p carve-out — an I13 requirement this file's own existing if/else already enforces for the other two shapes. **Blocking fixture (real `/api/enrich` handler):** the AWW shape — title/year/publisher all resolve cleanly (Absolute Wonder Woman, 2026, DC Comics), only Vision's own issue guess disagrees with a 5-item unanimous "#16" comps pool clearing `priceBands.js`'s tier-3 `verifiedActive.length>=3` threshold — card commits a real price (`$28.00` in this fixture, non-zero, `pricingSource='active_ask_derived'`), `contract.actionAuthority.state==='REVIEW'`, `marketStanding==='SIMILAR_ONLY'`, `reasonCodes` includes `'ISSUE_CONTESTED'`, `contract.listable===false`, `contract.state` never `ID_REQUIRED`/`REFUSED`/`PRICED`. **Controls:** GK-153-156's G.I. Joe fixture, GK-157-158's handler-smoke Part 1/2/3, and GK-152/GK-158's own dedicated suites all re-verified byte-identical (zero regression — this fix touches no code any of those exercise). `tests/gk159-commit4-terminal-floor.test.js` (28/28 — the floor patch itself, the byte-identical hard-clear control, all three untouched sibling branches, the `priorOut.refusedToPrice` short-circuit precedence, full end-to-end `deriveMarketStanding`/`deriveActionAuthority`/`assembleContract` integration, and the genuine no-price control through the same real downstream machinery) + `tests/gk159-commit4-terminal-floor-handler-smoke.test.js` (18/18, GK-138 real handler proof). Full detail: Pattern Library. | aliases:
GK-160 | OPEN | - | 2026-08-22 | **Report-only, no fix — `visualReferenceEvidence` familyKey mislabel.** Found during the AWW rescan review: the visual-reference-evidence record built for the Absolute Wonder Woman #16 Talavera scan carries a `familyKey` reading "something-is-killing-the-children" — a title-family cluster label lifted verbatim from an unrelated pool member's title text (a different comic entirely, evidently sharing enough raw-pool proximity or token overlap for the clustering step to adopt its string as the family's own display key) rather than any Wonder-Woman-derived text. Purely a DISPLAY/diagnostic mislabel on this one internal field — traced to have no effect on `confirmedTitle`, pricing, or `issueAuthority` (all independently correct on this same scan) — but worth fixing since a diagnostic field that lies about its own contents undermines exactly the kind of trace-first debugging this registry depends on. Not investigated further or fixed this pass — logged for a future dedicated trace of `buildVisualReferenceEvidence`'s (`src/lib/issueAuthority.js`) familyKey assignment. | aliases:
GK-161 | OPEN | - | 2026-08-22 | **Report-only, no fix — post-escalate publisher backfill assigns a wrong publisher.** Found during the AWW rescan review: after `[vision-zero-support] ESCALATE`-class identity uncertainty resolves, a publisher-backfill step assigns "BOOM! Studios" to a book independently confirmed as DC Comics (Absolute Wonder Woman is a DC imprint title). The backfill appears to source from some pool-adjacent or fallback publisher signal rather than the corroborated identity's own publisher, but the exact mechanism was not traced this pass. No downstream pricing/eligibility effect confirmed or ruled out — not investigated. Logged for a future dedicated trace of whichever post-escalate publisher-backfill call site in `api/enrich.js` fires after identity resolution; likely candidate given prior sibling findings (GK-107 cache-key publisher gap, the ComicVine-volume shape) but not confirmed. | aliases:
GK-162 | OPEN | - | 2026-08-22 | **Architecture note, not a defect today — `decisionEngine.js`'s `computeDecision` has no awareness of `out.issueAuthority` at all.** Disclosed finding from the GK-159 suite work (`tests/q-trackB-commit4.3.1-retention-decline-fail-closed.test.js`): given a real price and no identity-completeness/manual-review blockers, `computeDecision` resolves to `LIST_NOW` even when `issueAuthority.status==='conflicted'` — it reacts only to `identityComplete`/`identityMissingFields`/`manualReviewRequired`-class fields, none of which GK-159's floor patch sets. Listing currently stays correctly gated regardless: `deriveActionAuthority`/`assembleContract` (`src/lib/actionAuthority.js`/`responseContract.js`) are the actual transaction-authority boundary, and `marketStanding` never reaching `EXACT_CURRENT` while any facet is contested makes `READY` unreachable independent of what `decision.action` itself says — the SAME established precedent the pre-existing P1 commit-p carve-out already relies on (it likewise never touches `identityConfident`). Not a defect: the contract layer is the single source of truth for listability by design (Directive Z), and `decision.action` is advisory display text, never itself consulted for the listing gate. Flagged as an architecture note worth revisiting once the ADAPTER/DATA-1-era format-agnostic decision layer is designed — a genuinely format/facet-aware `computeDecision` that reads `issueAuthority` (and its sibling variant/year/title authority objects) directly would make the card's own displayed action text honest about contested facets, not just the underlying gate. No fix scoped or attempted here. **DATA-1C Addition 3 (2026-08-23) registry note:** this split is the seed of a future EXECUTION AUTHORITY layer between economic recommendation and execution — "SELL, identity review required before listing" as a coherent state, distinct from either a plain decision or a plain listing gate. Design-era note only, logged for the post-1D architecture (see `docs/adr/DATA-1C-MEDIA-DESIGN.md`, Addition 3, capture-order law) — nothing built against it this dispatch. | aliases:
GK-163 | OPEN | c17321d | 2026-08-23 | **Idempotency without a request fingerprint, DATA-1B-wide — `attachMedia` has the fix the other 9 service operations still need.** Found and verified during the DATA-1C correction pass (D4): `src/modules/assets/idempotency.js`'s `checkIdempotencyReplay`/`claimIdempotencyKey` — the shared mechanism `createPhysicalAsset`, `assignIdentity`, `correctIdentity`, `transferOwnership`, `recordAcquisition`, `recordValuation`, `recordDecision`, and `attachMediaMetadata` (9 of the 10 total service operations) all use — has no concept of "does this replay's request actually match the original." Confirmed directly, not assumed: re-running `S3-9-idempotency-key-replay.mjs` (unmodified, `C:\grailkey-data\data-1\service-proof\`) shows step 3 sending the SAME `idempotencyKey` with a DIFFERENT `recordValuation` payload and silently receiving back the ORIGINAL (`$33.33`) result — no error, no warning, the same key just "wins" regardless of whether the new call's content agrees with the old one. `attachMedia` (DATA-1C, the 10th operation) now carries the fix: a request fingerprint (`sha256` of the operation's own semantic fields) stored inside its `result_snapshot` and checked on every replay hit — same key + same fingerprint → the original result; same key + a different fingerprint → a typed `ConflictError`, never a silent wrong-answer replay. **OPEN, governed by the service layer** (`src/modules/assets/`) — fix shape: extend the same fingerprint pattern to the other 9 operations, either per-operation (mirroring `attachMedia`'s own approach, no schema change) or as a shared enhancement to `idempotency.js` itself (a schema/contract change to `idempotency_key`, needs its own ruling). **Sequencing note:** this should land as a bounded pass BEFORE DATA-1D's authentication work builds new call sites on top of the current, unguarded mechanism — a wrong-answer replay silently returning stale data is a worse failure mode once real multi-user auth exists than it is today, single-operator. No fix attempted here — DATA-1C's own bounded scope covered only the operation it introduced. | aliases:
MEDIA-DEBT | OPEN | c17321d | 2026-08-23 | **Two items, one entry, both pre-production, both named during DATA-1C, neither fixed.** (a) **Orphan-object reconciliation/GC** — `attachMedia`'s storage-then-transaction ordering (D5, `src/modules/assets/service.js`) means a `media.put()` that succeeds followed by a transactional write that later fails leaves a real, correctly-content-addressed object with no `media` row pointing at it — not identity corruption (the object's own hash is still correct), but a genuine storage/retention leak: bytes billed and held with nothing referencing them. No sweep/reconciliation mechanism exists. Must be solved before any production capture path (`GK-151`-gated) ships real, billable volume. (b) **Historical IndexedDB photo extraction has no real path today** — checked directly against `src/App.jsx` during the DATA-1C correction pass (D8): `exportJSON` (`:2962`), `exportCSV` (`:2974`), and `backupToDrive` (`:2994`) all explicitly exclude `images` from what they serialize (confirmed by direct grep, not assumed) — there is no existing app flow that gets a real historical photo's bytes out of the browser at all. A purpose-built internal export/read path is required before genuine (non-substitute) M4-1 evidence can ever be produced; until it exists, M4-1 stays `BLOCKED-ON-SOURCE-ACCESS` exactly as `docs/adr/DATA-1C-MEDIA-DESIGN.md` (D1/D8) states. Neither item fixed or attempted — logged so both stay greppable ahead of the internal/staging capture-integration dispatch, which (per the capture-order law) starts producing real server-side bytes going forward and shrinks item (b) to a one-time historical migration rather than an ongoing gap. | aliases:
```

## Standing Invariants (registry-scoped)

Rules a directive has explicitly ruled on and that future work must not
silently re-violate. Distinct from the general Authority Propagation
Invariant (`docs/PATTERN-LIBRARY.md`, mechanisms a/b/c) — these are narrower,
registry-local rules tied to specific ticket lines above.

- **Unknown printing confers no exact-print authority.** When a book's
  specific printing (1st vs. 2nd vs. 5th, etc.) has not been determined, the
  system must never silently treat it as first print — and, symmetrically,
  must never silently treat it as any OTHER specific printing either. Ruled
  2026-08-12 (Directive J Task 1), scoping GK-77A/GK-77B: completing
  `REPRINT_RE`'s Nth-print coverage (GK-77A) without first giving the
  scanned book its own printing-authority signal (GK-77B) would silently
  flip the unstated default from "assume later print, discount accordingly"
  to "assume first print, price at a premium" — trading one silent wrong
  default for another, not fixing the underlying absence of evidence. GK-77A
  is blocked on GK-77B specifically because of this invariant.

## UNKNOWN tickets — need Jimmy

Five tickets appear only as bare cross-references in CLAUDE.md / the Pattern
Library, with no dedicated write-up found in this repo establishing a shipped
SHA or a current open/closed state. Not guessed — flagged:

- **GK-19** — the `hasSufficientTitleOverlap` bigram-join relocation target
  named when Q22's stale assertions were characterized. Referenced only in
  CLAUDE.md's stale-test-suite section, never independently confirmed shipped
  or still open.
- **GK-24** — the "printing-axis fallback trigger" in `priceBands.js`,
  referenced only as a file-overlap check against the `priceBands (7)`
  test-failure triage. No dedicated entry found.
- **GK-47, GK-48, GK-49** — all three are referenced in Dispatch 48's entry as
  "pre-existing labels this repo had never logged" (i.e. externally assigned
  numbers this repo's own dispatches inherited mid-sequence) — Dispatch 50
  explicitly declined to backfill full write-ups for them, only noting their
  existence so a future renumbering pass wouldn't collide. Still true here:
  this pass does not backfill them either, consistent with that same decision.

## Related, not in this registry — do not confuse with GK-N

`Ship #NN` and `Q-NN` are a separate, older, stable identifier namespace with
no observed collision/renumbering problem — out of scope for this registry.
The renumbering problem this file exists to fix is specific to the `GK-N`
family (see GK-38/41/42's draft-vs-original collision above, the concrete
case that motivated this file).

## Candidate Patterns (not yet doctrine)

Concepts under observation from a single instance — explicitly NOT added to
the established Pattern Library mechanism list (Computed-Then-Discarded /
Validation Bypass on Authority Replacement / Stale Authority Inheritance) in
`docs/PATTERN-LIBRARY.md` or CLAUDE.md. A candidate is promoted to that list
only when an independent second instance is found; until then it stays here,
structurally separate from doctrine.

- **Confidence Without Coverage** *(hypothesis, one observation, not
  doctrine — logged 2026-08-12, Directive I, Task 3 synthesis)*. A
  determination is produced and acted on with no check that the evidence was
  adequate to produce it — and where a quality/confidence label exists
  alongside it, that label's real scope is honestly narrower than the claim
  its placement implies to a reader. Candidate test: *for any value that
  moves price or gates a decision, name the code that verifies the inputs
  were sufficient. If none exists, this may be an instance.* Single
  observation to date: GK-79B (`gradeMultiplier` applied with no
  evidence-coverage check; the adjacent `confidence` label is real and
  correctly scoped to image legibility, but is displayed under a
  condition-implying name — see GK-79A — inviting the reader to treat it as
  vouching for something it doesn't measure). **Promote to doctrine only if
  books 16–50 produce an independent second instance.**

## Observations

Non-ticket notes — record only, no GK-N assigned, no status tracked.

- **Classics Illustrated #26, 2026-08-11 re-scan.** Taxonomy changed
  `ID_REQUIRED` → `RESEARCH` between two scans of the same book. Containment
  held: price authority cleared, listing locked, Decision Safe false, price 0,
  identity unresolved. Phase 2 promoted a provisional identity (`pool family
  has 6 members >= 3 floor`), which moved the label but not the boundary. No
  defect established.
