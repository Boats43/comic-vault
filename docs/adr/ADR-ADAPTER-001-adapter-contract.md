# ADR-ADAPTER-001 — Adapter Contract, GK-147 Expansion Gate, Listing-as-Projection

**Status:** Ratified (Master Architecture Summit, Phase 1). **Rulings covered: 30–34, 37.**

## Context

CLAUDE.md's own AssetCore architecture already states the intended shape: "AssetCore is now universal — operates on primitives only... All format-specific domain knowledge lives in adapters." `src/adapters/ComicAdapter.js` (312 lines, 4/4 functions) is the one adapter that exists; `BookAdapter.js`/`CardAdapter.js` are documented roadmap (Session 4A/4B), not built. GK-147 (`docs/DATA-1-READINESS.md` D2, found during the DATA-1 readiness interrogation) proved the "universal" claim is not yet true in practice: `decisionEngine.js:137-151` hard-gates identity-completeness on comic-specific `issue`/`publisher` presence; `pricingEngine.js:126` carries its own TODO admitting a comic-specific assumption; `pricingEngine.js:111` hardcodes the 1956 Silver Age boundary inside a module claiming to be domain-agnostic; `pricingEngine.js:46,183` exposes `isMegaKey` as if it were universal. GK-147's own registry text was explicit about its purpose: "this ticket is the fence, not a repair" — logged so a second adapter isn't built on top of an assumption nobody checked. Separately, GK-150's Asset Network census finding (`bundleId = ebayItemId`) showed a second, related defect class: a GrailKey concept (a bundle) deriving its own identity from an external marketplace's ID scheme, with no independent existence of its own.

## Decision

**Ruling 30 — the adapter contract is formalized, not just documented as intent.** An adapter implements a fixed, small interface (matching `ComicAdapter.js`'s own existing 4-function shape: `detectKeyValue`, `verifyStory`, `computeEraRisk`, `sanitizeFormatTitle`, or their equivalents for a new asset class) and NOTHING outside that interface may be format-specific. AssetCore (`identityCore.js`, `pricingEngine.js`, `decisionEngine.js`) may only call INTO the adapter interface for anything format-specific — it may never itself branch on asset type, hardcode a format-specific threshold, or assume a format-specific field exists.

**Ruling 31 — GK-147 is the literal exit gate for building a second adapter.** No `BookAdapter.js`/`CardAdapter.js` (or any future adapter) may be started until every item GK-147 named is either fixed (moved behind the adapter interface) or explicitly re-scoped with its own ticket and rationale for why it can wait. This is stated as a hard sequencing rule, not a preference: building a second adapter on top of an AssetCore that secretly assumes comic-shaped data would silently reproduce the exact "second, competing truth system" failure class this project's other ADRs and `THE REBUILD RULE` already exist to prevent, one layer up — a hypothetical `BookAdapter.js` would either inherit the comic-specific bugs unknowingly, or need its own parallel patches, defeating the point of AssetCore's universality claim.

**Ruling 32 — listing-as-projection.** A marketplace listing (eBay, Whatnot, any future channel) is a PROJECTION of the canonical `gkAssetId`/identity/pricing data, never an independent source of truth and never the origin of a GrailKey-owned identifier. GK-150's `bundleId = ebayItemId` is the concrete defect this ruling fixes: a bundle's real identity is a GrailKey-owned grouping of `gkAssetId`s (ADR-ASSET-001); `ebayItemId` becomes an `external_map` row (ADR-ID-001's own external-reference model) — one mapped reference among possibly several external channels, never the primary key. This generalizes past bundles to every listing-shaped concept the adapter/commerce layer produces.

**Ruling 33 — commerce separation.** Identity/catalog concerns (what is this, which physical copy, who owns it) are architecturally separate from commerce concerns (is it listed, at what price, on which channel, in what state of a sale). The commerce layer CONSUMES identity/catalog/pricing data through defined interfaces; it never becomes a second place identity or ownership facts get written. This is the same principle ADR-AUTH-001's four-step authorization chain already assumes (asset authorization is checked before marketplace-account authorization, not folded together) — stated here as a structural separation, not just an authorization-order rule.

**Ruling 34 — commerce-layer idempotency.** Listing creation, delisting, and bundle operations are idempotent under the same discipline ADR-API-001 (Ruling 26) and ADR-ID-001 (mint idempotency) already require elsewhere: retrying a "list this asset" request must never produce two live listings for the same `gkAssetId` on the same channel.

**Ruling 37 — the adapter interface itself is versioned and stable independently of any single adapter's implementation.** Changing what AssetCore expects from an adapter (adding a required function, changing a signature) is an interface-version change, reviewed with the same weight as a schema migration — an adapter written against interface v1 must not silently break when AssetCore evolves, matching ADR-API-001's own versioning discipline extended to this internal contract.

## Invariants

1. No file under `src/lib/` (AssetCore) branches on asset type, format, or any format-specific field name.
2. No second adapter is started while any GK-147 item remains unresolved and unre-scoped.
3. No marketplace/channel identifier (eBay item ID, Whatnot listing ID, etc.) is ever used as, or stored as, a GrailKey primary key — always `external_map`.
4. Listing/bundle mutations are idempotent per Ruling 34.

## Consequences

- GK-147 gains real teeth: it is not just a documented debt, it is a hard blocker on the Session 4A/4B roadmap CLAUDE.md already describes.
- GK-150's fix has a concrete target (`external_map` + `gkBundleId`, per that ticket's own registry text) and is no longer just "logged, no fix" — this ADR is its remediation path (amendment A2: "GK-150 OPEN/remediation-ADAPTER").
- Future adapters (Book, Card) inherit a real, enforced contract rather than needing to independently discover which parts of AssetCore actually are universal versus quietly comic-shaped.

## Rejected Alternatives

- **Fix GK-147's specific 4 named items and consider the adapter boundary "good enough."** Rejected: GK-147's own text frames itself as "the fence, not a repair" — a spot-fix of the 4 known instances doesn't establish the STRUCTURAL guarantee (Invariant 1) that a fifth, not-yet-found instance can't exist; the contract must be enforced, not just patched around known cases.
- **Let `BookAdapter.js` work proceed in parallel with GK-147 remediation.** Rejected: risks the second-adapter effort discovering AND working around the same comic-specific leakage independently, duplicating the fix and likely diverging from it — sequencing (Ruling 31) is cheaper than parallel rediscovery.
- **Keep `ebayItemId` as a bundle's primary key for simplicity (it already works for the single-channel prototype).** Rejected: GK-150's own finding already identifies why — no independent existence for non-eBay bundles (Whatnot, local sale, any future channel), and the "external ID as primary key" pattern is exactly the shape ADR-ID-001's `external_map` model exists to replace project-wide.

## Implementation Gates

- `BookAdapter.js`/`CardAdapter.js` (Session 4A/4B, CLAUDE.md roadmap) are blocked until GK-147 is closed or explicitly re-scoped per Ruling 31.
- Any new marketplace/channel integration must route its own identifiers through `external_map` from its first commit, never introduce a new "external ID as primary key" pattern.

## Related Tickets

- GK-147 — OPEN/exit-gate-ADAPTER (per amendment A2) — the founding finding and the literal gate this ADR enforces.
- GK-150 — OPEN/remediation-ADAPTER (per amendment A2) — fixed by Ruling 32 (listing-as-projection), implementation not yet built.

## Amendment A3 — U2 Ratification (2026-09-21, GrailKey Universalization Census, U1/U2)

**Type: ARCHITECTURE/DOCUMENTATION RATIFICATION ONLY.** No code changed by this amendment. Ratified against `main` commit `836f264`. Full evidence trail: `docs/U1-UNIVERSAL-CENSUS.md`, C2/C10, and its U1 Review Packet addendum, items 4 and 10.

**Ruling 38 — the ComicAdapter seam's status is ratified LIVE + DRIFTED, not merely "the document exists."** `src/adapters/ComicAdapter.js` (4 exports: `detectKeyValue`, `verifyStory`, `computeEraRisk`, `sanitizeComicTitle`) has exactly **one** production caller, `api/enrich.js` (imported at line 84, called at 4 real sites: lines 3741, 11676, 11679, 11682), and two direct test files (`tests/price-lane-1-gk228-gk229-regression.test.js`, `tests/q119-compound-title-consolidation.test.js`). It is genuinely live — real caller, real tests, real production request path. It is simultaneously drifted in the specific sense this ADR itself defines: Invariant 1 ("No file under `src/lib/` branches on asset type, format, or any format-specific field name") is still violated today, one month after this ADR's original ratification (2026-08-21) — all four items GK-147 originally named remain unfixed, re-verified directly against source on 2026-09-21: `decisionEngine.js:137-139`'s `identity-incomplete` hard blocker, keyed off ComicAdapter's own comic-specific `identityComplete` computation; `pricingEngine.js:125-126`'s own comment admitting the 1970/1985 era-boundary math is comic-specific; the 1956 Silver Age boundary hardcoded at `pricingEngine.js:111`; `isMegaKey` read directly inside AssetCore pricing logic at `pricingEngine.js:46,183`. **This status must not be read as a finished, ratified integration merely because both the code and this governing document exist — Ruling 31's own gate (below) remains open precisely because of this status.**

**Ruling 39 — U3 targets amendment of this existing seam; no parallel `CategoryAdapterV2` or competing abstraction is authorized.** The existing 4-method contract, this ADR's own ratified invariants, and the existing (dormant) `src/adapters/BookAdapter.js` skeleton (122 lines, `detectKeyValue(edition, signed, author)` defined, zero call sites — created the same day as `ComicAdapter.js`, 2026-06-06) are ratified as the correct foundation for U3. No evidence found anywhere in the repository justifying a second, competing interface. U3 itself (reconciling this contract with GK-147's still-open items, and formalizing the category-vs-marketplace-adapter split named in `docs/U1-UNIVERSAL-CENSUS.md` C10) is explicitly **not** performed by this amendment — this ruling only ratifies U3's target and non-negotiable constraint (amend, don't replace) ahead of that work.

**U3 scope boundary (2026-09-21, ratified explicitly rather than left implicit):** U3 is contract-reconciliation-only — amending this ADR and `docs/ASSETCORE_INTERFACE.md` so they accurately describe live Production behavior, and formalizing the category-vs-marketplace-adapter split. U3 does **not**: move logic out of `api/enrich.js`, extract pricing math into the adapter interface, redesign pricing, or touch `GK-230`/`GK-232`/`GK-233`. That extraction work — actually relocating comic-specific logic behind the adapter boundary — belongs to U5 (Ruling 41), not U3. A U3 dispatch that starts moving code is out of its own ratified scope.

**Ruling 40 — Generic Asset Mode (U4) is the universal kernel operating with NO Category Adapter attached; it is not a "GenericAdapter" and does not trigger Ruling 31.** For an adapter-absent physical asset: automated identity/condition/valuation are `UNKNOWN` (a legal, first-class state per ADR-ASSET-001), operator-supplied facts are permitted, physical evidence (media) is durable, and kernel economics (acquisition, valuation via `method:'operator-override'`, inventory, outcomes) remain fully usable. U4 therefore does **not** instantiate a second adapter and does **not** trigger Ruling 31's requirement that GK-147 be resolved first — the first actual second *category adapter* (an implementation of this ADR's `detectKeyValue`/`verifyStory`/`computeEraRisk`/`sanitizeFormatTitle`-shaped interface for a new category) remains U6, still correctly gated on GK-147/Ruling 31 resolution via U5 (Ruling 41, below).

*Compatibility check against Ruling 31's own exact wording, performed explicitly before ratifying Ruling 40, not assumed:* Ruling 31's stated concern, in its own words, is a *second adapter... built on top of an AssetCore that secretly assumes comic-shaped data*, risking that "a hypothetical `BookAdapter.js` would either inherit the comic-specific bugs unknowingly, or need its own parallel patches" — i.e., the risk is specific to a new implementation of *this ADR's own AssetCore-facing interface* (the `detectKeyValue`/`verifyStory`/`computeEraRisk`/`sanitizeFormatTitle` shape named in Ruling 30 and this document's Context section). Generic Asset Mode, as scoped by U1/U4, implements none of that interface and never calls `src/lib/identityCore.js`/`pricingEngine.js`/`decisionEngine.js` (AssetCore) at all for an adapter-absent asset — it operates entirely within the GrailKey physical-asset kernel (`gk_asset`, `media`, `valuation_event` with `method:'operator-override'`, `collection_item.attributes`). On that basis, Ruling 40 is **ratified as compatible with Ruling 31's actual wording, not merely with an assumed intent.** One residual textual ambiguity is disclosed, not treated as resolved by silence: Ruling 31's parenthetical "(or any future adapter)" is broad enough that a future, stricter reading could in principle try to characterize Generic Asset Mode itself as "an adapter" — this amendment's position, recorded here explicitly rather than left implicit, is that Ruling 31's own stated rationale (comic-shaped-bug inheritance risk via the AssetCore interface) does not apply to a mode that never reaches that interface, and this is what makes Ruling 40 defensible rather than a reinterpretation-by-assumption.

**Ruling 41 — U5 must absorb or explicitly resolve/re-scope GK-147 and Ruling 31; the domain-leakage work may not silently disappear under a new phase name.** Whatever dispatch performs the ComicAdapter-compatibility extraction (U5, per `docs/U1-UNIVERSAL-CENSUS.md` C14) must, as part of its own acceptance criteria, either (a) fix all four items GK-147 named (moving them behind the adapter interface, per Ruling 30), or (b) explicitly re-scope each remaining item with its own ticket and a stated rationale for why it can wait — silently completing U5 without touching GK-147's status is not an acceptable outcome under this ADR. U5's regression requirement, ratified: the real exported Production fixture corpus already banked under GK-231/236/237/238 (e.g. `comic-vault-fixture-corpus-2026-09-20 (1111.json` and its siblings) is the required regression harness — every banked comic fixture must replay to an equivalent result before and after extraction. No pricing redesign may be mixed into the extraction itself; GK-230/GK-232/GK-233 remain untouched, carried through exactly as they are today, per this same discipline already used successfully by the GK-238 dispatch.

## Related Tickets

- GK-147 — OPEN/exit-gate-ADAPTER (per amendment A2) — the founding finding and the literal gate this ADR enforces.
- GK-150 — OPEN/remediation-ADAPTER (per amendment A2) — fixed by Ruling 32 (listing-as-projection), implementation not yet built.
- GK-241 — fixed at both application and database layers, deployed to Production (per Amendment A3's own review packet, `docs/TICKET-REGISTRY.md`) — `src/modules/buyer/service.js`'s `marketStanding` enum whitelist predated and fell further behind GK-238's `NO_SOLD_EVIDENCE` value; unrelated to the adapter seam itself, cross-referenced here only because it was found during the same review pass. Fresh Production Buyer Decision acceptance remains deferred to normal operation — see `docs/TICKET-REGISTRY.md`'s own operational acceptance check.

## Supersession

None. New ADR, formalizes intent CLAUDE.md's own AssetCore documentation already stated but GK-147 proved was not yet actually true. Amendment A3 (above) does not supersede Rulings 30-34/37 — it ratifies their current compliance status and adds Rulings 38-41 additively.
