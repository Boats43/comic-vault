# Copy to .env (gitignored, same directory) before running `docker compose up`.
# This password protects a container bound to 127.0.0.1 only, holding a
# public CC BY-SA 4.0 metadata dump — local-dev strength is sufficient.
GCD_STAGING_MYSQL_ROOT_PASSWORD=change-me-local-only
